{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Jubilee Powder Documentation","text":"<p>Welcome to Jubilee Powder documentation. This system provides automated powder dispensing and hardness testing workflows on the Jubilee Motion Platform for precision laboratory tasks.</p>"},{"location":"#what-is-jubilee-powder","title":"What is Jubilee Powder?","text":"<p>Jubilee Powder is a Python-based system that enables programmatic control of the Jubilee Motion Platform for automated powder dispensing and hardness testing operations. It provides high-level APIs, a browser UI, and validated motion workflows for safe lab operation.</p>"},{"location":"#quick-navigation","title":"Quick Navigation","text":"OperatorsDevelopers <p>If you are looking to use the Jubilee system for laboratory work:</p> <ul> <li>Start here: Quick Start Guide</li> <li>Safety and best practices: Best Practices</li> <li>Learn concepts: Architecture Overview</li> <li>Follow recipes: How-To Guides</li> <li>Use the Automation UI: Automation UI Guide</li> <li>Use the Web UI: Web Interface Guide</li> <li>Read LCD displays: LCD Display Reading Guide</li> </ul> <p>If you are looking to extend or modify the system:</p> <ul> <li>Core API: JubileeManager Reference</li> <li>State Machine: MotionPlatformStateMachine Reference</li> <li>Frontend Store: Jubilee Store Reference</li> <li>LCD Reader: HardnessTester Reference</li> <li>All APIs: Complete API Reference</li> </ul>"},{"location":"#whats-important","title":"What's Important?","text":""},{"location":"#automation-ui-recommended-for-most-users","title":"Automation UI (Recommended for Most Users)","text":"<p>The Automation UI provides a touchscreen-friendly browser interface:</p> <ul> <li>Visual well selection and configuration</li> <li>Real-time progress monitoring</li> <li>Hardware configuration without code</li> <li>Built-in safety checklist</li> <li>Live weight display</li> </ul> <p>Best choice for interactive operation</p> <p>Use the Automation UI for day-to-day dispensing and hardness jobs, monitoring, and hardware setup without writing scripts.</p>"},{"location":"#python-api-jubileemanager","title":"Python API: JubileeManager","text":"<p>The <code>JubileeManager</code> class is your main programming entry point:</p> <ul> <li>Safe, validated movements through an internal state machine</li> <li>High-level operations (dispense, weigh, move)</li> <li>Connection management for all hardware components</li> <li>Error handling and safety checks</li> </ul> <p>Use for scripted automation</p> <p>Choose JubileeManager when you need custom workflows, batch scripts, or integration with external tools.</p>"},{"location":"#frontend-store-jubilee-store","title":"Frontend Store: Jubilee Store","text":"<p>The <code>Jubilee Store</code> coordinates browser state with the backend:</p> <ul> <li>MVVM-inspired architecture using Zustand</li> <li>Callback system for real-time updates</li> <li>Job execution management</li> <li>Thread-safe operations</li> </ul> <p>Frontend extension point</p> <p>Use the Jubilee Store when customizing or extending the web frontend.</p>"},{"location":"#advanced-control-motionplatformstatemachine","title":"Advanced Control: MotionPlatformStateMachine","text":"<p>The <code>MotionPlatformStateMachine</code> provides granular control when needed:</p> <p>Advanced use only</p> <p>Direct state machine access is for complex sequences, lower-level movement primitives, and custom validation logic. Only use this if JubileeManager does not provide what you need.</p>"},{"location":"#lcd-display-reading-hardnesstester","title":"LCD Display Reading: HardnessTester","text":"<p>The <code>HardnessTester</code> reads 7-segment LCD displays using segment detection:</p> <ul> <li>Segment-based recognition (no OCR required)</li> <li>Grayscale, sharpening, CLAHE, and threshold preprocessing pipeline</li> <li>Works with low-contrast displays</li> <li>Calibration system for accurate reading</li> <li>Fast and lightweight (no ML dependencies)</li> </ul>"},{"location":"#simple-examples","title":"Simple Examples","text":""},{"location":"#powder-dispensing","title":"Powder Dispensing","text":"<p>Here's a minimal example of using JubileeManager to perform a powder dispense operation:</p> <pre><code>from src.JubileeManager import JubileeManager\n\n# Create and connect to the powder dispensing system\nmanager = JubileeManager(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10\n)\n\n# Connect to hardware\nif manager.connect(\n    machine_address=\"192.168.1.100\",\n    scale_port=\"/dev/ttyUSB0\"\n):\n    print(\"Connected successfully!\")\n\n    # Perform a dispense operation\n    success = manager.dispense_to_well(\n        well_id=\"0\",\n        target_weight=50.0  # grams\n    )\n\n    if success:\n        print(\"Dispense complete!\")\n\n    # Clean up\n    manager.disconnect()\n</code></pre>"},{"location":"#lcd-display-reading","title":"LCD Display Reading","text":"<p>Here's a minimal example of reading a 7-segment LCD display:</p> <pre><code>from src.HardnessTester import HardnessTester\nfrom src.ConfigLoader import config\n\n# Build from system_config.json (lcd_calibration_path per tester mode)\nreader = HardnessTester.from_system_config(\n    tester_mode=\"shore_a\",\n    cfg=config.system.hardness_testers.shore_a,\n)\n\nif reader.load_assigned_calibration():\n    result = reader.read_display()\n    if result and \"?\" not in result:\n        print(f\"LCD shows: {result}\")\n    else:\n        print(\"Reading failed or unclear\")\n</code></pre>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Web Automation UI: Modern interface for interactive powder dispensing and hardness testing</li> <li>Python API: Full programmatic control for automation</li> <li>Hardware Integration: Control Jubilee motion platform, scales, and dispensers</li> <li>LCD Display Reading: Segment-based recognition for 7-segment displays</li> <li>Safety Validation: All movements validated through state machine</li> <li>Flexible Configuration: JSON-based configuration system</li> <li>Job Logging: Per-job JSON records with optional Google Drive backup (CSV and images)</li> <li>MVVM Architecture: Clean separation between React views, store, and backend model</li> <li>Type Safety: Full type hints throughout the codebase</li> </ul>"},{"location":"#system-architecture","title":"System Architecture","text":"<pre><code>graph TD\n    A[Automation UI / User Scripts] --&gt; B[Jubilee Store]\n    A --&gt; C[JubileeManager]\n    A --&gt; I[HardnessTester]\n    B --&gt; L[FastAPI Server]\n    L --&gt; C\n    C --&gt; D[MotionPlatformStateMachine]\n    D --&gt; E[Jubilee Machine]\n    D --&gt; F[Scale]\n    D --&gt; G[PistonDispenser]\n    C --&gt; H[Manipulator]\n    H --&gt; D\n    I --&gt; J[Camera]\n    I --&gt; K[Hardness Display]</code></pre> <p>The system uses a layered architecture where:</p> <ol> <li>Automation UI / User Scripts interact with the store, API, and core managers</li> <li>Jubilee Store + FastAPI coordinate browser workflows and telemetry</li> <li>JubileeManager coordinates high-level powder dispensing operations</li> <li>HardnessTester reads segmented displays and controls tester interactions</li> <li>MotionPlatformStateMachine validates and executes movements</li> <li>Hardware Components (Jubilee, Scale, Dispensers, Camera) perform physical actions</li> </ol>"},{"location":"#see-also","title":"See Also","text":"<ul> <li>New to the system? Quick Start Guide</li> <li>Ready to use it? How-To Guides</li> <li>Need API details? API Reference</li> <li>Want to understand the design? Architecture Guide</li> <li>Terminology questions? Glossary</li> </ul>"},{"location":"#getting-help","title":"Getting Help","text":"<p>If you encounter issues or have questions:</p> <ol> <li>Check the Glossary for terminology</li> <li>Review the How-To Guides for common tasks</li> <li>Consult the API Reference for detailed function documentation</li> </ol>"},{"location":"api/config-loader/","title":"ConfigLoader API Reference","text":"<p><code>ConfigLoader</code> is the typed loader for <code>jubilee_api_config/system_config.json</code>. It validates machine, server, safety, manipulator, trickler, and hardness-tester settings at import time and exposes them through Pydantic models and typed accessors.</p> <p>Singleton in production</p> <p>Import the module-level <code>config</code> singleton in application code. Use <code>ConfigLoader.from_file()</code> only in tests or tooling that needs an alternate config path.</p>"},{"location":"api/config-loader/#overview","title":"Overview","text":"<p><code>ConfigLoader</code>:</p> <ul> <li>Loads and validates config using Pydantic models</li> <li>Exposes typed access via <code>config.system</code> and helper getters</li> <li>Fails fast with <code>ConfigError</code> on missing or invalid required fields</li> </ul> Config file Loader Primary access <code>jubilee_api_config/system_config.json</code> <code>ConfigLoader</code> <code>config.system</code>, typed getters <code>jubilee_api_config/motion_platform_positions.json</code> <code>PositionRegistry</code> (<code>src/motion_config.py</code>) Position Configuration"},{"location":"api/config-loader/#class-reference","title":"Class Reference","text":""},{"location":"api/config-loader/#src.ConfigLoader","title":"ConfigLoader","text":"<p>Typed loader for <code>api_config/system_config.json</code>.</p> <p>The module exposes a process-wide <code>config</code> singleton that validates machine settings at import time. Use :class:<code>ConfigLoader</code> directly only when loading alternate paths in tests via :meth:<code>ConfigLoader.from_file</code>.</p> <p>For position and action definitions, see <code>api_config/motion_platform_positions.json</code> and :class:<code>~src.MotionPlatformStateMachine.PositionRegistry</code>.</p> Example <p>Read validated settings from the singleton::</p> <pre><code>from src.ConfigLoader import config\n\nduet_ip = config.get_duet_ip()\nfeedrate = config.system.machine.default_feedrate\n</code></pre> Note <p>Required fields must live in JSON. This loader does not substitute inline defaults for missing machine config.</p>"},{"location":"api/config-loader/#src.ConfigLoader-attributes","title":"Attributes","text":""},{"location":"api/config-loader/#src.ConfigLoader.DEFAULT_PROFILE_NAME","title":"DEFAULT_PROFILE_NAME  <code>module-attribute</code>","text":"<pre><code>DEFAULT_PROFILE_NAME = 'Default'\n</code></pre>"},{"location":"api/config-loader/#src.ConfigLoader.config","title":"config  <code>module-attribute</code>","text":"<pre><code>config = ConfigLoader()\n</code></pre>"},{"location":"api/config-loader/#src.ConfigLoader-classes","title":"Classes","text":""},{"location":"api/config-loader/#src.ConfigLoader.ConfigError","title":"ConfigError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when <code>system_config.json</code> is missing, malformed, or fails validation.</p>"},{"location":"api/config-loader/#src.ConfigLoader.PathsConfig","title":"PathsConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Filesystem paths section (<code>paths</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.SafetyConfig","title":"SafetyConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Dispense safety limits (<code>safety</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ServerConfig","title":"ServerConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Web server and simulation settings (<code>server</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.MachineConfig","title":"MachineConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Duet controller and hardware counts (<code>machine</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ToolRef","title":"ToolRef","text":"<p>               Bases: <code>BaseModel</code></p> <p>Tool index and registered name used by the Jubilee firmware.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ToolsConfig","title":"ToolsConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Registered toolheads (<code>tools</code>). Extra tool keys are allowed.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ManipulatorConfig","title":"ManipulatorConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Tamper axis letter and depth/speed limits (<code>manipulator</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessTestingConfig","title":"HardnessTestingConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Runtime hardness-testing behavior toggles (<code>hardness_testing</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.TricklerConfig","title":"TricklerConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Powder trickler tuning parameters (<code>trickler</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.PowderDispenserCoverConfig","title":"PowderDispenserCoverConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Servo configuration for powder dispenser cover actuation.</p>"},{"location":"api/config-loader/#src.ConfigLoader.GoogleDriveConfig","title":"GoogleDriveConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Optional Google Drive backup settings (<code>google_drive</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.GoogleDriveConfig-functions","title":"Methods:","text":""},{"location":"api/config-loader/#src.ConfigLoader.GoogleDriveConfig.folder_id_when_enabled","title":"folder_id_when_enabled","text":"<pre><code>folder_id_when_enabled()\n</code></pre> <p>Require a non-empty folder id when backup is enabled.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ButtonServosConfig","title":"ButtonServosConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Servo angles for hardness tester front-panel buttons.</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessTesterToolConfig","title":"HardnessTesterToolConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Hardness tester tool registration (index and name).</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessTesterConfig","title":"HardnessTesterConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-mode hardness tester hardware and calibration settings.</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessTestersConfig","title":"HardnessTestersConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Shore-A and Shore-D hardness tester profiles (<code>hardness_testers</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessTesterProfileConfig","title":"HardnessTesterProfileConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-tester hardness settings inside a shared named profile.</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessProfileConfig","title":"HardnessProfileConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Shared hardness profile with per-tester hardware sections.</p>"},{"location":"api/config-loader/#src.ConfigLoader.TricklerProfilesConfig","title":"TricklerProfilesConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Named trickler profile library with active selection.</p>"},{"location":"api/config-loader/#src.ConfigLoader.HardnessProfilesConfig","title":"HardnessProfilesConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Named hardness profile library with active selection.</p>"},{"location":"api/config-loader/#src.ConfigLoader.SystemConfig","title":"SystemConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Root Pydantic model for <code>system_config.json</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader","title":"ConfigLoader","text":"<pre><code>ConfigLoader()\n</code></pre> <p>Singleton loader for validated <code>system_config.json</code>.</p> <p>Access the process-wide instance via the module-level <code>config</code> object or construct with <code>ConfigLoader()</code> (returns the same singleton). For tests, use :meth:<code>from_file</code> or :meth:<code>reset_singleton_for_tests</code>.</p> ATTRIBUTE DESCRIPTION <code>system</code> <p>Validated :class:<code>SystemConfig</code> parsed from disk.</p> <p> TYPE: <code>SystemConfig</code> </p> Note <p>Prefer <code>config.system.&lt;section&gt;.&lt;field&gt;</code> when reading several values from one section; use typed getters for single fields.</p> <p>Load <code>system_config.json</code> from the project <code>api_config</code> directory.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader-attributes","title":"Attributes","text":""},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.project_root","title":"project_root  <code>property</code>","text":"<pre><code>project_root\n</code></pre> <p>Repository root directory (parent of <code>src/</code>).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.system","title":"system  <code>property</code>","text":"<pre><code>system\n</code></pre> <p>Validated root config model.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.trickler_profiles","title":"trickler_profiles  <code>property</code>","text":"<pre><code>trickler_profiles\n</code></pre> <p>Validated trickler profile library.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.hardness_profiles","title":"hardness_profiles  <code>property</code>","text":"<pre><code>hardness_profiles\n</code></pre> <p>Validated hardness profile library.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader-functions","title":"Methods:","text":""},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.__new__","title":"__new__","text":"<pre><code>__new__()\n</code></pre> <p>Return the singleton instance, creating it on first access.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(config_path, project_root=None)\n</code></pre> <p>Load config from an arbitrary path without touching the singleton.</p> PARAMETER DESCRIPTION <code>config_path</code> <p>Path to a <code>system_config.json</code> file.</p> <p> TYPE: <code>Path</code> </p> <code>project_root</code> <p>Project root used to resolve relative paths in config. When omitted, inferred from <code>config_path</code> when it lives under <code>api_config/</code>.</p> <p> TYPE: <code>Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>'ConfigLoader'</code> <p>A standalone :class:<code>ConfigLoader</code> instance bound to <code>config_path</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.reset_singleton_for_tests","title":"reset_singleton_for_tests  <code>classmethod</code>","text":"<pre><code>reset_singleton_for_tests()\n</code></pre> <p>Clear the singleton so the next <code>ConfigLoader()</code> reloads from disk.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_api_config_dir","title":"get_api_config_dir","text":"<pre><code>get_api_config_dir()\n</code></pre> <p>Return the <code>api_config</code> directory under :attr:<code>project_root</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_system_config_path","title":"get_system_config_path","text":"<pre><code>get_system_config_path()\n</code></pre> <p>Return the resolved path to the loaded <code>system_config.json</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_profiles_path","title":"get_trickler_profiles_path","text":"<pre><code>get_trickler_profiles_path()\n</code></pre> <p>Return the resolved path to <code>trickler_profiles.json</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_hardness_profiles_path","title":"get_hardness_profiles_path","text":"<pre><code>get_hardness_profiles_path()\n</code></pre> <p>Return the resolved path to <code>hardness_profiles.json</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_motion_config_path","title":"get_motion_config_path","text":"<pre><code>get_motion_config_path()\n</code></pre> <p>Return the path to <code>motion_platform_positions.json</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_active_trickler_profile","title":"get_active_trickler_profile","text":"<pre><code>get_active_trickler_profile()\n</code></pre> <p>Return the currently selected trickler profile.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_active_hardness_profile","title":"get_active_hardness_profile","text":"<pre><code>get_active_hardness_profile()\n</code></pre> <p>Return the currently selected hardness profile.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.set_trickler_profiles","title":"set_trickler_profiles","text":"<pre><code>set_trickler_profiles(payload)\n</code></pre> <p>Validate and persist full trickler profile library payload.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.set_hardness_profiles","title":"set_hardness_profiles","text":"<pre><code>set_hardness_profiles(payload)\n</code></pre> <p>Validate and persist full hardness profile library payload.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_job_files_dir","title":"get_job_files_dir","text":"<pre><code>get_job_files_dir()\n</code></pre> <p>Return <code>paths.job_files_dir</code>, resolved relative to :attr:<code>project_root</code> when needed.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_max_weight_per_well","title":"get_max_weight_per_well","text":"<pre><code>get_max_weight_per_well()\n</code></pre> <p>Return <code>safety.max_weight_per_well</code> in grams.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_weight_tolerance","title":"get_weight_tolerance","text":"<pre><code>get_weight_tolerance()\n</code></pre> <p>Return <code>safety.weight_tolerance</code> in grams.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_duet_ip","title":"get_duet_ip","text":"<pre><code>get_duet_ip()\n</code></pre> <p>Return <code>machine.duet_ip</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_default_feedrate","title":"get_default_feedrate","text":"<pre><code>get_default_feedrate()\n</code></pre> <p>Return <code>machine.default_feedrate</code> (mm/min).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamper_travel_position","title":"get_tamper_travel_position","text":"<pre><code>get_tamper_travel_position()\n</code></pre> <p>Return <code>machine.tamper_travel_position</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_scale_port","title":"get_scale_port","text":"<pre><code>get_scale_port()\n</code></pre> <p>Return <code>machine.scale_port</code> serial device path.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_num_dispensers","title":"get_num_dispensers","text":"<pre><code>get_num_dispensers()\n</code></pre> <p>Return <code>machine.num_dispensers</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_pistons_per_dispenser","title":"get_pistons_per_dispenser","text":"<pre><code>get_pistons_per_dispenser()\n</code></pre> <p>Return <code>machine.pistons_per_dispenser</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamp_depth_min","title":"get_tamp_depth_min","text":"<pre><code>get_tamp_depth_min()\n</code></pre> <p>Return <code>manipulator.tamp_depth_min</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamp_depth_max","title":"get_tamp_depth_max","text":"<pre><code>get_tamp_depth_max()\n</code></pre> <p>Return <code>manipulator.tamp_depth_max</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamp_speed_min","title":"get_tamp_speed_min","text":"<pre><code>get_tamp_speed_min()\n</code></pre> <p>Return <code>manipulator.tamp_speed_min</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamp_speed_max","title":"get_tamp_speed_max","text":"<pre><code>get_tamp_speed_max()\n</code></pre> <p>Return <code>manipulator.tamp_speed_max</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_tamp_defaults","title":"get_tamp_defaults","text":"<pre><code>get_tamp_defaults()\n</code></pre> <p>Return default tamp depth and speed from <code>manipulator</code> config.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_hardness_monotonic_drop_check_enabled","title":"get_hardness_monotonic_drop_check_enabled","text":"<pre><code>get_hardness_monotonic_drop_check_enabled()\n</code></pre> <p>Return <code>hardness_testing.enable_monotonic_drop_check</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_cors_origins","title":"get_cors_origins","text":"<pre><code>get_cors_origins()\n</code></pre> <p>Return a copy of <code>server.cors_origins</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_mock_hardware","title":"get_mock_hardware","text":"<pre><code>get_mock_hardware()\n</code></pre> <p>Return <code>server.mock_hardware</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_google_drive_enabled","title":"get_google_drive_enabled","text":"<pre><code>get_google_drive_enabled()\n</code></pre> <p>Return <code>google_drive.enabled</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_google_drive_credentials_file","title":"get_google_drive_credentials_file","text":"<pre><code>get_google_drive_credentials_file()\n</code></pre> <p>Return <code>google_drive.credentials_file</code>, resolved relative to :attr:<code>project_root</code> when needed.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_google_drive_folder_id","title":"get_google_drive_folder_id","text":"<pre><code>get_google_drive_folder_id()\n</code></pre> <p>Return <code>google_drive.drive_folder_id</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_retry_interval_seconds","title":"get_retry_interval_seconds","text":"<pre><code>get_retry_interval_seconds()\n</code></pre> <p>Return <code>google_drive.retry_interval_seconds</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_flow_ema_alpha","title":"get_trickler_flow_ema_alpha","text":"<pre><code>get_trickler_flow_ema_alpha()\n</code></pre> <p>Return <code>trickler.flow_ema_alpha</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_yield_ema_alpha","title":"get_trickler_yield_ema_alpha","text":"<pre><code>get_trickler_yield_ema_alpha()\n</code></pre> <p>Return <code>trickler.yield_ema_alpha</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_jam_yield_threshold","title":"get_trickler_jam_yield_threshold","text":"<pre><code>get_trickler_jam_yield_threshold()\n</code></pre> <p>Return <code>trickler.jam_yield_threshold</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_jam_iter_threshold","title":"get_trickler_jam_iter_threshold","text":"<pre><code>get_trickler_jam_iter_threshold()\n</code></pre> <p>Return <code>trickler.jam_iter_threshold</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_jam_auto_recovery_vibration_amplitude","title":"get_trickler_jam_auto_recovery_vibration_amplitude","text":"<pre><code>get_trickler_jam_auto_recovery_vibration_amplitude()\n</code></pre> <p>Return <code>trickler.jam_auto_recovery_vibration_amplitude</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_jam_auto_recovery_wait_seconds","title":"get_trickler_jam_auto_recovery_wait_seconds","text":"<pre><code>get_trickler_jam_auto_recovery_wait_seconds()\n</code></pre> <p>Return <code>trickler.jam_auto_recovery_wait_seconds</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_max_step_size_mm","title":"get_trickler_max_step_size_mm","text":"<pre><code>get_trickler_max_step_size_mm()\n</code></pre> <p>Return <code>trickler.max_step_size_mm</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_min_step_size_mm","title":"get_trickler_min_step_size_mm","text":"<pre><code>get_trickler_min_step_size_mm()\n</code></pre> <p>Return <code>trickler.min_step_size_mm</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_warmup_steps","title":"get_trickler_warmup_steps","text":"<pre><code>get_trickler_warmup_steps()\n</code></pre> <p>Return <code>trickler.warmup_steps</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_warmup_max_step_mm","title":"get_trickler_warmup_max_step_mm","text":"<pre><code>get_trickler_warmup_max_step_mm()\n</code></pre> <p>Return <code>trickler.warmup_max_step_mm</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_coarse_threshold_pct","title":"get_trickler_coarse_threshold_pct","text":"<pre><code>get_trickler_coarse_threshold_pct()\n</code></pre> <p>Return <code>trickler.coarse_threshold_pct</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_finish_threshold_pct","title":"get_trickler_finish_threshold_pct","text":"<pre><code>get_trickler_finish_threshold_pct()\n</code></pre> <p>Return <code>trickler.finish_threshold_pct</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_coarse_target_steps","title":"get_trickler_coarse_target_steps","text":"<pre><code>get_trickler_coarse_target_steps()\n</code></pre> <p>Return <code>trickler.coarse_target_steps</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_coarse_feedrate","title":"get_trickler_coarse_feedrate","text":"<pre><code>get_trickler_coarse_feedrate()\n</code></pre> <p>Return <code>trickler.coarse_feedrate</code> (mm/min).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_fine_feedrate","title":"get_trickler_fine_feedrate","text":"<pre><code>get_trickler_fine_feedrate()\n</code></pre> <p>Return <code>trickler.fine_feedrate</code> (mm/min).</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_coarse_vibration_amplitude","title":"get_trickler_coarse_vibration_amplitude","text":"<pre><code>get_trickler_coarse_vibration_amplitude()\n</code></pre> <p>Return <code>trickler.coarse_vibration_amplitude</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_fine_vibration_amplitude","title":"get_trickler_fine_vibration_amplitude","text":"<pre><code>get_trickler_fine_vibration_amplitude()\n</code></pre> <p>Return <code>trickler.fine_vibration_amplitude</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_trickler_max_dribble_step_mm","title":"get_trickler_max_dribble_step_mm","text":"<pre><code>get_trickler_max_dribble_step_mm()\n</code></pre> <p>Return <code>trickler.max_dribble_step_mm</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_powder_dispenser_cover_servo","title":"get_powder_dispenser_cover_servo","text":"<pre><code>get_powder_dispenser_cover_servo()\n</code></pre> <p>Return <code>powder_dispenser_cover.servo</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_powder_dispenser_cover_open_angle","title":"get_powder_dispenser_cover_open_angle","text":"<pre><code>get_powder_dispenser_cover_open_angle()\n</code></pre> <p>Return <code>powder_dispenser_cover.open_angle</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader.ConfigLoader.get_powder_dispenser_cover_closed_angle","title":"get_powder_dispenser_cover_closed_angle","text":"<pre><code>get_powder_dispenser_cover_closed_angle()\n</code></pre> <p>Return <code>powder_dispenser_cover.closed_angle</code>.</p>"},{"location":"api/config-loader/#src.ConfigLoader-functions","title":"Functions:","text":""},{"location":"api/config-loader/#src.ConfigLoader._parse_system_config","title":"_parse_system_config","text":"<pre><code>_parse_system_config(raw, path)\n</code></pre> <p>Validate raw system config payload and raise <code>ConfigError</code> on failure.</p>"},{"location":"api/config-loader/#src.ConfigLoader._parse_trickler_profiles","title":"_parse_trickler_profiles","text":"<pre><code>_parse_trickler_profiles(raw, path)\n</code></pre> <p>Validate raw trickler profile payload and raise <code>ConfigError</code> on failure.</p>"},{"location":"api/config-loader/#src.ConfigLoader._parse_hardness_profiles","title":"_parse_hardness_profiles","text":"<pre><code>_parse_hardness_profiles(raw, path)\n</code></pre> <p>Validate raw hardness profile payload and raise <code>ConfigError</code> on failure.</p>"},{"location":"api/config-loader/#basic-usage","title":"Basic usage","text":"Typed gettersPydantic models <pre><code>from src.ConfigLoader import config\n\nduet_ip = config.get_duet_ip()\nscale_port = config.get_scale_port()\nfeedrate = config.get_default_feedrate()\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n</code></pre> <pre><code>from src.ConfigLoader import config\n\nduet_ip = config.system.machine.duet_ip\nmock_hw = config.system.server.mock_hardware\ncoarse_feedrate = config.system.trickler.coarse_feedrate\n</code></pre> <p>Pick one access style</p> <p>Prefer <code>config.system.&lt;section&gt;.&lt;field&gt;</code> when you need several values from the same section. Use typed getters when a single accessor is enough.</p>"},{"location":"api/config-loader/#test-only-loading","title":"Test-only loading","text":"Alternate paths in tests and fixtures <p>Production code uses the import-time singleton. For temporary or fixture configs, construct a loader explicitly:</p> <pre><code>from pathlib import Path\nfrom src.ConfigLoader import ConfigLoader\n\nloader = ConfigLoader.from_file(\n    Path(\"tmp/system_config.json\"),\n    project_root=Path(\".\"),\n)\n</code></pre>"},{"location":"api/config-loader/#required-config-rule","title":"Required config rule","text":"<p>Config is the source of truth</p> <ul> <li>Keep machine behavior in checked-in JSON under <code>jubilee_api_config/</code>.</li> <li>Do not use silent fallback defaults in production paths (<code>src/</code>, <code>frontend/src/hardware_manager.py</code>, etc.).</li> <li>Add new required fields to the corresponding Pydantic model so validation catches issues at load time.</li> </ul> <p>See the Configuration Guide for the full edit workflow.</p>"},{"location":"api/config-loader/#see-also","title":"See Also","text":"<ul> <li>Configuration Guide - editing <code>system_config.json</code></li> <li>Position Configuration - <code>motion_platform_positions.json</code> schema and transitions</li> <li>JubileeManager - primary runtime entry point that consumes this config</li> <li>MotionPlatformStateMachine - loads position config at <code>connect()</code> time</li> </ul>"},{"location":"api/hardness-tester/","title":"HardnessTester","text":"<p>The <code>HardnessTester</code> class serves two related roles:</p> <ol> <li>Shore hardness testing toolhead - picks up the tester tool, presses it into a sample, and reads the result as part of a validated state-machine workflow.</li> <li>LCD display reader - reads 7-segment LCD displays by detecting which segments are active rather than using traditional OCR. This is more reliable for low-contrast displays and requires no training data.</li> </ol> <p>Both roles are combined in one class because the tester's LCD display is its primary output channel.</p> Operator GuideAPI Reference"},{"location":"api/hardness-tester/#calibration-walkthrough","title":"Calibration Walkthrough","text":"<p>Before the system can read hardness values, the camera must be calibrated to the position of each digit on the display. Calibration is a one-time setup per physical mounting.</p>"},{"location":"api/hardness-tester/#step-1-capture-a-reference-image","title":"Step 1: Capture a reference image","text":"<pre><code>from src.HardnessTester import HardnessTester\n\ntester = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n    use_camera=True,\n)\nframe = tester.capture_image(save=True, output_path='calibration_reference.jpg')\nprint(\"Image saved - open it to identify digit pixel coordinates.\")\n</code></pre> <p>Check the saved image for:</p> <ul> <li>Digits are clearly visible and lit</li> <li>Lighting is adequate and not overexposed</li> <li>Camera is in focus</li> </ul>"},{"location":"api/hardness-tester/#step-2-run-the-interactive-calibration","title":"Step 2: Run the interactive calibration","text":"<pre><code>tester.calibrate(frame=frame, save_calibration=True)\n</code></pre> <p>The calibration process:</p> <ol> <li>Saves debug images of each preprocessing step (<code>calibration_step*.png</code>)</li> <li>Opens an interactive GUI: click-and-drag to draw a bounding box for each digit, then click to place segment polygon vertices for each of the 7 segments per digit</li> <li>Saves the calibration to the configured <code>calibration_path</code></li> <li>Immediately tests the calibration and prints a result (saves <code>calibration_test_step*.png</code>)</li> </ol> <p>Tip</p> <p>Open <code>calibration_step5_binary.png</code> in an image viewer. In the binary image, active LCD segments appear as dark regions on a bright background. Use this to verify that the preprocessing is enhancing the segments correctly before confirming calibration.</p>"},{"location":"api/hardness-tester/#step-3-verify","title":"Step 3: Verify","text":"<p>After calibration, run a standalone read to confirm the result:</p> <pre><code>if tester.load_assigned_calibration():\n    result = tester.read_display()\n    print(f\"Display reads: {result}\")\nelse:\n    print(\"Calibration file not found - re-run calibration.\")\n</code></pre>"},{"location":"api/hardness-tester/#reading-displays","title":"Reading Displays","text":""},{"location":"api/hardness-tester/#recommended-via-jubileemanager","title":"Recommended: via JubileeManager","text":"<p>When running a full hardness testing session through <code>JubileeManager</code>, you do not call <code>read_display()</code> directly. Instead call:</p> <pre><code># Turn on the tester, zero it, then test a sample\nmanager.hardness_turn_on(mode=\"shore_a\")  # actuate power button\nmanager.hardness_zero(mode=\"shore_a\")     # actuate zero button\n\nsuccess = manager.test_sample(\n    tray_index=0,\n    sample_index=3,\n    mode=\"shore_a\",\n)\nif success:\n    print(f\"Result: {manager.last_hardness_result}\")  # e.g. 42.5\n    print(f\"Error: {manager.last_hardness_error}\")    # None if clean read\n</code></pre> <p>The <code>test_sample</code> method handles moving to the sample position, pressing the tester into the sample, reading the display, and logging the result.</p>"},{"location":"api/hardness-tester/#standalone-use-no-jubileemanager","title":"Standalone use (no JubileeManager)","text":"<p>Use <code>from_system_config</code> to build a configured instance from <code>system_config.json</code>:</p> <pre><code>from src.HardnessTester import HardnessTester\nfrom src.ConfigLoader import config\n\ntester = HardnessTester.from_system_config(\n    tester_mode=\"shore_a\",\n    hardware_cfg=config.system.hardness_testers.shore_a,\n    profile_cfg=config.get_active_hardness_profile(),\n)\n\nif tester.load_assigned_calibration():\n    result = tester.read_display()\n    print(f\"Display: {result}\")\n</code></pre> <p>Or construct directly for testing purposes:</p> <pre><code>tester = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n    use_camera=True,\n)\ntester.load_calibration(\"jubilee_api_config/lcd_calibration_shore_a.json\")\nresult = tester.read_display()\n</code></pre>"},{"location":"api/hardness-tester/#reading-from-a-static-image","title":"Reading from a static image","text":"<p>Useful for debugging calibration without a live camera:</p> <pre><code>from src.HardnessTester import test_with_image\n\nresult = test_with_image(\n    \"lcd_photo.jpg\",\n    \"jubilee_api_config/lcd_calibration_shore_a.json\",\n)\nprint(f\"Result: {result}\")\n</code></pre>"},{"location":"api/hardness-tester/#checking-display-state","title":"Checking Display State","text":"<p>Two helper methods let you confirm the display is on and zeroed before testing:</p> <pre><code># Returns True if the display reads a valid number, False if OFF\n# Raises RuntimeError if indeterminate\nis_on = tester.is_display_on()\n\n# Returns True if the display reads 0-5 (effectively zeroed)\nis_zeroed = tester.is_display_zero()\n</code></pre>"},{"location":"api/hardness-tester/#adjusting-sensitivity","title":"Adjusting Sensitivity","text":"<p>The <code>segment_threshold</code> attribute controls how many pixels in a segment region must be active before the segment is counted as ON. The default is <code>0.5</code> (50%).</p> <pre><code>tester.segment_threshold = 0.3  # more sensitive - detects dimmer segments\ntester.segment_threshold = 0.7  # less sensitive - only bright segments\n</code></pre> <p>Lowering the threshold helps when lighting is dim or the display contrast is low. Raising it reduces false positives from stray light.</p>"},{"location":"api/hardness-tester/#troubleshooting","title":"Troubleshooting","text":""},{"location":"api/hardness-tester/#all-digits-show","title":"All digits show <code>?</code>","text":"<p>The segments are not being recognized. Check:</p> <ol> <li>Run with debug=True: <code>tester.read_display(debug=True, debug_prefix=\"debug\")</code> saves <code>debug_step*.png</code> images.</li> <li>Review the binary image (<code>debug_step5_binary.png</code>): active segments should be dark on a bright background.</li> <li>Re-run calibration if the camera has been moved.</li> <li>Adjust threshold: try <code>tester.segment_threshold = 0.3</code>.</li> </ol>"},{"location":"api/hardness-tester/#display-reads-off","title":"Display reads <code>OFF</code>","text":"<p>The tester is powered off or the segments are not lit. Power on the tester and wait for it to settle before reading.</p>"},{"location":"api/hardness-tester/#reading-is-inconsistent-between-calls","title":"Reading is inconsistent between calls","text":"<p><code>read_display()</code> with no arguments runs a 10-sample consensus read (captures 10 frames over ~1 second and returns the strict majority). If no majority is reached, it returns <code>None</code>. This is intentional: a <code>None</code> result means the display value is genuinely changing or the camera cannot achieve a consistent view.</p> <ul> <li>Check that the tester has finished settling after a sample press.</li> <li>Check for external vibration or changing lighting.</li> </ul>"},{"location":"api/hardness-tester/#reading-changes-with-lighting","title":"Reading changes with lighting","text":"<p>Camera auto-exposure may be adjusting between captures. On Raspberry Pi with Picamera2, the camera settings are fixed after the first capture. Ensure the camera is fully initialized and stabilized before the first read.</p>"},{"location":"api/hardness-tester/#limitations","title":"Limitations","text":"<ul> <li>Only digits 0-9 are recognized. No decimal point, negative sign, or unit symbols.</li> <li>Decimal position must be known in advance. The display on Shore testers has one implied decimal place: a raw display read of <code>\"0425\"</code> corresponds to 42.5 Shore units. The <code>test_sample()</code> method applies this scaling automatically.</li> <li><code>read_display()</code> does not detect partially lit segments. If a segment is damaged or dimly lit, it will be counted based on the threshold.</li> </ul>"},{"location":"api/hardness-tester/#performance","title":"Performance","text":"<p>Typical performance on Raspberry Pi 4:</p> <ul> <li>Calibration: 5-10 seconds (one-time setup)</li> <li>Single frame read: 100-200 ms</li> <li>Consensus read (10 frames, default): ~1 second</li> <li>With debug images enabled: add 300-500 ms</li> </ul> <p>On a desktop PC: single frame read 20-50 ms.</p>"},{"location":"api/hardness-tester/#class-reference","title":"Class Reference","text":""},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester","title":"HardnessTester","text":"<pre><code>HardnessTester(\n    calibration_path,\n    num_digits=4,\n    cam_usb_path=None,\n    use_camera=True,\n    bypass_cv=False,\n    monotonic_drop_threshold=0.0,\n    enable_monotonic_drop_check=False,\n    index=1,\n    name=\"HardnessTester\",\n    tester_mode=\"shore_a\",\n    servo=None,\n    power_press_angle=None,\n    power_release_angle=None,\n    zero_press_angle=None,\n    zero_release_angle=None,\n    state_machine=None,\n    threshold_bias=15,\n    sharpen_strength=31,\n    sharpen_blur_radius=90,\n    morph_kernel_size=5,\n    morph_iterations=3,\n    morph_open=False,\n)\n</code></pre> <p>               Bases: <code>Tool</code></p> <p>Shore hardness tester toolhead with segment-based LCD reading.</p> <p>Serves as both a Jubilee <code>Tool</code> for sample testing workflows and a camera-driven reader for the tester's 7-segment LCD display.</p> <p>Configuration is loaded from <code>system_config.json</code> via :meth:<code>from_system_config</code>. LCD geometry comes from a calibration JSON file produced by :meth:<code>calibrate</code>.</p> ATTRIBUTE DESCRIPTION <code>num_digits</code> <p>Number of digits on the LCD.</p> <p> </p> <code>segment_threshold</code> <p>Fraction of segment pixels that must be active (default 0.5) before a segment is counted ON.</p> <p> </p> <code>tester_mode</code> <p>Shore mode key (<code>\"shore_a\"</code> or <code>\"shore_d\"</code>).</p> <p> </p> Example <p>Build from config and read the display::</p> <pre><code>from src.HardnessTester import HardnessTester\nfrom src.ConfigLoader import config\n\ntester = HardnessTester.from_system_config(\n    tester_mode=\"shore_a\",\n    hardware_cfg=config.system.hardness_testers.shore_a,\n    profile_cfg=config.get_active_hardness_profile(),\n)\nif tester.load_assigned_calibration():\n    print(tester.read_display())\n</code></pre> Note <p>For full sample workflows prefer :meth:<code>~src.JubileeManager.JubileeManager.test_sample</code> rather than calling :meth:<code>test_sample</code> on this class directly.</p> <p>Initialize the Shore tester and LCD reader.</p> PARAMETER DESCRIPTION <code>calibration_path</code> <p>Path to the LCD calibration JSON for this tester.</p> <p> TYPE: <code>str</code> </p> <code>num_digits</code> <p>Number of digits in the display (default 4).</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p> <code>cam_usb_path</code> <p>Filesystem path to the camera USB device (for example <code>/dev/v4l/by-path/...-video-index0</code>). When <code>None</code>, the camera cannot open until USB discrimination resolves an index.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>use_camera</code> <p>Whether to initialize and use a camera device.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>index</code> <p>Jubilee tool index for this tester.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>name</code> <p>Jubilee tool name for this tester.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'HardnessTester'</code> </p> <code>tester_mode</code> <p>Shore tester mode (<code>\"shore_a\"</code> or <code>\"shore_d\"</code>).</p> <p> TYPE: <code>str</code> DEFAULT: <code>'shore_a'</code> </p> <code>servo</code> <p>Servo identifier for the shared button servo (e.g. \"S1\" = servo channel 1). The same physical servo actuates both the power and zero buttons; the button is selected by angle range.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>power_press_angle</code> <p>Servo angle in degrees when pressing the power button</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>power_release_angle</code> <p>Servo angle in degrees when releasing the power button</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>zero_press_angle</code> <p>Servo angle in degrees when pressing the zero button</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>zero_release_angle</code> <p>Servo angle in degrees when releasing the zero button</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>state_machine</code> <p>Optional MotionPlatformStateMachine reference. When set, turn_on(), turn_off(), and zero() use it directly without requiring a per-call argument (same pattern as Manipulator).</p> <p> TYPE: <code>object | None</code> DEFAULT: <code>None</code> </p> <code>threshold_bias</code> <p>Amount subtracted from Otsu's threshold before binarization (default 15). Increase to classify fewer pixels as active when reflections create spurious dark regions.</p> <p> TYPE: <code>int</code> DEFAULT: <code>15</code> </p> <code>sharpen_strength</code> <p>Unsharp masking strength applied to the grayscale image before CLAHE. On by default (31.0). Set to 0.0 to disable sharpening. Values in the range 1.0-2.0 work well for mildly blurry images; go higher (e.g. 3.0) for strongly blurred captures. Higher values risk amplifying noise, so pair with a larger sharpen_blur_radius if needed.</p> <p> TYPE: <code>float</code> DEFAULT: <code>31</code> </p> <code>sharpen_blur_radius</code> <p>Gaussian blur radius (odd integer) used when computing the unsharp mask. Larger values recover lower-frequency blur (wider blurred edges); smaller values target fine detail. Default is 5.</p> <p> TYPE: <code>int</code> DEFAULT: <code>90</code> </p> <code>morph_kernel_size</code> <p>Side length of the rectangular structuring element used in morphological closing (and optional opening). Larger values bridge bigger gaps within segments and remove larger noise regions. Default is 2.</p> <p> TYPE: <code>int</code> DEFAULT: <code>5</code> </p> <code>morph_iterations</code> <p>Number of times to apply each morphological operation. More iterations push the effect further without needing a huge kernel. Default is 1.</p> <p> TYPE: <code>int</code> DEFAULT: <code>3</code> </p> <code>morph_open</code> <p>If True, runs a morphological opening (erode then dilate) before the closing pass. This scrubs isolated noise blobs first so they are not accidentally merged into segments by the close. Default is False.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester-functions","title":"Methods:","text":""},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.from_system_config","title":"from_system_config  <code>classmethod</code>","text":"<pre><code>from_system_config(\n    tester_mode,\n    hardware_cfg,\n    profile_cfg,\n    state_machine=None,\n    enable_monotonic_drop_check=False,\n)\n</code></pre> <p>Build a configured Shore tester from validated system config.</p> PARAMETER DESCRIPTION <code>tester_mode</code> <p>Shore tester key (<code>\"shore_a\"</code> or <code>\"shore_d\"</code>).</p> <p> TYPE: <code>str</code> </p> <code>hardware_cfg</code> <p>Physical tester identity and tool config from <code>ConfigLoader.system.hardness_testers</code>.</p> <p> TYPE: <code>HardnessTesterConfig</code> </p> <code>profile_cfg</code> <p>Shared, named hardness profile selected from <code>hardness_profiles.json</code>.</p> <p> TYPE: <code>HardnessProfileConfig</code> </p> <code>state_machine</code> <p>Optional <code>MotionPlatformStateMachine</code> for validated button actuation and sample workflows.</p> <p> TYPE: <code>object | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>HardnessTester</code> <p>Configured <code>HardnessTester</code> instance.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.load_assigned_calibration","title":"load_assigned_calibration","text":"<pre><code>load_assigned_calibration()\n</code></pre> <p>Load the LCD calibration file configured for this tester mode.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if :attr:<code>calibration_path</code> loaded successfully.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.load_calibration","title":"load_calibration","text":"<pre><code>load_calibration(filepath=None)\n</code></pre> <p>Load digit ROIs and segment polygons from a calibration JSON file.</p> PARAMETER DESCRIPTION <code>filepath</code> <p>Calibration JSON path. Defaults to :attr:<code>calibration_path</code>.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True when <code>digit_rois</code> and <code>segment_points</code> loaded successfully.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.calibrate","title":"calibrate","text":"<pre><code>calibrate(\n    frame=None,\n    image_path=None,\n    save_calibration=True,\n    calibration_path=None,\n)\n</code></pre> <p>Run interactive OpenCV calibration for digit ROIs and segment polygons.</p> <p>Uses :attr:<code>calibration_path</code> when <code>calibration_path</code> is omitted.</p> PARAMETER DESCRIPTION <code>frame</code> <p>Input BGR frame. When <code>None</code>, uses <code>image_path</code> or captures from the camera.</p> <p> TYPE: <code>ndarray | None</code> DEFAULT: <code>None</code> </p> <code>image_path</code> <p>Path to an existing calibration image.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>save_calibration</code> <p>Write calibration JSON on success.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>calibration_path</code> <p>Output path for the calibration JSON.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True when calibration completed and was saved successfully.</p> Note <p>ROI phase: click-drag a bounding box per digit, then press Enter to confirm and advance.</p> <p>Segment phase: click to place polygon vertices for each of the seven segments. Keys: <code>d</code> reset defaults, <code>c</code> clear vertices, Backspace/Delete remove last vertex, Enter confirm segment, <code>q</code> or Esc cancel.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.capture_image","title":"capture_image","text":"<pre><code>capture_image(save=False, output_path='lcd_capture.jpg')\n</code></pre> <p>Capture one BGR frame from the configured camera.</p> PARAMETER DESCRIPTION <code>save</code> <p>Write the frame to <code>output_path</code> when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>output_path</code> <p>Destination path when <code>save</code> is True.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'lcd_capture.jpg'</code> </p> RETURNS DESCRIPTION <code>ndarray | None</code> <p>BGR <code>numpy</code> array, or <code>None</code> when camera mode is disabled or</p> <code>ndarray | None</code> <p>capture fails.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.preprocess_frame","title":"preprocess_frame","text":"<pre><code>preprocess_frame(frame, debug=False, debug_prefix='debug')\n</code></pre> <p>Preprocess a camera frame for segment detection.</p> <p>Pipeline: grayscale conversion, unsharp masking (sharpening; on by default via <code>sharpen_strength</code>), CLAHE contrast enhancement, Otsu thresholding (with <code>threshold_bias</code>), and morphological cleanup.</p> PARAMETER DESCRIPTION <code>frame</code> <p>Input BGR frame from the camera.</p> <p> TYPE: <code>ndarray</code> </p> <code>debug</code> <p>Save intermediate step images when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>debug_prefix</code> <p>Filename prefix for debug images.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'debug'</code> </p> RETURNS DESCRIPTION <code>ndarray</code> <p>Binary image with enhanced LCD segments.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.read_display","title":"read_display","text":"<pre><code>read_display(\n    frame=None,\n    debug=False,\n    debug_prefix=\"debug\",\n    image_save_path=None,\n)\n</code></pre> <p>Read all LCD digits through the full decode pipeline.</p> <p>Preprocessing applies grayscale conversion, sharpening (by default), CLAHE, thresholding, and morphological cleanup before segment lookup.</p> <p>When <code>frame</code> is supplied, decodes that single frame. Otherwise captures ten frames over one second and returns the strict majority consensus.</p> PARAMETER DESCRIPTION <code>frame</code> <p>Optional BGR frame. When provided, decodes once and returns.</p> <p> TYPE: <code>ndarray | None</code> DEFAULT: <code>None</code> </p> <code>debug</code> <p>Save debug preprocessing images when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>debug_prefix</code> <p>Filename prefix for debug images.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'debug'</code> </p> <code>image_save_path</code> <p>Optional path to save the consensus camera frame. Parent directories are created automatically. Ignored when <code>frame</code> is supplied.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>Digit string (for example <code>\"0425\"</code>), <code>\"OFF\"</code>, or <code>None</code> when</p> <code>str | None</code> <p>capture or consensus fails.</p> Example <p>Standalone read after loading calibration::</p> <pre><code>tester.load_assigned_calibration()\nresult = tester.read_display()\n</code></pre>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.analyze_segment","title":"analyze_segment","text":"<pre><code>analyze_segment(binary_frame, digit_idx, segment_name)\n</code></pre> <p>Classify one 7-segment polygon as ON or OFF.</p> PARAMETER DESCRIPTION <code>binary_frame</code> <p>Full preprocessed binary image from :meth:<code>preprocess_frame</code>.</p> <p> TYPE: <code>ndarray</code> </p> <code>digit_idx</code> <p>Zero-based digit index in the display.</p> <p> TYPE: <code>int</code> </p> <code>segment_name</code> <p>Segment key from :attr:<code>SEGMENT_ORDER</code>.</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>int</code> <p><code>1</code> when active pixels exceed :attr:<code>segment_threshold</code>; <code>0</code></p> <code>int</code> <p>otherwise.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.recognize_digit","title":"recognize_digit","text":"<pre><code>recognize_digit(binary_frame, digit_idx, debug=False)\n</code></pre> <p>Recognize one digit from its seven segment states.</p> PARAMETER DESCRIPTION <code>binary_frame</code> <p>Full preprocessed binary image from :meth:<code>preprocess_frame</code>.</p> <p> TYPE: <code>ndarray</code> </p> <code>digit_idx</code> <p>Zero-based digit index in the display.</p> <p> TYPE: <code>int</code> </p> <code>debug</code> <p>Print the segment pattern when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Recognized digit string, <code>\"OFF\"</code>, or <code>\"?\"</code> when unmatched.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.set_digit_rois","title":"set_digit_rois","text":"<pre><code>set_digit_rois(rois)\n</code></pre> <p>Set the bounding box for each LCD digit.</p> PARAMETER DESCRIPTION <code>rois</code> <p>List of <code>(x1, y1, x2, y2)</code> tuples, one per digit.</p> <p> TYPE: <code>list[tuple[int, int, int, int]]</code> </p> RAISES DESCRIPTION <code>ValueError</code> <p>If the ROI count does not match <code>num_digits</code>.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.turn_on","title":"turn_on","text":"<pre><code>turn_on()\n</code></pre> <p>Actuate the tester power button via the stored state machine.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if the validated turn-on action succeeded.</p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>state_machine</code> was not set on this instance.</p> <code>RuntimeError</code> <p>If the state machine rejects the action.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.turn_off","title":"turn_off","text":"<pre><code>turn_off()\n</code></pre> <p>Actuate the tester power button for shutdown via the stored state machine.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if the validated turn-off action succeeded.</p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>state_machine</code> was not set on this instance.</p> <code>RuntimeError</code> <p>If the state machine rejects the action.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.zero","title":"zero","text":"<pre><code>zero()\n</code></pre> <p>Actuate the tester zero button via the stored state machine.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if the validated zero action succeeded.</p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>state_machine</code> was not set on this instance.</p> <code>RuntimeError</code> <p>If the state machine rejects the action.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.is_display_on","title":"is_display_on","text":"<pre><code>is_display_on(debug=False, debug_prefix='display_on')\n</code></pre> <p>Classify whether the LCD display is powered and showing a value.</p> <p>Runs a consensus :meth:<code>read_display</code> capture before classifying.</p> PARAMETER DESCRIPTION <code>debug</code> <p>Save debug preprocessing images when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>debug_prefix</code> <p>Filename prefix for debug images.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'display_on'</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>False when all segments are off (<code>\"OFF\"</code>).</p> <code>bool</code> <p>True when a valid numeric reading is displayed.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When capture succeeds but the reading is indeterminate.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.is_display_zero","title":"is_display_zero","text":"<pre><code>is_display_zero(debug=False, debug_prefix='display_zero')\n</code></pre> <p>Check whether the LCD reads effectively zero (0000-0005).</p> <p>Runs a consensus :meth:<code>read_display</code> capture before classifying.</p> PARAMETER DESCRIPTION <code>debug</code> <p>Save debug preprocessing images when True.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>debug_prefix</code> <p>Filename prefix for debug images.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'display_zero'</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True when the consensus reading is a digit value from 0 to 5.</p> <code>bool</code> <p>False for <code>OFF</code>, <code>None</code>, or non-numeric readings.</p>"},{"location":"api/hardness-tester/#src.HardnessTester.HardnessTester.test_sample","title":"test_sample","text":"<pre><code>test_sample(\n    tray_index,\n    sample_id,\n    state_machine,\n    image_save_path=None,\n)\n</code></pre> <p>Execute one hardness sample test via the state machine.</p> <p>Moves to the sample tray and target sample, then runs the validated press-and-read action. Coordinate resolution and position validation are owned by the state machine.</p> PARAMETER DESCRIPTION <code>tray_index</code> <p>Sample tray identifier.</p> <p> TYPE: <code>str | int</code> </p> <code>sample_id</code> <p>Sample slot identifier within the tray.</p> <p> TYPE: <code>str | int</code> </p> <code>state_machine</code> <p><code>MotionPlatformStateMachine</code> that executes moves and the sample action.</p> <p> TYPE: <code>object</code> </p> <code>image_save_path</code> <p>Optional path to save the consensus camera frame.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>dict[str, float | str | bool | None]</code> <p>Dict with keys <code>result</code> (hardness float or None),</p> <code>dict[str, float | str | bool | None]</code> <p><code>sample_error</code> (error message or None), and <code>image_path</code></p> <code>dict[str, float | str | bool | None]</code> <p>(saved frame path or None).</p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>state_machine</code> is None.</p> <code>RuntimeError</code> <p>If any validated move or sample action fails.</p>"},{"location":"api/hardness-tester/#helper-function","title":"Helper Function","text":""},{"location":"api/hardness-tester/#src.HardnessTester.test_with_image","title":"test_with_image","text":"<pre><code>test_with_image(\n    image_path, calibration_file=_CLI_DEFAULT_CALIBRATION\n)\n</code></pre> <p>Read an LCD display from a static image file (no camera).</p> <p>Convenience helper for calibration verification and offline debugging.</p> PARAMETER DESCRIPTION <code>image_path</code> <p>Path to a BGR image of the LCD display.</p> <p> TYPE: <code>str</code> </p> <code>calibration_file</code> <p>Path to the segment calibration JSON file.</p> <p> TYPE: <code>str</code> DEFAULT: <code>_CLI_DEFAULT_CALIBRATION</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>Digit string from :meth:<code>HardnessTester.read_display</code>, or <code>None</code> when</p> <code>str | None</code> <p>the image or calibration could not be loaded.</p> Example <p>Verify calibration without hardware::</p> <pre><code>from src.HardnessTester import test_with_image\n\nprint(test_with_image(\"lcd_photo.jpg\"))\n</code></pre>"},{"location":"api/hardness-tester/#configuration-model","title":"Configuration Model","text":"<p><code>HardnessTester.from_system_config()</code> resolves tool and calibration settings from <code>system_config.json</code>.</p> <pre><code>{\n  \"hardness_testers\": {\n    \"shore_a\": {\n      \"use_camera\": true,\n      \"tool\": {\n        \"index\": 1,\n        \"name\": \"shore_a_hardness_tester\"\n      },\n      \"lcd_calibration_path\": \"jubilee_api_config/lcd_calibration_shore_a.json\",\n      \"button_servos\": {\n        \"servo\": \"S1\",\n        \"power_press_angle\": 90,\n        \"power_release_angle\": 0,\n        \"zero_press_angle\": 90,\n        \"zero_release_angle\": 0\n      },\n      \"cam_usb_path\": \"/dev/shore_a_cam\"\n    }\n  }\n}\n</code></pre>"},{"location":"api/hardness-tester/#calibration-file-format","title":"Calibration File Format","text":"<p>The calibration JSON produced by <code>calibrate()</code> and consumed by <code>load_calibration()</code>:</p> <pre><code>{\n  \"digit_rois\": [\n    [x1, y1, x2, y2],\n    [x1, y1, x2, y2],\n    [x1, y1, x2, y2],\n    [x1, y1, x2, y2]\n  ],\n  \"segment_points\": [\n    {\n      \"top\":          [[x, y], [x, y], [x, y], [x, y]],\n      \"top_left\":     [[x, y], [x, y], [x, y], [x, y]],\n      \"top_right\":    [[x, y], [x, y], [x, y], [x, y]],\n      \"middle\":       [[x, y], [x, y], [x, y], [x, y], [x, y], [x, y]],\n      \"bottom_left\":  [[x, y], [x, y], [x, y], [x, y]],\n      \"bottom_right\": [[x, y], [x, y], [x, y], [x, y]],\n      \"bottom\":       [[x, y], [x, y], [x, y], [x, y]]\n    }\n  ]\n}\n</code></pre> <p><code>digit_rois</code> holds the bounding box <code>[x1, y1, x2, y2]</code> for each digit in the camera frame (absolute pixel coordinates). <code>segment_points</code> holds the absolute pixel polygon vertices for each of the 7 segments per digit. These are populated by the calibration process and used by <code>analyze_segment()</code>.</p>"},{"location":"api/hardness-tester/#segment-lookup-table","title":"Segment Lookup Table","text":"<p>Segment order is <code>(top, top_left, top_right, middle, bottom_left, bottom_right, bottom)</code>.</p> Pattern Digit <code>(1, 1, 1, 0, 1, 1, 1)</code> <code>0</code> <code>(0, 0, 1, 0, 0, 1, 0)</code> <code>1</code> <code>(1, 0, 1, 1, 1, 0, 1)</code> <code>2</code> <code>(1, 0, 1, 1, 0, 1, 1)</code> <code>3</code> <code>(0, 1, 1, 1, 0, 1, 0)</code> <code>4</code> <code>(1, 1, 0, 1, 0, 1, 1)</code> <code>5</code> <code>(1, 1, 0, 1, 1, 1, 1)</code> <code>6</code> <code>(1, 0, 1, 0, 0, 1, 0)</code> <code>7</code> <code>(1, 1, 1, 1, 1, 1, 1)</code> <code>8</code> <code>(1, 1, 1, 1, 0, 1, 1)</code> <code>9</code> <code>(0, 0, 0, 0, 0, 0, 0)</code> <code>OFF</code>"},{"location":"api/hardness-tester/#advanced-debugging-example","title":"Advanced Debugging Example","text":"<p>After calibration, inspect segment detection per digit using the updated API:</p> <pre><code>tester = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n    use_camera=True,\n)\ntester.load_assigned_calibration()\n\nframe = tester.capture_image()\nbinary = tester.preprocess_frame(frame, debug=True, debug_prefix=\"debug\")\n\nfor i in range(tester.num_digits):\n    segments = {\n        name: tester.analyze_segment(binary, i, name)\n        for name in tester.SEGMENT_ORDER\n    }\n    digit = tester.recognize_digit(binary, i, debug=True)\n    print(f\"Digit {i}: {segments} =&gt; {digit}\")\n</code></pre> <p>Note that <code>analyze_segment</code> and <code>recognize_digit</code> both take the full binary frame and a <code>digit_idx</code> argument. They no longer accept a cropped digit ROI.</p>"},{"location":"api/hardness-tester/#design-notes","title":"Design Notes","text":"Why not OCR? <p>Traditional OCR (Tesseract, EasyOCR) struggles with 7-segment LCD displays because:</p> <ul> <li>Low contrast between segments and background</li> <li>Font variations across display manufacturers</li> <li>Hollow or partially lit segments</li> <li>Sensitivity to lighting changes</li> <li>Sensitivity to camera distortion</li> </ul> <p>Segment-based detection avoids these problems:</p> <ul> <li>Specifically designed for 7-segment geometry</li> <li>Reliable with low-contrast or dim displays</li> <li>No training data required</li> <li>Fast and lightweight</li> <li>Easy to debug: each segment decision is independently inspectable</li> </ul> Preprocessing pipeline <p>The preprocessing and sharpening pipeline is optimized for LCD displays:</p> <ol> <li>Grayscale conversion - converts the BGR camera frame to grayscale as the primary preprocessing path.</li> <li>Unsharp masking (sharpening) - applied by default after grayscale to recover blurred segment edges before CLAHE. Default <code>sharpen_strength</code> is 31; set to <code>0.0</code> to disable.</li> <li>CLAHE (Contrast Limited Adaptive Histogram Equalization) - enhances contrast in local 8x8 tiles rather than globally, handling shadows and glare that would fool global thresholding.</li> <li>Otsu thresholding - automatically finds the optimal binary threshold value for the local image content.</li> <li>Morphological cleaning - removes small noise pixels that survive thresholding.</li> </ol> Segment detection <p>Each segment is detected by:</p> <ol> <li>Applying the calibrated polygon mask for that segment to the binary image</li> <li>Counting dark (active) pixels inside the mask with <code>cv2.countNonZero</code></li> <li>Comparing the ratio against <code>segment_threshold</code> (default 0.5)</li> </ol> <p>Segment polygons are stored as absolute pixel coordinates in <code>segment_points</code>, populated from the calibration file. The polygon approach handles trapezoidal LCD segment shapes more accurately than rectangular ROI proportions.</p>"},{"location":"api/hardness-tester/#see-also","title":"See Also","text":"<ul> <li>JubileeManager - orchestrates Shore test workflows via <code>test_sample</code>, <code>hardness_turn_on</code>, <code>hardness_turn_off</code>, <code>hardness_zero</code></li> <li>MotionPlatformStateMachine - validates movements and executes hardware actions</li> <li>Reading LCD Displays - operator how-to guide</li> <li>Architecture Overview - system design context</li> </ul>"},{"location":"api/jubilee-manager/","title":"JubileeManager API Reference","text":"<p>The <code>JubileeManager</code> class is the primary interface for controlling the Jubilee powder dispensing system. It provides high-level methods for common operations and coordinates multiple hardware components.</p> <p>Recommended entry point</p> <p>Use <code>JubileeManager</code> for routine automation. Reach for MotionPlatformStateMachine directly only when you need unexposed operations.</p>"},{"location":"api/jubilee-manager/#overview","title":"Overview","text":"<p><code>JubileeManager</code> is designed to be the main entry point for:</p> <ul> <li>Connecting to and managing hardware</li> <li>Performing dispense operations</li> <li>Reading scale weights</li> <li>Coordinating complex multi-step movements</li> </ul> <p>All movements are validated through an internal <code>MotionPlatformStateMachine</code> which cannot be bypassed, ensuring safety and consistency.</p>"},{"location":"api/jubilee-manager/#class-reference","title":"Class Reference","text":"<p>Public methods delegate to the internal state machine. Longer workflow notes and examples live in the sections below; docstrings focus on signatures and safety callouts.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager","title":"JubileeManager","text":"<pre><code>JubileeManager(\n    num_piston_dispensers=0, num_pistons_per_dispenser=0\n)\n</code></pre> <p>High-level manager for Jubilee powder dispensing operations.</p> <p>Owns a :class:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine</code> that validates every movement. Application code should call methods on this class rather than issuing moves through :attr:<code>machine_read_only</code>.</p> ATTRIBUTE DESCRIPTION <code>scale</code> <p>Connected :class:<code>~src.Scale.Scale</code>, or <code>None</code> before connect.</p> <p> TYPE: <code>Scale | None</code> </p> <code>manipulator</code> <p>:class:<code>~src.Manipulator.Manipulator</code> instance, or <code>None</code>.</p> <p> TYPE: <code>Manipulator | None</code> </p> <code>state_machine</code> <p>Internal FSM, or <code>None</code> before connect.</p> <p> TYPE: <code>MotionPlatformStateMachine | None</code> </p> <code>connected</code> <p>Whether hardware initialization completed successfully.</p> <p> TYPE: <code>bool</code> </p> Example <p>Typical connect / operate / disconnect flow::</p> <pre><code>manager = JubileeManager(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10,\n)\ntry:\n    if manager.connect():\n        manager.dispense_to_well(\"0\", 50.0)\nfinally:\n    manager.disconnect()\n</code></pre> Note <p>Always call :meth:<code>disconnect</code> when finished. Check :attr:<code>connected</code> before operations.</p> Warning <p>Do not move axes through :attr:<code>machine_read_only</code>; that bypasses FSM validation and desynchronizes tracked state.</p> <p>Initialize the JubileeManager.</p> <p>Creates a manager with dispenser capacity metadata. Does not connect to hardware; call :meth:<code>connect</code> to establish connections.</p> PARAMETER DESCRIPTION <code>num_piston_dispensers</code> <p>Number of piston dispenser units to track.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> <code>num_pistons_per_dispenser</code> <p>Initial piston count per dispenser.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> Note <p>Feedrate and network addresses come from <code>api_config/system_config.json</code> at connect time.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager-attributes","title":"Attributes","text":""},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.machine_read_only","title":"machine_read_only  <code>property</code>","text":"<pre><code>machine_read_only\n</code></pre> <p>Read-only access to the underlying Jubilee <code>Machine</code>.</p> RETURNS DESCRIPTION <code>Machine | None</code> <p>The connected <code>Machine</code> instance, or <code>None</code> when disconnected.</p> Warning <p>Named <code>read_only</code> intentionally. Querying position or status is fine; issuing moves here bypasses FSM validation and can cause collisions or lost state tracking.</p> Example <p>Safe read-only query::</p> <pre><code>if manager.machine_read_only:\n    pos = manager.machine_read_only.get_position()\n</code></pre>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.deck","title":"deck  <code>property</code>","text":"<pre><code>deck\n</code></pre> <p>Access to the deck configuration and labware layout.</p> RETURNS DESCRIPTION <code>Deck | None</code> <p>The <code>Deck</code> instance when the state machine is initialized,</p> <code>Deck | None</code> <p>otherwise <code>None</code>.</p> Example <p>List slots that have labware loaded::</p> <pre><code>if manager.deck:\n    loaded = [\n        slot_key\n        for slot_key, slot in manager.deck.slots.items()\n        if slot.has_labware\n    ]\n</code></pre>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.piston_dispensers","title":"piston_dispensers  <code>property</code>","text":"<pre><code>piston_dispensers\n</code></pre> <p>Access to all configured piston dispensers.</p> RETURNS DESCRIPTION <code>list[PistonDispenser]</code> <p>List of :class:<code>~src.PistonDispenser.PistonDispenser</code> instances.</p> <code>list[PistonDispenser]</code> <p>Empty when the state machine is not initialized.</p> Example <p>Inspect remaining pistons::</p> <pre><code>for dispenser in manager.piston_dispensers:\n    print(dispenser.index, dispenser.num_pistons)\n</code></pre>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager-functions","title":"Methods:","text":""},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.connect","title":"connect","text":"<pre><code>connect(\n    machine_address=None,\n    scale_port=None,\n    state_machine_config=None,\n)\n</code></pre> <p>Connect to hardware and initialize the system.</p> <p>Connects the Duet controller and scale, loads <code>motion_platform_positions.json</code>, initializes deck and dispensers, homes axes, and registers tools. See the API page for the full sequence.</p> PARAMETER DESCRIPTION <code>machine_address</code> <p>Duet IP address. Defaults to <code>machine.duet_ip</code> from config when omitted.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>scale_port</code> <p>Serial device path for the scale. Defaults to <code>machine.scale_port</code> from config when omitted.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>state_machine_config</code> <p>Override path to <code>motion_platform_positions.json</code>. Relative paths resolve against the project root.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> when initialization succeeds; <code>False</code> on any failure.</p> <code>Check</code> <p>attr:<code>connected</code> after calling.</p> <p> TYPE: <code>bool</code> </p> RAISES DESCRIPTION <code>FileNotFoundError</code> <p>When <code>state_machine_config</code> does not exist.</p> <code>RuntimeError</code> <p>When homing fails.</p> <code>ConnectionError</code> <p>When machine or scale connection fails.</p> Note <p>Homing can take 30-60 seconds. Ensure axes are clear before calling.</p> Warning <p>A partial failure may leave hardware in an unknown state. Reset before retrying connect.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.disconnect","title":"disconnect","text":"<pre><code>disconnect()\n</code></pre> <p>Disconnect from hardware and release resources.</p> <p>Safe to call when not connected or after a partial connect failure. Parks the active tool and powers off a mounted hardness tester first.</p> Example <p>Always disconnect in <code>finally</code>::</p> <pre><code>manager = JubileeManager()\ntry:\n    manager.connect()\n    ...\nfinally:\n    manager.disconnect()\n</code></pre>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.get_weight_stable","title":"get_weight_stable","text":"<pre><code>get_weight_stable()\n</code></pre> <p>Return scale weight after the reading stabilizes.</p> RETURNS DESCRIPTION <code>float | None</code> <p>Weight in grams, or <code>None</code> when no scale is connected.</p> Note <p>Waits for stability (typically 1-3 seconds). Prefer :meth:<code>get_weight_unstable</code> only for live monitoring during fill.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.get_weight_unstable","title":"get_weight_unstable","text":"<pre><code>get_weight_unstable()\n</code></pre> <p>Return the current scale weight without waiting for stability.</p> RETURNS DESCRIPTION <code>float | None</code> <p>Instantaneous weight in grams, or <code>None</code> when no scale is connected.</p> Note <p>Suitable for live fill monitoring only. Record measurements with :meth:<code>get_weight_stable</code>.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.move_to_mold_slot","title":"move_to_mold_slot","text":"<pre><code>move_to_mold_slot(well_id)\n</code></pre> <p>Move to a mold slot ready position.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Deck well index as a string (for example <code>\"0\"</code>).</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> when the validated move succeeds.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When validation fails.</p> <code>KeyError</code> <p>When <code>well_id</code> is not in the deck layout.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.move_to_scale","title":"move_to_scale","text":"<pre><code>move_to_scale()\n</code></pre> <p>Move to the scale ready position.</p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success; <code>False</code> when no scale is configured.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When validation fails.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.move_to_dispenser","title":"move_to_dispenser","text":"<pre><code>move_to_dispenser()\n</code></pre> <p>Move to the next available piston dispenser ready position.</p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success; <code>False</code> when not connected.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When the state machine is missing or validation fails.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.fill_powder","title":"fill_powder","text":"<pre><code>fill_powder(target_weight)\n</code></pre> <p>Fill the mold on the scale to <code>target_weight</code> via the trickler.</p> PARAMETER DESCRIPTION <code>target_weight</code> <p>Target powder mass in grams.</p> <p> TYPE: <code>float</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success; <code>False</code> when no scale is configured.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When validation fails.</p> Warning <p>The mold must already be on the scale. Dispensing without a mold deposits powder directly on the load cell.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.get_piston_from_dispenser","title":"get_piston_from_dispenser","text":"<pre><code>get_piston_from_dispenser()\n</code></pre> <p>Retrieve a piston at the current dispenser ready position.</p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success; <code>False</code> when not connected.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>When the state machine is missing or validation fails.</p> Warning <p>Call :meth:<code>move_to_dispenser</code> first. The FSM rejects retrieval from the wrong position.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.dispense_to_well","title":"dispense_to_well","text":"<pre><code>dispense_to_well(well_id, target_weight)\n</code></pre> <p>Run the full dispense workflow for one mold slot.</p> <p>Picks up the mold, fills on the scale, retrieves a piston, tamps, and returns the mold to its slot. Each step is validated by the FSM.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Deck well index as a string (for example <code>\"0\"</code>).</p> <p> TYPE: <code>str</code> </p> <code>target_weight</code> <p>Target powder mass in grams.</p> <p> TYPE: <code>float</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> when every step succeeds; <code>False</code> when not connected or</p> <code>bool</code> <p>a handled error occurs.</p> RAISES DESCRIPTION <code>ToolStateError</code> <p>When manipulator or scale is unavailable.</p> <code>RuntimeError</code> <p>When the state machine is missing.</p> <code>ValueError</code> <p>When <code>well_id</code> is unknown or <code>target_weight</code> exceeds <code>safety.max_weight_per_well</code>.</p> Warning <p>A mid-workflow failure may leave the mold at an intermediate position. Manual recovery may be required.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.test_sample","title":"test_sample","text":"<pre><code>test_sample(\n    tray_index, sample_index, mode, image_save_path=None\n)\n</code></pre> <p>Run a hardness measurement on one tray sample.</p> <p>Selects the Shore-A or Shore-D tester from <code>mode</code>, ensures it is mounted, and delegates to :meth:<code>~src.HardnessTester.HardnessTester.test_sample</code>. Results are stored on <code>last_hardness_result</code>, <code>last_hardness_error</code>, and <code>last_hardness_image_path</code>.</p> PARAMETER DESCRIPTION <code>tray_index</code> <p>Zero-based hardness tray index.</p> <p> TYPE: <code>int</code> </p> <code>sample_index</code> <p>Sample position within the tray.</p> <p> TYPE: <code>int</code> </p> <code>mode</code> <p>Measurement pass: <code>shore_a</code> or <code>shore_d</code>.</p> <p> TYPE: <code>str</code> </p> <code>image_save_path</code> <p>Optional directory for debug capture images.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True when the workflow completed without exception, False when not</p> <code>bool</code> <p>connected or an error was caught.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.hardness_turn_on","title":"hardness_turn_on","text":"<pre><code>hardness_turn_on(mode=None)\n</code></pre> <p>Power on a Shore hardness tester via its servo button.</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>shore_a</code> or <code>shore_d</code>. Required (<code>None</code> raises :class:<code>ValueError</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True on success, False when not connected or an error was caught.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.hardness_turn_off","title":"hardness_turn_off","text":"<pre><code>hardness_turn_off(mode=None)\n</code></pre> <p>Power off a Shore hardness tester via its servo button.</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>shore_a</code> or <code>shore_d</code>. Required (<code>None</code> raises :class:<code>ValueError</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True on success, False when not connected or an error was caught.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.hardness_zero","title":"hardness_zero","text":"<pre><code>hardness_zero(mode=None)\n</code></pre> <p>Zero a Shore hardness tester via its servo button.</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>shore_a</code> or <code>shore_d</code>. Required (<code>None</code> raises :class:<code>ValueError</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True on success, False when not connected or an error was caught.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.move_to_global_ready","title":"move_to_global_ready","text":"<pre><code>move_to_global_ready()\n</code></pre> <p>Move to the global ready transit position.</p> RETURNS DESCRIPTION <code>bool</code> <p>True when the validated move succeeds.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If the state machine is missing or validation fails.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.set_dispenser_pistons","title":"set_dispenser_pistons","text":"<pre><code>set_dispenser_pistons(index, num_pistons)\n</code></pre> <p>Set the piston count for one dispenser while connected.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based dispenser index.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>Replacement count (must be &gt;= 0).</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success; <code>False</code> when the state machine is missing or</p> <code>bool</code> <p>the index is out of range.</p>"},{"location":"api/jubilee-manager/#src.JubileeManager.JubileeManager.abort","title":"abort","text":"<pre><code>abort()\n</code></pre> <p>Send firmware emergency stop (M112) to the Duet controller.</p> <p>Intentionally bypasses the state machine. After M112 the controller requires a manual reset before motion resumes, so tracked FSM state is no longer reliable.</p> Note <p>Best-effort only. Silently ignored when no machine connection exists.</p>"},{"location":"api/jubilee-manager/#usage-examples","title":"Usage Examples","text":"Connect and read weightDispense to wellHardware accessHardness testingError handling <pre><code>from src.JubileeManager import JubileeManager\n\nmanager = JubileeManager(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10,\n)\n\nif manager.connect(machine_address=\"192.168.1.100\"):\n    print(\"Connected successfully!\")\n\n    weight = manager.get_weight_stable()\n    print(f\"Current weight: {weight}g\")\n\n    manager.disconnect()\n</code></pre> <pre><code># After connecting...\nsuccess = manager.dispense_to_well(\n    well_id=\"0\",\n    target_weight=0.5,\n)\n\nif success:\n    print(\"Dispense completed successfully!\")\nelse:\n    print(\"Dispense failed - check logs for details\")\n</code></pre> <pre><code># Read-only access to machine (for queries, not movements)\nif manager.machine_read_only:\n    position = manager.machine_read_only.get_position()\n    print(f\"Current position: {position}\")\n\n# Access deck for labware information\nif manager.deck:\n    loaded_slots = [\n        slot_key\n        for slot_key, slot in manager.deck.slots.items()\n        if slot.has_labware\n    ]\n    print(f\"Slots with labware: {loaded_slots}\")\n\n# Access piston dispensers\nfor dispenser in manager.piston_dispensers:\n    print(f\"Dispenser {dispenser.index}: {dispenser.num_pistons} pistons\")\n</code></pre> <pre><code># After connecting with hardness testers configured in system_config.json...\n\nmanager.hardness_turn_on(mode=\"shore_a\")   # actuate power button via servo\nmanager.hardness_zero(mode=\"shore_a\")      # actuate zero button via servo\n\nsuccess = manager.test_sample(tray_index=0, sample_index=3, mode=\"shore_a\")\nif success:\n    print(f\"Hardness: {manager.last_hardness_result}\")   # e.g. 42.5\n    print(f\"Error:    {manager.last_hardness_error}\")    # None if clean read\n\nmanager.hardness_turn_off(mode=\"shore_a\")\n</code></pre> <p>To target Shore-D instead of Shore-A, pass <code>mode=\"shore_d\"</code> to any hardness method.</p> <pre><code>from src.JubileeManager import JubileeManager\n\nmanager = JubileeManager()\n\ntry:\n    if not manager.connect():\n        raise ConnectionError(\"Failed to connect to Jubilee\")\n\n    success = manager.dispense_to_well(\"0\", 0.5)\n    if not success:\n        print(\"Operation failed but system is still connected\")\n\nexcept Exception as e:\n    print(f\"Error occurred: {e}\")\n\nfinally:\n    manager.disconnect()\n</code></pre>"},{"location":"api/jubilee-manager/#internal-validation-and-execution-methods","title":"Internal Validation and Execution Methods","text":"<p>Internal API</p> <p>The symbols below document the state-machine path <code>JubileeManager</code> delegates to. Use <code>JubileeManager</code> instead in application code unless you are extending movement validation.</p> <p>JubileeManager delegates movement validation and execution to the state machine. The symbols below document the current internal execution path:</p>"},{"location":"api/jubilee-manager/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_mold_slot","title":"validated_move_to_mold_slot","text":"<pre><code>validated_move_to_mold_slot(well_id)\n</code></pre> <p>Validate and execute movement to a specific mold slot.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string <code>\"0\"</code> through <code>\"17\"</code>).</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code> with updated <code>context.position_id</code></p> <code>MoveValidationResult</code> <p>on success.</p> Note <p>Target resolves to <code>mold_ready_{well_id}</code> from deck labware or config. Requires a valid transition from the current position.</p>"},{"location":"api/jubilee-manager/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_scale","title":"validated_move_to_scale","text":"<pre><code>validated_move_to_scale()\n</code></pre> <p>Validate and execute movement to <code>scale_ready</code>.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires scale configured on the context, correct active tool, and z-height policy satisfied (typically <code>mold_transfer_safe</code>).</p>"},{"location":"api/jubilee-manager/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_dispenser","title":"validated_move_to_dispenser","text":"<pre><code>validated_move_to_dispenser()\n</code></pre> <p>Validate and execute movement to the next available dispenser ready position.</p> <p>Selects the first configured dispenser that still has pistons remaining and moves to its ready position. All dispenser tracking lives here, callers do not choose dispenser.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/jubilee-manager/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_fill_powder","title":"validated_fill_powder","text":"<pre><code>validated_fill_powder(target_weight)\n</code></pre> <p>Validate and execute trickler fill at <code>scale_active</code>.</p> PARAMETER DESCRIPTION <code>target_weight</code> <p>Target powder mass in grams.</p> <p> TYPE: <code>float</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. Final stable weight is available on</p> <code>MoveValidationResult</code> <p>attr:<code>last_fill_weight</code> after success.</p> Note <p>Requires tool engaged with mold on scale. Jam handling may pause the fill loop until an operator clears the blockage.</p>"},{"location":"api/jubilee-manager/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_retrieve_piston","title":"validated_retrieve_piston","text":"<pre><code>validated_retrieve_piston(manipulator_config)\n</code></pre> <p>Validate and execute piston retrieval at the current dispenser.</p> <p>Derives the dispenser index from <code>context.position_id</code> (for example <code>dispenser_ready_0</code> maps to dispenser 0). Call :meth:<code>validated_move_to_dispenser</code> first. On success, decrements the dispenser count and sets <code>has_top_piston</code> on the carried mold.</p> PARAMETER DESCRIPTION <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires manipulator tool active, <code>mold_without_top_piston</code> payload, and a <code>dispenser_ready_N</code> position with pistons remaining.</p>"},{"location":"api/jubilee-manager/#design-notes","title":"Design Notes","text":""},{"location":"api/jubilee-manager/#state-machine-ownership","title":"State Machine Ownership","text":"<p><code>JubileeManager</code> owns the <code>MotionPlatformStateMachine</code> instance. This design ensures:</p> <ul> <li>All movements must go through validation</li> <li>No external code can bypass safety checks</li> <li>Consistent state tracking across the system</li> </ul>"},{"location":"api/jubilee-manager/#read-only-machine-access","title":"Read-Only Machine Access","text":"<p>The <code>machine_read_only</code> property provides access to the underlying <code>Machine</code> object for read operations only.</p> <p>Do not move axes through machine_read_only</p> <p>While it is possible to issue movement commands using this property, doing so bypasses safety validation and corrupts <code>JubileeManager</code>'s internal state.</p> <p>Use <code>machine_read_only</code> only for:</p> <ul> <li>Querying current position</li> <li>Reading sensor values</li> <li>Checking machine state</li> </ul> <p>Never use it for:</p> <ul> <li>Moving axes</li> <li>Picking or parking tools</li> <li>Any operation that changes machine state</li> </ul>"},{"location":"api/jubilee-manager/#connection-sequence","title":"Connection Sequence","text":"<p>The <code>connect()</code> method performs several initialization steps:</p> <ol> <li>Connects to the Duet controller</li> <li>Connects to the scale</li> <li>Initializes the state machine with configuration from Position Configuration and ConfigLoader</li> <li>Initializes the deck and dispensers</li> <li>Homes all axes (X, Y, Z, U)</li> <li>Leaves tools parked for on-demand pickup during operations</li> </ol> <p>This ensures the system is in a known, safe state before operations begin.</p>"},{"location":"api/jubilee-manager/#see-also","title":"See Also","text":"<ul> <li>MotionPlatformStateMachine - advanced movement control and validation</li> <li>Position Configuration - named positions and transition rules</li> <li>ConfigLoader - <code>system_config.json</code> loading</li> <li>Manipulator - gripper tool details</li> <li>Scale - scale interface</li> <li>Quick Start Guide - getting started tutorial</li> </ul>"},{"location":"api/manipulator/","title":"Manipulator API Reference","text":"<p>The <code>Manipulator</code> class represents a gripper tool with a vertical axis (V-axis) used for picking and placing molds.</p>"},{"location":"api/manipulator/#overview","title":"Overview","text":"<p>The Manipulator is a custom toolhead that provides:</p> <ul> <li>Gripper: moves up/down to hold objects</li> <li>V-Axis: Vertical movement independent of machine Z-axis</li> <li>State Integration: Automatically updates state machine context</li> </ul> <p>State machine required</p> <p>Pass a connected <code>MotionPlatformStateMachine</code> to <code>__init__</code>. Methods that move or change payload raise <code>RuntimeError</code> if <code>state_machine</code> is missing.</p> <p>Prefer JubileeManager for workflows</p> <p>Construct a <code>Manipulator</code> when extending low-level tool behavior. For dispense and tamp sequences, use JubileeManager so movements and payload state stay synchronized.</p>"},{"location":"api/manipulator/#class-reference","title":"Class Reference","text":""},{"location":"api/manipulator/#src.Manipulator.Manipulator","title":"Manipulator","text":"<pre><code>Manipulator(\n    index, name, state_machine=None, config_source=None\n)\n</code></pre> <p>               Bases: <code>Tool</code></p> <p>Jubilee gripper toolhead for mold handling and tamping.</p> <p>Delegates validation and motion to :class:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine</code> and mirrors mold state on <code>state_machine.context</code>.</p> ATTRIBUTE DESCRIPTION <code>state_machine</code> <p>Motion platform FSM used for every pick/place/tamp call.</p> <p> </p> <code>tamper_axis</code> <p>V-axis letter loaded from <code>system_config.json</code>.</p> <p> TYPE: <code>str</code> </p> <code>current_well</code> <p>:class:<code>~src.trickler_labware.Mold</code> carried by the manipulator, or <code>None</code> when empty (property).</p> <p> TYPE: <code>Mold | None</code> </p> Warning <p>Direct :class:<code>~science_jubilee.Machine.Machine</code> moves bypass safety. Always move via :meth:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_mold_slot</code> (or manager helpers) before pick/place.</p> Note <p>Tamping and top-piston placement require a mold without a top piston.</p> <p>Initialize the manipulator tool.</p> PARAMETER DESCRIPTION <code>index</code> <p>Jubilee tool index (see <code>tools.manipulator</code> in config).</p> <p> TYPE: <code>int</code> </p> <code>name</code> <p>Jubilee tool name registered with the firmware.</p> <p> TYPE: <code>str</code> </p> <code>state_machine</code> <p>Connected :class:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine</code>. Required for all motion methods.</p> <p> TYPE: <code>MotionPlatformStateMachine | None</code> DEFAULT: <code>None</code> </p> <code>config_source</code> <p>Unused; retained for call-site compatibility.</p> <p> TYPE: <code>Any | None</code> DEFAULT: <code>None</code> </p> Note <p><code>tamper_axis</code> loads from <code>system_config.json</code> via :mod:<code>src.ConfigLoader</code> at construction time.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator-attributes","title":"Attributes","text":""},{"location":"api/manipulator/#src.Manipulator.Manipulator.state_machine","title":"state_machine  <code>instance-attribute</code>","text":"<pre><code>state_machine = state_machine\n</code></pre>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.tamper_axis","title":"tamper_axis  <code>instance-attribute</code>","text":"<pre><code>tamper_axis = _cfg.system.manipulator.tamper_axis\n</code></pre>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.current_well","title":"current_well  <code>property</code>","text":"<pre><code>current_well\n</code></pre> <p>Mold object currently carried by the manipulator.</p> RETURNS DESCRIPTION <code>Mold | None</code> <p><code>Mold</code> instance from the state machine context, or <code>None</code> when</p> <code>Mold | None</code> <p>the manipulator is empty or no state machine is configured.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator-functions","title":"Methods:","text":""},{"location":"api/manipulator/#src.Manipulator.Manipulator._get_config_dict","title":"_get_config_dict","text":"<pre><code>_get_config_dict()\n</code></pre> <p>Package manipulator settings for state machine validated calls.</p> RETURNS DESCRIPTION <code>dict[str, Any]</code> <p>Dict with <code>tamper_axis</code> only. Z policies use <code>context.z_height_id</code></p> <code>dict[str, Any]</code> <p>and motion <code>z_heights</code> from JSON.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.home_tamper","title":"home_tamper","text":"<pre><code>home_tamper(machine_connection=None)\n</code></pre> <p>Home the tamper (V) axis through the state machine.</p> <p>Safe while holding a mold without a top piston. Homing uses the mold cavity as a mechanical reference:</p> <ul> <li>Start: <code>v=2</code> (tamper inserted into mold)</li> <li>End: <code>v=-7</code> (tamper at mold bottom)</li> </ul> PARAMETER DESCRIPTION <code>machine_connection</code> <p>Deprecated; ignored.</p> <p> TYPE: <code>Any | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>state_machine</code> is missing or homing fails.</p> Warning <p>Do not home when the mold has a top piston. The travel path can damage the tamper or piston.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.tamp","title":"tamp","text":"<pre><code>tamp(tamp_depth, tamp_speed)\n</code></pre> <p>Compress powder in the held mold (tamping).</p> <p>Tamping reduces powder volume so the top piston fits and limits airborne powder during piston insertion. Typically run at <code>scale_ready</code> after filling. The V axis is re-homed automatically afterward.</p> PARAMETER DESCRIPTION <code>tamp_depth</code> <p>Target depth in mm.</p> <p> TYPE: <code>float</code> </p> <code>tamp_speed</code> <p>Feed rate in mm/min.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>state_machine</code> is not configured.</p> <code>ToolStateError</code> <p>If tamping is blocked or parameters are out of bounds.</p> Note <p>Bounds and defaults live in <code>system_config.json</code> under <code>manipulator</code>. Use :meth:<code>src.ConfigLoader.config.get_tamp_defaults</code> rather than hardcoding values.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.get_status","title":"get_status","text":"<pre><code>get_status()\n</code></pre> <p>Return manipulator status for telemetry and debugging.</p> RETURNS DESCRIPTION <code>dict[str, Any]</code> <p>Dict with <code>has_mold</code>, <code>tamper_axis</code>, and optional <code>current_well</code></p> <code>dict[str, Any]</code> <p>metadata (name, weights, <code>has_top_piston</code>, <code>valid</code>).</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.is_carrying_mold","title":"is_carrying_mold","text":"<pre><code>is_carrying_mold()\n</code></pre> <p>Return whether the manipulator is carrying a mold.</p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> when :attr:<code>current_well</code> is not <code>None</code>.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.pick_mold","title":"pick_mold","text":"<pre><code>pick_mold(well_id)\n</code></pre> <p>Pick up a mold from <code>mold_ready_{well_id}</code>.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string <code>\"0\"</code> through <code>\"17\"</code>).</p> <p> TYPE: <code>str</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>state_machine</code> is not configured.</p> <code>ToolStateError</code> <p>If pickup is not allowed in the current state.</p> Note <p>Call :meth:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_mold_slot</code> first. Requires empty payload and manipulator tool active.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.place_mold","title":"place_mold","text":"<pre><code>place_mold(well_id)\n</code></pre> <p>Place the carried mold into <code>mold_ready_{well_id}</code>.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (for example <code>\"0\"</code>, <code>\"1\"</code>).</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>The</code> <p>class:<code>~src.trickler_labware.Mold</code> that was placed, or <code>None</code></p> <p> TYPE: <code>Mold | None</code> </p> <code>Mold | None</code> <p>if nothing was carried.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>state_machine</code> is not configured.</p> <code>ToolStateError</code> <p>If placement is not allowed in the current state.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.place_top_piston","title":"place_top_piston","text":"<pre><code>place_top_piston(piston_dispenser)\n</code></pre> <p>Place a top piston on the mold currently held by the manipulator.</p> <p>Only allowed when carrying a mold without a top piston and when the state machine is at a dispenser ready position.</p> PARAMETER DESCRIPTION <code>piston_dispenser</code> <p>Dispenser supplying the piston.</p> <p> TYPE: <code>PistonDispenser</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True if successful.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If the state machine is not configured.</p> <code>ToolStateError</code> <p>If placement is not allowed in the current state.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.place_mold_on_scale","title":"place_mold_on_scale","text":"<pre><code>place_mold_on_scale()\n</code></pre> <p>Place the mold currently held by the manipulator onto the scale.</p> <p>Only allowed when carrying a mold without a top piston. Engages the tool at <code>scale_active</code> on success.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if successful.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If the state machine is not configured.</p> <code>ToolStateError</code> <p>If placement is not allowed in the current state.</p>"},{"location":"api/manipulator/#src.Manipulator.Manipulator.pick_mold_from_scale","title":"pick_mold_from_scale","text":"<pre><code>pick_mold_from_scale()\n</code></pre> <p>Pick up the mold resting on the scale.</p> <p>Requires the mold to be on the scale (<code>scale_active</code> / tool engaged). Disengages the tool and returns to <code>scale_ready</code> on success.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if successful.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If the state machine is not configured.</p> <code>ToolStateError</code> <p>If pickup is not allowed in the current state.</p>"},{"location":"api/manipulator/#exceptions","title":"Exceptions","text":""},{"location":"api/manipulator/#toolstateerror","title":"ToolStateError","text":"<pre><code>class ToolStateError(Exception)\n</code></pre> <p>Exception raised when a tool operation is attempted in an invalid state.</p> <p>This error is raised when trying to perform operations that require specific tool or payload states that are not currently met.</p> <p>Common Scenarios: - Attempting to pick a mold when already holding one - Trying to place a mold when not holding one - Operating at wrong position for the requested action</p> <p>Example: <pre><code>from src.Manipulator import Manipulator, ToolStateError\n\ntry:\n    manipulator.pick_mold(\"0\")\nexcept ToolStateError as e:\n    print(f\"Operation failed: {e}\")\n</code></pre></p>"},{"location":"api/manipulator/#usage-examples","title":"Usage Examples","text":""},{"location":"api/manipulator/#creating-a-manipulator","title":"Creating a Manipulator","text":"<pre><code>from src.Manipulator import Manipulator\nfrom src.MotionPlatformStateMachine import MotionPlatformStateMachine\n\n# Assume state_machine is already created\nmanipulator = Manipulator(\n    index=0,                      # Tool index on Jubilee\n    name=\"manipulator\",           # Tool name\n    state_machine=state_machine   # Reference to state machine\n)\n</code></pre>"},{"location":"api/manipulator/#picking-and-placing-molds","title":"Picking and Placing Molds","text":"<pre><code># Pick up a mold from a well\nmanipulator.pick_mold(well_id=\"0\")\n\n# Place mold on scale\nmanipulator.place_mold_on_scale()\n\n# Pick mold from scale\nmanipulator.pick_mold_from_scale()\n\n# Place mold back in well\nmanipulator.place_mold(well_id=\"0\")\n</code></pre>"},{"location":"api/manipulator/#v-axis-movement-and-homing","title":"V-Axis Movement and Homing","text":"<pre><code># Home V-axis (tamper axis) through the state machine\nmanipulator.home_tamper()\n</code></pre> <p>Direct V-axis positioning is not exposed on <code>Manipulator</code>; use validated moves to <code>scale_ready</code> or <code>mold_ready_N</code> positions and let pick/place/tamp sequences drive V.</p> <p>Homing with a Mold:</p> <p>The V-axis can be homed while holding a mold, as long as the mold does not have a top piston. During homing:</p> <ul> <li>Start position: <code>v=2</code> - Tamper is inserted into the mold</li> <li>End position: <code>v=-7</code> - Tamper touches the bottom of the mold</li> <li>This allows the system to establish accurate positioning reference using the mold itself</li> </ul> <pre><code>from src.ConfigLoader import config\n\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n\n# Example: Homing after picking up a mold without top piston\nmanipulator.pick_mold(well_id=\"0\")\nmanipulator.home_tamper()  # Valid - mold has no top piston\n\n# After tamping, V-axis is automatically re-homed\nmanipulator.tamp(tamp_depth=tamp_depth, tamp_speed=tamp_speed)\n</code></pre> <p>Important: Do not attempt to home the V-axis when:</p> <ul> <li>The mold already has a top piston inserted</li> <li>Not holding a mold (home before picking up a mold)</li> </ul> <p>Homing with a top piston risks collision</p> <p>V-axis homing uses the mold cavity as a mechanical reference (<code>v=2</code> to <code>v=-7</code>). A mold with a top piston blocks that travel and can damage the tamper or piston.</p>"},{"location":"api/manipulator/#pick-and-place-operations","title":"Pick and Place Operations","text":""},{"location":"api/manipulator/#pick-mold","title":"Pick Mold","text":"<p>The <code>pick_mold()</code> operation:</p> <ol> <li>Validates current position (must be at mold slot)</li> <li>Moves V-axis down to mold height</li> <li>Moves manipulator under mold</li> <li>Moves V-axis up with mold</li> <li>Moves back to mold ready position</li> <li>Updates payload state to <code>\"mold_without_top_piston\"</code></li> </ol> <pre><code>try:\n    manipulator.pick_mold(well_id=\"0\")\n    print(\"Mold picked successfully\")\nexcept ToolStateError as e:\n    print(f\"Pick failed: {e}\")\n</code></pre> <p>Requirements: - Must be at the correct mold slot position - Payload must be <code>\"empty\"</code> - V-axis must be homed</p>"},{"location":"api/manipulator/#place-mold","title":"Place Mold","text":"<p>The <code>place_mold()</code> operation:</p> <ol> <li>Validates current position</li> <li>Moves V-axis down</li> <li>Moves manipulator out from under mold</li> <li>Moves V-axis up</li> <li>Updates payload state to <code>\"empty\"</code></li> </ol> <pre><code>try:\n    manipulator.place_mold(well_id=\"0\")\n    print(\"Mold placed successfully\")\nexcept ToolStateError as e:\n    print(f\"Place failed: {e}\")\n</code></pre> <p>Requirements: - Must be at the correct mold slot position - Payload must be <code>\"mold_without_top_piston\"</code> or <code>\"mold_with_top_piston\"</code></p>"},{"location":"api/manipulator/#scale-operations","title":"Scale Operations","text":"<p>Special methods for scale interaction:</p> <pre><code># Place on scale\nmanipulator.place_mold_on_scale()\n\n# Pick from scale\nmanipulator.pick_mold_from_scale()\n</code></pre> <p>These are very similar to regular pick/place for scale interaction.</p>"},{"location":"api/manipulator/#tamping-operations","title":"Tamping Operations","text":""},{"location":"api/manipulator/#tamp-mold","title":"Tamp Mold","text":"<p>The <code>tamp()</code> method compresses powder in a mold held by the manipulator. This is typically done at the <code>scale_ready</code> position after filling a mold with powder and before inserting the top piston.</p> <p>Purpose of Tamping:</p> <ol> <li>Reduce powder volume - Allows the top piston to fit if the powder volume would otherwise prevent insertion</li> <li>Minimize airborne powder - Compressing the powder reduces the amount that becomes airborne when the top piston is inserted</li> </ol> <pre><code>from src.ConfigLoader import config\nfrom src.JubileeManager import JubileeManager\n\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n\n# After filling mold with powder (mold picked up from scale at scale_ready)\nmanipulator.pick_mold_from_scale()\n\n# Tamp the powder to compress it\nmanipulator.tamp(tamp_depth=tamp_depth, tamp_speed=tamp_speed)\n\n# Now safe to insert top piston\nmanager.move_to_dispenser()\nmanager.get_piston_from_dispenser()\n</code></pre> <p>Parameters:</p> <ul> <li><code>tamp_depth</code> (float): Target depth for tamping movement in mm (required)</li> <li><code>tamp_speed</code> (int): Speed for tamping movement in mm/min (required)</li> </ul> <p>Valid ranges and defaults are defined in <code>system_config.json</code> under <code>manipulator</code> (loaded at startup by <code>ConfigLoader</code>). Use <code>config.get_tamp_defaults()</code> rather than hardcoding values.</p> <p>Requirements:</p> <ul> <li>Must be carrying a mold (not empty)</li> <li>Mold must NOT have a top piston yet</li> <li>Typically performed at <code>scale_ready</code> position (but can be done at <code>mold_ready</code> positions too)</li> </ul> <p>How It Works:</p> <p>After tamping completes, the V axis is automatically re-homed to ensure axis accuracy. The homing process uses the mold itself as a reference:</p> <ul> <li>Homing starts at <code>v=2</code> (tamper inserted into mold)</li> <li>Homing ends at <code>v=-7</code> (tamper touching bottom of mold)</li> <li>This establishes an accurate position reference for subsequent operations</li> </ul> <p>Since the mold does not have a top piston during tamping, the V-axis homing is safe and provides precise positioning.</p> <p>Example with Error Handling:</p> <pre><code>from src.ConfigLoader import config\n\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n\ntry:\n    manipulator.pick_mold_from_scale()\n    manipulator.tamp(tamp_depth=tamp_depth, tamp_speed=tamp_speed)\n    print(\"Tamping successful\")\nexcept ToolStateError as e:\n    print(f\"Tamping failed: {e}\")\n</code></pre> <p>Common Errors:</p> <pre><code>from src.ConfigLoader import config\n\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n\n# Error: Trying to tamp when not holding a mold\nmanipulator.tamp(tamp_depth=tamp_depth, tamp_speed=tamp_speed)  # ToolStateError\n\n# Error: Trying to tamp after piston is already inserted\nstate_machine.validated_retrieve_piston(\n    manipulator_config=manipulator._get_config_dict()\n)  # sets has_top_piston on the carried mold\nmanipulator.tamp(tamp_depth=tamp_depth, tamp_speed=tamp_speed)  # ToolStateError\n\n# Error: Tamp depth or speed out of bounds (bounds from system_config.json)\nmanipulator.tamp(tamp_depth=999.0, tamp_speed=tamp_speed)   # ToolStateError\nmanipulator.tamp(tamp_depth=tamp_depth, tamp_speed=99999)   # ToolStateError\n</code></pre>"},{"location":"api/manipulator/#configuring-bounds","title":"Configuring Bounds","text":"ConfigPython <p>Tamping bounds and defaults live in <code>jubilee_api_config/system_config.json</code>:</p> <pre><code>{\n  \"manipulator\": {\n    \"tamper_axis\": \"V\",\n    \"tamp_depth_min\": 0.0,\n    \"tamp_depth_max\": 9.0,\n    \"tamp_speed_min\": 500,\n    \"tamp_speed_max\": 5000,\n    \"tamp_depth_default\": 9.0,\n    \"tamp_speed_default\": 500\n  }\n}\n</code></pre> <p>Values shown are from the checked-in config; adjust bounds in JSON and restart the process after changes.</p> <p>Read defaults and bounds through <code>ConfigLoader</code>:</p> <pre><code>from src.ConfigLoader import config\n\ntamp_depth, tamp_speed = config.get_tamp_defaults()\ndepth_min = config.get_tamp_depth_min()\ndepth_max = config.get_tamp_depth_max()\n</code></pre>"},{"location":"api/manipulator/#state-management","title":"State Management","text":""},{"location":"api/manipulator/#automatic-state-updates","title":"Automatic State Updates","text":"<p>The Manipulator automatically updates the state machine's payload state:</p> <pre><code># Initially empty\nprint(manipulator.state_machine.context.payload_state)  # \"empty\"\n\n# Pick mold\nmanipulator.pick_mold(\"0\")\nprint(manipulator.state_machine.context.payload_state)  # \"mold_without_top_piston\"\n\n# Place mold\nmanipulator.place_mold(\"0\")\nprint(manipulator.state_machine.context.payload_state)  # \"empty\"\n</code></pre>"},{"location":"api/manipulator/#payload-states","title":"Payload States","text":"State Description Set By <code>empty</code> Nothing held <code>place_mold()</code>, <code>place_mold_on_scale()</code> <code>mold_without_top_piston</code> Holding mold without top piston <code>pick_mold()</code>, <code>pick_mold_from_scale()</code> <code>mold_with_top_piston</code> Holding mold with top piston Piston retrieval workflow"},{"location":"api/manipulator/#configuration","title":"Configuration","text":"<p>The manipulator reads tool registration and tamping limits from <code>system_config.json</code> via <code>ConfigLoader</code> at construction time. Mold heights, gripper offsets, and named motion positions are defined in <code>motion_platform_positions.json</code>, not on the <code>Manipulator</code> class.</p> ConfigPython <p>Tool index and name (under <code>tools</code>):</p> <pre><code>{\n  \"tools\": {\n    \"manipulator\": {\n      \"index\": 0,\n      \"name\": \"manipulator\"\n    }\n  }\n}\n</code></pre> <p>Tamping axis and parameter bounds (top-level <code>manipulator</code>):</p> <pre><code>{\n  \"manipulator\": {\n    \"tamper_axis\": \"V\",\n    \"tamp_depth_min\": 0.0,\n    \"tamp_depth_max\": 9.0,\n    \"tamp_speed_min\": 500,\n    \"tamp_speed_max\": 5000,\n    \"tamp_depth_default\": 9.0,\n    \"tamp_speed_default\": 500\n  }\n}\n</code></pre> <pre><code>from src.ConfigLoader import config\n\ntool_index = config.system.tools.manipulator.index\ntamper_axis = config.system.manipulator.tamper_axis\ntamp_depth, tamp_speed = config.get_tamp_defaults()\n\n# Passed to state machine validated actions\nmanipulator_config = manipulator._get_config_dict()\n# {\"tamper_axis\": \"V\"}\n</code></pre> <p>Motion positions are separate</p> <p>Pick/place heights and XY coordinates come from Position Configuration. Edit <code>motion_platform_positions.json</code> for layout changes; edit <code>system_config.json</code> for tamp limits and tool index, and meta system configuration.</p>"},{"location":"api/manipulator/#error-handling","title":"Error Handling","text":""},{"location":"api/manipulator/#common-error-scenarios","title":"Common Error Scenarios","text":"<p>Picking when not empty: <pre><code># This will raise ToolStateError\nmanipulator.pick_mold(\"0\")\nmanipulator.pick_mold(\"1\")  # Error: already holding mold\n</code></pre></p> <p>Placing when empty: <pre><code># This will raise ToolStateError\nmanipulator.place_mold(\"0\")  # Error: not holding anything\n</code></pre></p> <p>Wrong position: <pre><code># Move to wrong position\nstate_machine.validated_move_to_scale()\n\n# This will raise ToolStateError\nmanipulator.pick_mold(\"0\")  # Error: not at mold slot\n</code></pre></p>"},{"location":"api/manipulator/#advanced-usage","title":"Advanced Usage","text":"State machine integration <p>The manipulator updates payload context on pick/place; the state machine validates whether the next move is allowed.</p> <pre><code>manipulator = Manipulator(\n    index=0,\n    name=\"manipulator\",\n    state_machine=state_machine,\n)\n\nmanipulator.pick_mold(\"0\")\n\nresult = state_machine.validated_move_to_scale()\nif not result.valid:\n    print(f\"Move failed: {result.reason}\")\n</code></pre>"},{"location":"api/manipulator/#best-practices","title":"Best Practices","text":""},{"location":"api/manipulator/#use-try-except-blocks","title":"Use Try-Except Blocks","text":"<p>Always handle <code>ToolStateError</code>:</p> <pre><code>try:\n    manipulator.pick_mold(\"0\")\n    manipulator.place_mold_on_scale()\nexcept ToolStateError as e:\n    print(f\"Operation failed: {e}\")\n</code></pre>"},{"location":"api/manipulator/#coordinate-with-state-machine","title":"Coordinate with State Machine","text":"<p>Validate moves before manipulator actions</p> <pre><code>result = state_machine.validated_move_to_mold_slot(\"0\")\nif result.valid:\n    manipulator.pick_mold(\"0\")\n</code></pre> <p>Direct machine moves bypass safety</p> <pre><code>machine.move_to(x=100, y=100, z=50)  # Not validated\nmanipulator.pick_mold(\"0\")             # May fail or collide\n</code></pre>"},{"location":"api/manipulator/#see-also","title":"See Also","text":"<ul> <li>JubileeManager - High-level interface using Manipulator</li> <li>MotionPlatformStateMachine - State tracking and validation</li> <li>PistonDispenser - Related component</li> <li>Quick Start Guide - Basic usage examples</li> </ul>"},{"location":"api/motion-platform/","title":"MotionPlatformStateMachine API Reference","text":"<p>The <code>MotionPlatformStateMachine</code> provides validated movement control for the Jubilee powder dispensing system. It ensures all operations are safe by tracking system state and enforcing constraints.</p>"},{"location":"api/motion-platform/#overview","title":"Overview","text":"<p>The state machine:</p> <ul> <li>Tracks current position, tool, and payload state</li> <li>Validates all requested movements</li> <li>Enforces safety constraints</li> <li>Provides detailed error messages when operations are invalid</li> </ul> <p>Advanced Usage</p> <p>Most users should interact with <code>JubileeManager</code> instead of using the state machine directly. Only use the state machine when you need operations not provided by <code>JubileeManager</code>.</p>"},{"location":"api/motion-platform/#state-machine-diagram","title":"State Machine Diagram","text":"<p>The state machine operates on two layers: a motion control FSM and an operational workflow defined by named positions and payload context.</p>"},{"location":"api/motion-platform/#motion-control-fsm","title":"Motion Control FSM","text":"<p>The inner FSM (powered by <code>python-statemachine</code>) manages three exclusive control states. Every validated method enters and exits <code>Moving</code> transiently; the <code>Tool Engaged</code> state is held while the mold rests on the scale.</p> <pre><code>graph LR\n    classDef fsm fill:#f1f5f9,stroke:#64748b,color:#1e293b\n\n    s(( )) --&gt; Idle\n\n    Idle([Idle])\n    Moving([Moving])\n    TE([Tool Engaged])\n\n    Idle --&gt;|begin_motion| Moving\n    Moving --&gt;|\"complete_motion / abort_motion\"| Idle\n    Moving --&gt;|complete_motion_with_tool| TE\n    Idle --&gt;|engage_tool| TE\n    TE --&gt;|disengage_tool| Idle\n\n    class Idle,Moving,TE fsm</code></pre>"},{"location":"api/motion-platform/#operational-workflow","title":"Operational Workflow","text":"<p>The diagram below shows the logical position states and the actions that connect them. The <code>Mold Slot</code> subgraph groups all N physical slots into a single logical region - the validation logic is identical for every slot. Payload state is encoded as distinct nodes so pick/place and piston retrieval become directed edges rather than self-loops. In-place operations that leave the position unchanged (<code>tamp</code>, <code>fill_powder</code>, <code>pickup_tool / park_tool</code>) are listed inside the node label. Scale Active is highlighted to indicate that the FSM's <code>Tool Engaged</code> control state is active while the mold is on the scale.</p> <pre><code>graph TD\n    classDef pos fill:#dbeafe,stroke:#3b82f6,color:#1e293b\n    classDef hot fill:#fef3c7,stroke:#f59e0b,color:#1e293b\n\n    s(( )) --&gt;|home_all| GR[\"Global Ready&lt;br&gt;pickup_tool / park_tool\"]\n\n    subgraph Slots[\"Mold Slot \u2014 same logic for all N slots\"]\n        direction LR\n        MS_E([\"empty\"])\n        MS_M([\"mold, no piston\"])\n        MS_E --&gt;|pick_mold| MS_M\n    end\n\n    GR --&gt;|move_to_mold_slot| MS_E\n    MS_M --&gt;|move_to_scale| SR[\"Scale Ready&lt;br&gt;tamp\"]\n\n    subgraph Ops[\"Dispensing Operations\"]\n        direction LR\n        SA[\"Scale Active&lt;br&gt;tool engaged \u00b7 fill_powder\"]\n        D[\"Dispenser&lt;br&gt;any of M\"]\n        MS_P([\"mold + piston\"])\n        D --&gt;|retrieve_piston| MS_P\n    end\n\n    SR --&gt;|place_mold_on_scale| SA\n    SR --&gt;|move_to_dispenser| D\n    SA --&gt;|pick_mold_from_scale| SR\n    MS_P --&gt;|place_mold| MS_E\n    SR --&gt;|move_to_mold_slot| MS_M\n\n    class GR,MS_E,MS_M,SR,D,MS_P pos\n    class SA hot</code></pre>"},{"location":"api/motion-platform/#class-reference","title":"Class Reference","text":""},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine","title":"MotionPlatformStateMachine","text":"<pre><code>MotionPlatformStateMachine(\n    registry, machine, *, context=None, scale=None\n)\n</code></pre> <p>               Bases: <code>StateMachine</code></p> <p>Validates and sequences Jubilee motion platform moves.</p> <p>Uses <code>python-statemachine</code> for the control FSM (<code>idle</code>, <code>moving</code>, <code>tool_engaged</code>) and :class:<code>PositionRegistry</code> for transition rules from <code>motion_platform_positions.json</code>. Public callers should use :meth:<code>validated_*</code> methods, which delegate hardware motion to an internal :class:<code>~src.MovementExecutor.MovementExecutor</code>.</p> ATTRIBUTE DESCRIPTION <code>context</code> <p>Mutable :class:<code>MotionContext</code> (position, tools, payload, deck).</p> <p> </p> <code>idle</code> <p>Initial FSM state when no motion is in progress.</p> <p> </p> <code>moving</code> <p>Transient FSM state while a move executes.</p> <p> </p> <code>tool_engaged</code> <p>FSM state while a mold rests on the scale with tool engaged.</p> <p> </p> Warning <p>Not thread-safe. Do not call methods from multiple threads without external locking.</p> Note <p>Validated methods return :class:<code>MoveValidationResult</code>; always inspect <code>result.valid</code> and surface <code>result.reason</code> before retrying.</p> See Also <p>:class:<code>~src.JubileeManager.JubileeManager</code> for the recommended high-level entry point.</p> <p>Construct a state machine bound to a position registry and machine.</p> <p>Prefer :meth:<code>from_config_file</code> for production setup.</p> PARAMETER DESCRIPTION <code>registry</code> <p>Loaded :class:<code>PositionRegistry</code> with positions and actions.</p> <p> TYPE: <code>PositionRegistry</code> </p> <code>machine</code> <p>Connected Jubilee :class:<code>~science_jubilee.Machine.Machine</code>.</p> <p> TYPE: <code>Machine</code> </p> <code>context</code> <p>Optional initial :class:<code>MotionContext</code>. Defaults to the first <code>GLOBAL_READY</code> position.</p> <p> TYPE: <code>MotionContext | None</code> DEFAULT: <code>None</code> </p> <code>scale</code> <p>Optional :class:<code>~src.Scale.Scale</code> stored on the context.</p> <p> TYPE: <code>Scale | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>ValueError</code> <p>If no <code>GLOBAL_READY</code> position exists when <code>context</code> is omitted, or if a provided context references an unknown position.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine-attributes","title":"Attributes","text":""},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.idle","title":"idle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>idle = State('Idle', initial=True)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.moving","title":"moving  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>moving = State('Moving')\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.tool_engaged","title":"tool_engaged  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tool_engaged = State('Tool Engaged')\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.begin_motion","title":"begin_motion  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>begin_motion = idle.to(moving)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.complete_motion","title":"complete_motion  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>complete_motion = moving.to(idle)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.complete_motion_with_tool","title":"complete_motion_with_tool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>complete_motion_with_tool = moving.to(tool_engaged)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.engage_tool","title":"engage_tool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>engage_tool = idle.to(tool_engaged)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.disengage_tool","title":"disengage_tool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>disengage_tool = tool_engaged.to(idle)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.abort_motion","title":"abort_motion  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>abort_motion = moving.to(idle)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._registry","title":"_registry  <code>instance-attribute</code>","text":"<pre><code>_registry = registry\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._actions","title":"_actions  <code>instance-attribute</code>","text":"<pre><code>_actions = registry.actions\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.context","title":"context  <code>instance-attribute</code>","text":"<pre><code>context = context\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._executor","title":"_executor  <code>instance-attribute</code>","text":"<pre><code>_executor = MovementExecutor(machine, scale=scale)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.machine","title":"machine  <code>property</code>","text":"<pre><code>machine\n</code></pre> <p>Read-only Jubilee machine access for position and status queries.</p> Warning <p>Do not issue moves through this property. Use :meth:<code>validated_*</code> methods so transition rules and coordinate checks stay enforced.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.last_fill_weight","title":"last_fill_weight  <code>property</code>","text":"<pre><code>last_fill_weight\n</code></pre> <p>The final stable weight reading from the most recent successful powder fill.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine-functions","title":"Methods:","text":""},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._axis_or_config","title":"_axis_or_config  <code>staticmethod</code>","text":"<pre><code>_axis_or_config(axis)\n</code></pre>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.from_config_file","title":"from_config_file  <code>classmethod</code>","text":"<pre><code>from_config_file(\n    path,\n    machine,\n    *,\n    context_overrides=None,\n    scale=None,\n    system_config_path=None\n)\n</code></pre> <p>Create a state machine from <code>motion_platform_positions.json</code>.</p> PARAMETER DESCRIPTION <code>path</code> <p>Path to <code>motion_platform_positions.json</code>.</p> <p> TYPE: <code>str | Path</code> </p> <code>machine</code> <p>Connected Jubilee :class:<code>~science_jubilee.Machine.Machine</code>.</p> <p> TYPE: <code>Machine</code> </p> <code>context_overrides</code> <p>Optional fields merged into the initial :class:<code>MotionContext</code> (for example <code>payload_state</code>).</p> <p> TYPE: <code>Mapping[str, object] | None</code> DEFAULT: <code>None</code> </p> <code>scale</code> <p>Optional :class:<code>~src.Scale.Scale</code> stored on the context.</p> <p> TYPE: <code>Scale | None</code> DEFAULT: <code>None</code> </p> <code>system_config_path</code> <p>Deprecated; passed to :meth:<code>PositionRegistry.from_config_file</code> for compatibility.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>'MotionPlatformStateMachine'</code> <p>Configured state machine with <code>context.position_id</code> at</p> <code>'MotionPlatformStateMachine'</code> <p><code>GLOBAL_READY</code>.</p> RAISES DESCRIPTION <code>ValueError</code> <p>If the configuration lacks a <code>GLOBAL_READY</code> position.</p> Example <p>Wire up deck and dispensers after construction::</p> <pre><code>sm = MotionPlatformStateMachine.from_config_file(\n    \"api_config/motion_platform_positions.json\",\n    machine=machine,\n    scale=scale,\n)\nsm.initialize_deck()\nsm.initialize_dispensers(num_piston_dispensers=2, num_pistons_per_dispenser=10)\n</code></pre> Note <p>Position ids, transitions, and z-height policies come from JSON only. See :doc:<code>position configuration &lt;/api/position-config&gt;</code>.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.initialize_deck","title":"initialize_deck","text":"<pre><code>initialize_deck(\n    deck_name=\"weight_well_deck\", config_path=None\n)\n</code></pre> <p>Initialize the deck with mold labware in each slot.</p> <p>Loads <code>Mold</code> objects into slots 0-17 and links each well to a <code>mold_ready_N</code> position id from config.</p> PARAMETER DESCRIPTION <code>deck_name</code> <p>Deck configuration name (default <code>weight_well_deck</code>).</p> <p> TYPE: <code>str</code> DEFAULT: <code>'weight_well_deck'</code> </p> <code>config_path</code> <p>Optional path to deck labware JSON files.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Note <p>Pick/place coordinates still come from <code>motion_platform_positions.json</code>, not deck labware offsets.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.initialize_dispensers","title":"initialize_dispensers","text":"<pre><code>initialize_dispensers(\n    num_piston_dispensers=0, num_pistons_per_dispenser=0\n)\n</code></pre> <p>Initialize piston dispenser inventory tracking.</p> PARAMETER DESCRIPTION <code>num_piston_dispensers</code> <p>Number of side-mounted piston dispensers.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> <code>num_pistons_per_dispenser</code> <p>Initial piston count per dispenser.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.set_dispenser_pistons","title":"set_dispenser_pistons","text":"<pre><code>set_dispenser_pistons(index, num_pistons)\n</code></pre> <p>Set the piston count for a specific dispenser.</p> <p>Allows the piston count to be updated (e.g. after reloading a tray) without disconnecting from the machine.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based dispenser index.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>New piston count (must be &gt;= 0).</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True on success, False if the index is out of range.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.get_mold_from_deck","title":"get_mold_from_deck","text":"<pre><code>get_mold_from_deck(well_id)\n</code></pre> <p>Get a mold object from the deck by mold slot ID.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string \"0\" through \"17\")</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>object | None</code> <p>Mold object if found, None otherwise</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.reset_mold_metadata","title":"reset_mold_metadata","text":"<pre><code>reset_mold_metadata()\n</code></pre> <p>Reset transient mold state captured during a dispensing job.</p> <p>Clears mold-level metadata so a new job starts from a clean baseline.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_pick_mold","title":"validated_pick_mold","text":"<pre><code>validated_pick_mold(well_id, manipulator_config)\n</code></pre> <p>Validate and execute picking up a mold from a mold slot.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string <code>\"0\"</code> through <code>\"17\"</code>).</p> <p> TYPE: <code>str</code> </p> <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>). Use :meth:<code>~src.Manipulator.Manipulator._get_config_dict</code>.</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. On success, sets <code>current_well</code> and</p> <code>MoveValidationResult</code> <p><code>payload_state</code> to <code>mold_without_top_piston</code>.</p> Note <p>Requires the machine to already be at <code>mold_ready_{well_id}</code> with empty payload and manipulator tool active.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_place_mold","title":"validated_place_mold","text":"<pre><code>validated_place_mold(well_id, manipulator_config=None)\n</code></pre> <p>Validate and execute placing a mold in a mold slot.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string <code>\"0\"</code> through <code>\"17\"</code>).</p> <p> TYPE: <code>str</code> </p> <code>manipulator_config</code> <p>Optional manipulator settings dict (unused for placement; retained for call-site compatibility).</p> <p> TYPE: <code>dict[str, object] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. On success, clears <code>current_well</code> and</p> <code>MoveValidationResult</code> <p>sets <code>payload_state</code> to <code>empty</code>.</p> Note <p>Requires carrying a mold at <code>mold_ready_{well_id}</code>.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_place_mold_on_scale","title":"validated_place_mold_on_scale","text":"<pre><code>validated_place_mold_on_scale(manipulator_config)\n</code></pre> <p>Validate and execute placing the carried mold on the scale.</p> <p>On success, transitions to <code>scale_active</code> and engages the tool.</p> PARAMETER DESCRIPTION <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires <code>scale_ready</code>, mold without top piston, and manipulator tool active. Engages the tool at <code>scale_active</code> after placement.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_pick_mold_from_scale","title":"validated_pick_mold_from_scale","text":"<pre><code>validated_pick_mold_from_scale(manipulator_config)\n</code></pre> <p>Validate and execute picking the mold up from the scale.</p> <p>On success, disengages the tool and returns to <code>scale_ready</code>.</p> PARAMETER DESCRIPTION <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires <code>scale_active</code> with tool engaged and <code>mold_on_scale</code> set on the context.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_place_top_piston","title":"validated_place_top_piston","text":"<pre><code>validated_place_top_piston(\n    piston_dispenser, manipulator_config\n)\n</code></pre> <p>Validate and execute placing a top piston on the carried mold.</p> PARAMETER DESCRIPTION <code>piston_dispenser</code> <p>:class:<code>~src.PistonDispenser.PistonDispenser</code> at the current <code>dispenser_ready_N</code> position.</p> <p> TYPE: <code>PistonDispenser</code> </p> <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. On success, sets <code>has_top_piston</code></p> <code>MoveValidationResult</code> <p>on the carried mold.</p> Warning <p>The underlying executor currently uses absolute coordinates and is validated for a single dispenser layout. Confirm machine config before running on multi-dispenser setups.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_tamp","title":"validated_tamp","text":"<pre><code>validated_tamp(manipulator_config, tamp_depth, tamp_speed)\n</code></pre> <p>Validate and execute tamping to compress powder in the held mold.</p> <p>Typically performed at <code>scale_ready</code> after filling and before piston insertion. Bounds are enforced from <code>system_config.json</code> via :mod:<code>src.ConfigLoader</code>.</p> PARAMETER DESCRIPTION <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> <code>tamp_depth</code> <p>Target tamp depth in mm.</p> <p> TYPE: <code>float</code> </p> <code>tamp_speed</code> <p>Tamper feed rate in mm/min.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires a mold without top piston. The V axis is re-homed after tamping for positioning accuracy.</p> See Also <p>:meth:<code>~src.Manipulator.Manipulator.tamp</code> raises :class:<code>~src.Manipulator.ToolStateError</code> when validation fails.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._resolve_ready_position_coords","title":"_resolve_ready_position_coords","text":"<pre><code>_resolve_ready_position_coords(\n    position_id, *, require_v=True\n)\n</code></pre> <p>Validate that a named position exists, has all required coordinates defined, and resolve the Z axis (handling USE_Z_HEIGHT_POLICY via context).</p> PARAMETER DESCRIPTION <code>position_id</code> <p>Identifier of the position to resolve.</p> <p> TYPE: <code>str</code> </p> <code>require_v</code> <p>When True, the V axis must be defined for the position.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Returns a _ResolvedPositionResult with either <ul> <li>error set to a failed MoveValidationResult, or</li> <li>coords set to (x, y, z, v) ready for use. V can be None when not required.</li> </ul>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._validate_and_execute","title":"_validate_and_execute","text":"<pre><code>_validate_and_execute(\n    target_position_id=None,\n    action_id=None,\n    additional_requirements=None,\n    execution_func=None,\n    **execution_kwargs\n)\n</code></pre> <p>Generic validation and execution for movements and tool actions.</p> <p>This method performs comprehensive validation for either: - Position movements (when target_position_id is provided) - Tool actions (when action_id is provided)</p> <p>Validation steps for MOVEMENTS: 1. Checks state machine is not already moving 2. Validates position transition is allowed (current \u2192 target) 3. Validates machine is actually at expected current position 4. Validates z-height policy for target position 5. Validates all requirements for target position 6. If valid, executes the provided function and transitions position</p> <p>Validation steps for ACTIONS: 1. Checks state machine is not already moving 2. Validates action exists in registry 3. Validates tool engagement state (if required/blocked) 4. Validates required tool ID matches 5. Validates position scope (action allowed at current position) 6. Validates action requirements and excludes 7. If valid, executes the provided function (no position change)</p> PARAMETER DESCRIPTION <code>target_position_id</code> <p>The target position identifier (for movements)</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>action_id</code> <p>The action identifier (for tool actions)</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>additional_requirements</code> <p>Extra requirements beyond position/action requirements</p> <p> TYPE: <code>dict[str, object] | None</code> DEFAULT: <code>None</code> </p> <code>execution_func</code> <p>Function to execute if validation passes</p> <p> TYPE: <code>Callable[..., object] | None</code> DEFAULT: <code>None</code> </p> <code>**execution_kwargs</code> <p>Arguments to pass to execution function</p> <p> TYPE: <code>object</code> DEFAULT: <code>{}</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with validation outcome</p> RAISES DESCRIPTION <code>ValueError</code> <p>If both or neither target_position_id and action_id are provided</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._validate_and_execute_move","title":"_validate_and_execute_move","text":"<pre><code>_validate_and_execute_move(\n    target_position_id,\n    additional_requirements=None,\n    execution_func=None,\n    **execution_kwargs\n)\n</code></pre> <p>Internal method to validate and execute position movements.</p> <p>See _validate_and_execute() for full documentation.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._validate_and_execute_action","title":"_validate_and_execute_action","text":"<pre><code>_validate_and_execute_action(\n    action_id,\n    additional_requirements=None,\n    execution_func=None,\n    **execution_kwargs\n)\n</code></pre> <p>Internal method to validate and execute tool actions.</p> <p>All actions must be defined in the registry (config JSON).</p> <p>See _validate_and_execute_move() for full documentation.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_mold_slot","title":"validated_move_to_mold_slot","text":"<pre><code>validated_move_to_mold_slot(well_id)\n</code></pre> <p>Validate and execute movement to a specific mold slot.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Mold slot identifier (numerical string <code>\"0\"</code> through <code>\"17\"</code>).</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code> with updated <code>context.position_id</code></p> <code>MoveValidationResult</code> <p>on success.</p> Note <p>Target resolves to <code>mold_ready_{well_id}</code> from deck labware or config. Requires a valid transition from the current position.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_scale","title":"validated_move_to_scale","text":"<pre><code>validated_move_to_scale()\n</code></pre> <p>Validate and execute movement to <code>scale_ready</code>.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires scale configured on the context, correct active tool, and z-height policy satisfied (typically <code>mold_transfer_safe</code>).</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_dispenser","title":"validated_move_to_dispenser","text":"<pre><code>validated_move_to_dispenser()\n</code></pre> <p>Validate and execute movement to the next available dispenser ready position.</p> <p>Selects the first configured dispenser that still has pistons remaining and moves to its ready position. All dispenser tracking lives here, callers do not choose dispenser.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_sample_tray","title":"validated_move_to_sample_tray","text":"<pre><code>validated_move_to_sample_tray(tray_index)\n</code></pre> <p>Validate and execute movement to the sample tray ready position.</p> <p>Per-sample slot moves use :meth:<code>validated_move_to_hardness_sample</code>.</p> PARAMETER DESCRIPTION <code>tray_index</code> <p>Zero-based tray index configured in <code>motion_platform_positions.json</code>.</p> <p> TYPE: <code>str | int</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_hardness_sample","title":"validated_move_to_hardness_sample","text":"<pre><code>validated_move_to_hardness_sample(\n    tray_index, sample_id, mode=None\n)\n</code></pre> <p>Validate and execute movement to a specific hardness sample slot.</p> <p>Resolves the per-slot position from the registry using the <code>sample_tray_{tray_index}_slot_{sample_id}</code> identifier. The state machine owns all coordinate validation; the executor receives only the resolved (x, y, z) floats.</p> PARAMETER DESCRIPTION <code>tray_index</code> <p>Zero-based tray index.</p> <p> TYPE: <code>str | int</code> </p> <code>sample_id</code> <p>Sample index within the tray.</p> <p> TYPE: <code>str | int</code> </p> <code>mode</code> <p>Optional Shore mode (<code>\"shore_a\"</code> or <code>\"shore_d\"</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_test_sample","title":"validated_test_sample","text":"<pre><code>validated_test_sample(\n    tray_index,\n    sample_id,\n    mode=None,\n    hardness_tester=None,\n    image_save_path=None,\n)\n</code></pre> <p>Validate and execute the hardness measurement at the current sample slot.</p> <p>The machine must already be positioned at the target slot (via <code>validated_move_to_hardness_sample</code>) before this action is called.</p> PARAMETER DESCRIPTION <code>tray_index</code> <p>Zero-based tray index.</p> <p> TYPE: <code>str | int</code> </p> <code>sample_id</code> <p>Sample index within the tray.</p> <p> TYPE: <code>str | int</code> </p> <code>mode</code> <p>Optional Shore mode (<code>\"shore_a\"</code> or <code>\"shore_d\"</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>hardness_tester</code> <p><code>HardnessTester</code> used for LCD capture.</p> <p> TYPE: <code>HardnessTester | None</code> DEFAULT: <code>None</code> </p> <code>image_save_path</code> <p>Optional path for a debug camera frame.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome. OCR results are stored on the</p> <code>MoveValidationResult</code> <p>executor as <code>last_hardness_result</code> and <code>last_hardness_error</code>.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._extract_servo_angles","title":"_extract_servo_angles  <code>staticmethod</code>","text":"<pre><code>_extract_servo_angles(hardness_tester, button)\n</code></pre> <p>Extract and validate servo channel and angles from a HardnessTester.</p> PARAMETER DESCRIPTION <code>hardness_tester</code> <p>HardnessTester instance (duck-typed).</p> <p> TYPE: <code>object | None</code> </p> <code>button</code> <p>\"power\" or \"zero\".</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>(servo_channel, press_angle, release_angle, error_message).</p> <code>int | None</code> <p>error_message is None on success; a descriptive string on failure.</p> <code>int | None</code> <p>Both angles are validated to be in [0, 180].</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_hardness_turn_on","title":"validated_hardness_turn_on","text":"<pre><code>validated_hardness_turn_on(mode=None, hardness_tester=None)\n</code></pre> <p>Validate and execute hardness tester power-on button actuation.</p> <p>Extracts the servo channel and press/release angles from hardness_tester, validates that both angles are in the range [0, 180], then delegates to the executor. This action is intentionally allowed at any position.</p> PARAMETER DESCRIPTION <code>mode</code> <p>Optional Shore mode string (for logging).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>hardness_tester</code> <p><code>HardnessTester</code> with servo configuration.</p> <p> TYPE: <code>HardnessTester | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_hardness_turn_off","title":"validated_hardness_turn_off","text":"<pre><code>validated_hardness_turn_off(\n    mode=None, hardness_tester=None\n)\n</code></pre> <p>Validate and execute hardness tester power-off button actuation.</p> <p>Extracts the servo channel and press/release angles from hardness_tester, validates that both angles are in the range [0, 180], then delegates to the executor. This action is intentionally allowed at any position.</p> PARAMETER DESCRIPTION <code>mode</code> <p>Optional Shore mode string (for logging).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>hardness_tester</code> <p><code>HardnessTester</code> with servo configuration.</p> <p> TYPE: <code>HardnessTester | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_hardness_zero","title":"validated_hardness_zero","text":"<pre><code>validated_hardness_zero(mode=None, hardness_tester=None)\n</code></pre> <p>Validate and execute hardness tester zero button actuation.</p> <p>Extracts the servo channel and press/release angles from hardness_tester, validates that both angles are in the range [0, 180], then delegates to the executor. This action is intentionally allowed at any position.</p> PARAMETER DESCRIPTION <code>mode</code> <p>Optional Shore mode string (for logging).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>hardness_tester</code> <p><code>HardnessTester</code> with servo configuration.</p> <p> TYPE: <code>HardnessTester | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_fill_powder","title":"validated_fill_powder","text":"<pre><code>validated_fill_powder(target_weight)\n</code></pre> <p>Validate and execute trickler fill at <code>scale_active</code>.</p> PARAMETER DESCRIPTION <code>target_weight</code> <p>Target powder mass in grams.</p> <p> TYPE: <code>float</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. Final stable weight is available on</p> <code>MoveValidationResult</code> <p>attr:<code>last_fill_weight</code> after success.</p> Note <p>Requires tool engaged with mold on scale. Jam handling may pause the fill loop until an operator clears the blockage.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_open_powder_dispenser_cover","title":"validated_open_powder_dispenser_cover","text":"<pre><code>validated_open_powder_dispenser_cover()\n</code></pre> <p>Validate and execute opening the powder dispenser cover.</p> <p>Requires the manipulator tool to be active, a mold to be placed on the scale, and the tool to be engaged at the scale_active position.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_close_powder_dispenser_cover","title":"validated_close_powder_dispenser_cover","text":"<pre><code>validated_close_powder_dispenser_cover()\n</code></pre> <p>Validate and execute closing the powder dispenser cover.</p> <p>Requires the manipulator tool to be active, a mold to be placed on the scale, and the tool to be engaged at the scale_active position.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_move_to_global_ready","title":"validated_move_to_global_ready","text":"<pre><code>validated_move_to_global_ready()\n</code></pre> <p>Validate and execute movement to the global ready position.</p> <p>Resolves base coordinates from config and moves to global_ready.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_home_tamper","title":"validated_home_tamper","text":"<pre><code>validated_home_tamper(tamper_axis=None)\n</code></pre> <p>Validate and execute tamper (V-axis) homing.</p> <p>Uses the mold cavity as a mechanical reference when carrying a mold without a top piston:</p> <ul> <li>Start: <code>v=2</code> (tamper inserted into mold)</li> <li>End: <code>v=-7</code> (tamper at mold bottom)</li> </ul> PARAMETER DESCRIPTION <code>tamper_axis</code> <p>Axis letter; defaults to <code>manipulator.tamper_axis</code> from <code>system_config.json</code>.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Warning <p>Do not home while the mold has a top piston inserted. The travel path can damage the tamper or piston.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_home_all","title":"validated_home_all","text":"<pre><code>validated_home_all()\n</code></pre> <p>Validate and execute homing for all axes (X, Y, Z, U).</p> <p>Returns the machine to <code>global_ready</code> and sets <code>z_height_id</code> to <code>mold_transfer_safe</code> on success.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Warning <p>Requires empty payload and no manipulator tool picked up. Homing with a mold or active tool can collide with labware.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_home_manipulator","title":"validated_home_manipulator","text":"<pre><code>validated_home_manipulator(manipulator_axis=None)\n</code></pre> <p>Validate and execute homing for the manipulator axis (V).</p> PARAMETER DESCRIPTION <code>manipulator_axis</code> <p>Axis letter; defaults to <code>manipulator.tamper_axis</code> from <code>system_config.json</code>.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Warning <p>Requires <code>payload_state</code> of <code>empty</code>. For homing while holding a mold without top piston, use :meth:<code>validated_home_tamper</code> instead.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_home_trickler","title":"validated_home_trickler","text":"<pre><code>validated_home_trickler(trickler_axis='W')\n</code></pre> <p>Validate and execute homing for the trickler axis (W).</p> <p>Can be homed at any time with no requirements.</p> PARAMETER DESCRIPTION <code>trickler_axis</code> <p>Axis letter for trickler (default 'W')</p> <p> TYPE: <code>str</code> DEFAULT: <code>'W'</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_pickup_tool","title":"validated_pickup_tool","text":"<pre><code>validated_pickup_tool(tool)\n</code></pre> <p>Validate and execute picking up a tool.</p> <p>Valid only from global_ready position. Requires no tool already picked up and mold_transfer_safe z_height. Returns to global_ready position. After the pickup completes, the active tool offset is switched to the tool's configured default offset (e.g. picking up a hardness tester loads the durometer offset). The offset is applied only AFTER the machine has settled at global_ready, so the offset transition cannot put the new tool tip in an unsafe position.</p> <p>Note: The machine's pickup_tool() method is decorated with @requires_safe_z, which automatically raises the bed height to deck.safe_z + 20 if it is not already at that height.</p> PARAMETER DESCRIPTION <code>tool</code> <p>The Tool object to pick up</p> <p> TYPE: <code>object</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_park_tool","title":"validated_park_tool","text":"<pre><code>validated_park_tool()\n</code></pre> <p>Validate and execute parking the current tool.</p> <p>Valid from global_ready position. Requires a supported tool to be active. Returns to global_ready position.</p> <p>Note: The machine's park_tool() method is decorated with @requires_safe_z, which automatically raises the bed height to deck.safe_z + 20 if it is not already at that height.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_retrieve_piston","title":"validated_retrieve_piston","text":"<pre><code>validated_retrieve_piston(manipulator_config)\n</code></pre> <p>Validate and execute piston retrieval at the current dispenser.</p> <p>Derives the dispenser index from <code>context.position_id</code> (for example <code>dispenser_ready_0</code> maps to dispenser 0). Call :meth:<code>validated_move_to_dispenser</code> first. On success, decrements the dispenser count and sets <code>has_top_piston</code> on the carried mold.</p> PARAMETER DESCRIPTION <code>manipulator_config</code> <p>Manipulator settings dict (must include <code>tamper_axis</code>).</p> <p> TYPE: <code>dict[str, object]</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>.</p> Note <p>Requires manipulator tool active, <code>mold_without_top_piston</code> payload, and a <code>dispenser_ready_N</code> position with pistons remaining.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.request_move","title":"request_move","text":"<pre><code>request_move(request)\n</code></pre> <p>Evaluate and, if permissible, initiate a move or action.</p> <p>If the request references an action, the FSM validates the action without changing state. Otherwise, the FSM transitions into the moving state and records the pending move for completion tracking.</p> PARAMETER DESCRIPTION <code>request</code> <p>Target position and optional action metadata.</p> <p> TYPE: <code>MoveRequest</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult indicating whether the request was accepted.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.perform_action","title":"perform_action","text":"<pre><code>perform_action(action_id)\n</code></pre> <p>Validate whether an auxiliary action is permitted at the current state.</p> PARAMETER DESCRIPTION <code>action_id</code> <p>Action identifier from <code>motion_platform_positions.json</code>.</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with <code>valid=True</code> when all action constraints</p> <code>MoveValidationResult</code> <p>(tool, engagement, position scope, requirements) are satisfied.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validate_move","title":"validate_move","text":"<pre><code>validate_move(request)\n</code></pre> <p>Core validation hook for requested moves.</p> This implementation verifies <ul> <li>The target position exists in the registry.</li> <li>The transition between current and target positions is permitted.</li> <li>Z-height and contextual requirements are satisfied.</li> <li>Engaged tools remain constrained to their ready points.</li> </ul> PARAMETER DESCRIPTION <code>request</code> <p>Move request with <code>target_position_id</code>.</p> <p> TYPE: <code>MoveRequest</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>MoveValidationResult with outcome (does not execute motion).</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.complete_move","title":"complete_move","text":"<pre><code>complete_move(*, tool_still_engaged)\n</code></pre> <p>Finalize a move previously initiated via <code>request_move</code>.</p> PARAMETER DESCRIPTION <code>tool_still_engaged</code> <p>Indicates whether the tool engagement status should keep the FSM in the tool_engaged state after the move.</p> <p> TYPE: <code>bool</code> </p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.request_tool_engagement","title":"request_tool_engagement","text":"<pre><code>request_tool_engagement()\n</code></pre> <p>Transition from idle to tool engaged at the current position.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. Succeeds only when the position</p> <code>MoveValidationResult</code> <p><code>allows_tool_engagement</code> and engagement requirements pass.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.request_tool_disengagement","title":"request_tool_disengagement","text":"<pre><code>request_tool_disengagement()\n</code></pre> <p>Disengage the tool and return the FSM to idle.</p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code>. Requires the FSM to be in</p> <code>MoveValidationResult</code> <p><code>tool_engaged</code> with a known <code>engaged_ready_position_id</code>.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.register_tool","title":"register_tool","text":"<pre><code>register_tool(tool_status)\n</code></pre> <p>Introduce or update a tool within the motion context.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.update_tool_engagement","title":"update_tool_engagement","text":"<pre><code>update_tool_engagement(tool_id, engaged)\n</code></pre> <p>Update the engagement flag for a specific tool.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.update_context","title":"update_context","text":"<pre><code>update_context(\n    *,\n    active_tool_id=None,\n    payload_state=None,\n    z_height_id=None\n)\n</code></pre> <p>Mutate commonly updated :class:<code>MotionContext</code> fields.</p> PARAMETER DESCRIPTION <code>active_tool_id</code> <p>Tool name to record as active, or <code>None</code>.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>payload_state</code> <p>Manipulator payload (<code>empty</code>, <code>mold_without_top_piston</code>, <code>mold_with_top_piston</code>).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>z_height_id</code> <p>Active z-height policy id.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Warning <p>Bypasses validation. Use only when the physical machine state is known to match the values being set.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.validate_machine_state","title":"validate_machine_state","text":"<pre><code>validate_machine_state(\n    machine_x, machine_y, machine_z, machine_v\n)\n</code></pre> <p>Check physical coordinates against the FSM's expected position.</p> <p>Called automatically before validated moves and actions.</p> PARAMETER DESCRIPTION <code>machine_x</code> <p>Reported X coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_y</code> <p>Reported Y coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_z</code> <p>Reported Z coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_v</code> <p>Reported V coordinate (mm).</p> <p> TYPE: <code>float</code> </p> RETURNS DESCRIPTION <code>MoveValidationResult</code> <p>class:<code>MoveValidationResult</code> with coordinate mismatch details in</p> <code>MoveValidationResult</code> <p><code>reason</code> when validation fails.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.on_enter_moving","title":"on_enter_moving","text":"<pre><code>on_enter_moving()\n</code></pre> <p>Hook invoked when entering the moving state.</p> <p>Future implementations can trigger hardware-level commands or logging from this hook. The current framework simply asserts that a pending move exists when transitions occur.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine.on_enter_idle","title":"on_enter_idle","text":"<pre><code>on_enter_idle()\n</code></pre> <p>Reset pending move tracking when returning to idle.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._assert_engagement_exit_ready","title":"_assert_engagement_exit_ready","text":"<pre><code>_assert_engagement_exit_ready()\n</code></pre> <p>Ensure the machine satisfies engagement requirements before exiting.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._validate_requirements","title":"_validate_requirements","text":"<pre><code>_validate_requirements(requirements)\n</code></pre> <p>Validate context attributes against a requirements mapping.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._validate_excludes","title":"_validate_excludes","text":"<pre><code>_validate_excludes(excludes)\n</code></pre> <p>Validate that context attributes do not match excluded values.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._value_matches","title":"_value_matches  <code>staticmethod</code>","text":"<pre><code>_value_matches(actual, expected)\n</code></pre> <p>Determine whether a context value satisfies an expected requirement.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._machine_coords_from_position","title":"_machine_coords_from_position  <code>staticmethod</code>","text":"<pre><code>_machine_coords_from_position(current_pos)\n</code></pre> <p>Convert machine position payload into validated numeric coordinates.</p>"},{"location":"api/motion-platform/#src.MotionPlatformStateMachine.MotionPlatformStateMachine._format_options","title":"_format_options  <code>staticmethod</code>","text":"<pre><code>_format_options(options)\n</code></pre> <p>Render a collection of options as a comma-separated string.</p>"},{"location":"api/motion-platform/#state-tracking","title":"State Tracking","text":""},{"location":"api/motion-platform/#current-state","title":"Current State","text":"<p>The state machine maintains:</p> <pre><code>{\n    \"position_id\": \"global_ready\",           # Current named position\n    \"active_tool_id\": \"manipulator\",         # Active tool name (or None)\n    \"payload_state\": \"empty\",                # What manipulator holds\n}\n</code></pre>"},{"location":"api/motion-platform/#payload-states","title":"Payload States","text":"State Description <code>empty</code> Manipulator holds nothing <code>mold_without_top_piston</code> Manipulator holds a mold without a top piston <code>mold_with_top_piston</code> Manipulator holds a mold containing a top piston"},{"location":"api/motion-platform/#position-names","title":"Position Names","text":"<p>Named positions are defined in <code>motion_platform_positions.json</code>. See Position Configuration for the full schema and editing guide.</p> <ul> <li><code>global_ready</code>: Safe position away from all labware</li> <li><code>scale_ready</code> / <code>scale_active</code>: Positions for scale access and weighing</li> <li><code>mold_ready_N</code>: Positions for specific wells (e.g., <code>mold_ready_0</code>, <code>mold_ready_1</code>)</li> <li><code>dispenser_ready_N</code>: Positions for piston dispensers</li> </ul> <p>Config is the source of truth</p> <p>Position IDs, coordinates, transitions, and payload rules live in JSON only. Do not hardcode coordinates or invent position names in application code.</p>"},{"location":"api/motion-platform/#validation-results","title":"Validation Results","text":"<p>All validated methods return a <code>MoveValidationResult</code>:</p> <pre><code>@dataclass\nclass MoveValidationResult:\n    valid: bool              # Whether operation is allowed\n    reason: str | None = None  # Explanation if not valid\n</code></pre>"},{"location":"api/motion-platform/#example-usage","title":"Example Usage","text":"<pre><code>result = state_machine.validated_move_to_scale()\n\nif result.valid:\n    print(\"Movement successful\")\nelse:\n    print(f\"Movement failed: {result.reason}\")\n</code></pre> <p>Always check <code>MoveValidationResult.valid</code></p> <p>Validated methods never raise for rule violations; they return <code>valid=False</code> with a <code>reason</code> string. Log or surface <code>result.reason</code> before retrying or changing state.</p>"},{"location":"api/motion-platform/#common-operations","title":"Common Operations","text":""},{"location":"api/motion-platform/#homing","title":"Homing","text":"<pre><code># Home all axes (X, Y, Z, U)\nresult = state_machine.validated_home_all()\nif not result.valid:\n    print(f\"Homing failed: {result.reason}\")\n\n# Home manipulator axis (V)\nresult = state_machine.validated_home_manipulator(manipulator_axis='V')\nif not result.valid:\n    print(f\"Manipulator homing failed: {result.reason}\")\n</code></pre> <p>Requirements: - No tool picked up (for <code>validated_home_all</code>) - Payload must be empty - Currently at a named position</p>"},{"location":"api/motion-platform/#tool-management","title":"Tool Management","text":"<pre><code># Pick up a tool\nfrom src.Manipulator import Manipulator\n\nmanipulator = Manipulator(\n    index=0,\n    name=\"manipulator\",\n    state_machine=state_machine\n)\n\nresult = state_machine.validated_pickup_tool(manipulator)\nif not result.valid:\n    print(f\"Tool pickup failed: {result.reason}\")\n\n# Park the current tool\nresult = state_machine.validated_park_tool()\nif not result.valid:\n    print(f\"Tool parking failed: {result.reason}\")\n</code></pre> <p>Requirements: - Must be at <code>global_ready</code> position - Payload must be <code>empty</code> - No tool already picked up (for pickup) - Manipulator tool must be active (for parking) - Z-height must be at <code>mold_transfer_safe</code></p>"},{"location":"api/motion-platform/#position-movements","title":"Position Movements","text":"<pre><code># Move to scale\nresult = state_machine.validated_move_to_scale()\n\n# Move to mold slot\nresult = state_machine.validated_move_to_mold_slot(well_id=\"0\")\n\n# Move to the next dispenser that has pistons available\nresult = state_machine.validated_move_to_dispenser()\n</code></pre> <p>Requirements vary by destination: - Correct tool must be active - Payload state must be allowed at destination - Valid transition must exist from current position</p>"},{"location":"api/motion-platform/#specialized-operations","title":"Specialized Operations","text":"<pre><code># Fill powder to target weight\nresult = state_machine.validated_fill_powder(target_weight=50.0)\n\n# Retrieve piston from dispenser\nresult = state_machine.validated_retrieve_piston(\n    manipulator_config=manipulator._get_config_dict()\n)\n</code></pre>"},{"location":"api/motion-platform/#creating-from-configuration","title":"Creating from Configuration","text":"PythonConfig <pre><code>from pathlib import Path\nfrom science_jubilee.Machine import Machine\nfrom src.Scale import Scale\nfrom src.MotionPlatformStateMachine import MotionPlatformStateMachine\n\nmachine = Machine(address=\"192.168.1.100\")\nmachine.connect()\n\nscale = Scale(port=\"/dev/ttyUSB0\")\nscale.connect()\n\nconfig_path = Path(\"jubilee_api_config/motion_platform_positions.json\")\nstate_machine = MotionPlatformStateMachine.from_config_file(\n    path=config_path,\n    machine=machine,\n    scale=scale,\n)\n\nstate_machine.initialize_deck()\nstate_machine.initialize_dispensers(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10,\n)\n</code></pre> <p>Positions load from <code>jubilee_api_config/motion_platform_positions.json</code> (validated by Pydantic models in <code>src/motion_config.py</code>). Tool names referenced in position <code>requirements</code> come from <code>system_config.json</code> via <code>ConfigLoader</code>.</p> <pre><code>{\n  \"id\": \"scale_ready\",\n  \"type\": \"SCALE_READY\",\n  \"coordinates\": {\n    \"x\": 150.0,\n    \"y\": 80.0,\n    \"z\": \"USE_Z_HEIGHT_POLICY\",\n    \"v\": 30.0\n  },\n  \"requirements\": {\n    \"active_tool_id\": \"manipulator\"\n  },\n  \"z_height_policy\": {\n    \"allowed\": [\"mold_transfer_safe\"]\n  }\n}\n</code></pre> <p>Restart or reconnect after editing position JSON. See Position Configuration for the full file layout.</p>"},{"location":"api/motion-platform/#state-updates","title":"State Updates","text":""},{"location":"api/motion-platform/#manual-state-updates","title":"Manual State Updates","text":"<p>In some cases, you may need to manually update the state:</p> <pre><code># Update multiple state fields\nstate_machine.update_context(\n    active_tool_id=\"manipulator\",\n    payload_state=\"mold_without_top_piston\",\n)\n\n# Update position\nstate_machine.context.position_id = \"scale_ready\"\n</code></pre> <p>Manual Updates</p> <p>Manual state updates bypass validation. Only use when you're certain the physical state matches what you're setting.</p>"},{"location":"api/motion-platform/#validation-logic","title":"Validation Logic","text":""},{"location":"api/motion-platform/#position-validation","title":"Position Validation","text":"<p>The state machine validates movements based on:</p> <ol> <li>Current position: Must be at a valid starting position</li> <li>Target position: Must be defined in configuration</li> <li>Transition exists: Direct path must be allowed</li> <li>Tool requirement: Correct tool must be active</li> <li>Payload constraint: Payload must be allowed at target</li> </ol>"},{"location":"api/motion-platform/#example-validation-failure","title":"Example Validation Failure","text":"<pre><code># Trying to move to scale with wrong payload\nstate_machine.context.payload_state = \"invalid_state\"\nresult = state_machine.validated_move_to_scale()\n\n# Result:\n# valid = False\n# reason = \"Payload state 'invalid_state' not allowed at position 'scale_ready'\"\n</code></pre>"},{"location":"api/motion-platform/#configuration-structure","title":"Configuration Structure","text":"<p>The state machine reads <code>motion_platform_positions.json</code> at connect time. The file uses top-level <code>z_heights</code>, <code>coordinate_tolerance</code>, <code>positions</code> (array), and <code>actions</code> keys. Movement rules are expressed through each position's <code>allowed_origins</code>, <code>allowed_destinations</code>, and <code>requirements</code>, not a separate transitions map.</p> <p>Do not use legacy position schemas</p> <p>Older examples that show <code>positions</code> as a dict with a <code>transitions</code> block are obsolete. Follow Position Configuration and the checked-in JSON under <code>jubilee_api_config/</code>.</p>"},{"location":"api/motion-platform/#advanced-usage","title":"Advanced Usage","text":"Custom sequences and internal state <p>Chain validated methods for multi-step workflows, or inspect context when debugging. Prefer <code>JubileeManager</code> for production dispense flows.</p>"},{"location":"api/motion-platform/#custom-movement-sequences","title":"Custom Movement Sequences","text":"<p>For complex operations, chain validated movements:</p> <pre><code>def custom_operation(state_machine, manipulator, well_id, target_weight):\n    \"\"\"Example custom operation using state machine.\"\"\"\n\n    result = state_machine.validated_move_to_mold_slot(well_id)  # (1)!\n    if not result.valid:\n        return False, f\"Move to slot failed: {result.reason}\"\n\n    manipulator.pick_mold(well_id)  # (2)!\n\n    result = state_machine.validated_move_to_scale()  # (3)!\n    if not result.valid:\n        return False, f\"Move to scale failed: {result.reason}\"\n\n    manipulator.place_mold_on_scale()  # (4)!\n\n    result = state_machine.validated_fill_powder(target_weight)  # (5)!\n    if not result.valid:\n        return False, f\"Fill failed: {result.reason}\"\n\n    return True, \"Success\"\n</code></pre> <ol> <li>Validate and execute each move before continuing.</li> <li>Pick updates payload state via <code>validated_pick_mold</code> (raises <code>ToolStateError</code> on failure).</li> <li>Destination rules come from position <code>requirements</code> in JSON.</li> <li>Place engages the tool at <code>scale_active</code> so fill can run on the scale.</li> <li>Powder fill runs trickler logic through the state machine.</li> </ol>"},{"location":"api/motion-platform/#accessing-internal-state","title":"Accessing Internal State","text":"<pre><code>position = state_machine.context.position_id\ntool = state_machine.context.active_tool_id\npayload = state_machine.context.payload_state\n\nprint(f\"State: pos={position}, tool={tool}, payload={payload}\")\n\nmachine = state_machine.machine\nscale = state_machine.context.scale\ndeck = state_machine.context.deck\ndispensers = state_machine.context.piston_dispensers\n</code></pre>"},{"location":"api/motion-platform/#error-handling","title":"Error Handling","text":""},{"location":"api/motion-platform/#common-validation-errors","title":"Common Validation Errors","text":"Error Reason Cause Solution \"Position not found\" Target position not in config Add position to configuration \"No transition defined\" Movement not allowed Add transition or use intermediate position \"Wrong tool active\" Tool requirement not met Pick up required tool first \"Payload not allowed\" Payload state incompatible Change payload or destination \"Tool already picked up\" Tool state conflict Park current tool first"},{"location":"api/motion-platform/#debugging-validation-failures","title":"Debugging Validation Failures","text":"<pre><code>result = state_machine.validated_move_to_scale()\n\nif not result.valid:\n    print(f\"Validation failed: {result.reason}\")\n    print(\"Current state:\")\n    print(f\"  Position: {state_machine.context.position_id}\")\n    print(f\"  Tool: {state_machine.context.active_tool_id}\")\n    print(f\"  Payload: {state_machine.context.payload_state}\")\n</code></pre> <p>Compare against position requirements</p> <p>Cross-check <code>result.reason</code> with the target position's <code>requirements</code> and <code>allowed_origins</code> / <code>allowed_destinations</code> in Position Configuration. Most failures are a payload, tool, or transition mismatch.</p>"},{"location":"api/motion-platform/#thread-safety","title":"Thread Safety","text":"<p>Not Thread-Safe</p> <p>The state machine is not thread-safe. Do not call methods from multiple threads simultaneously. Use appropriate locking if multi-threaded access is required.</p>"},{"location":"api/motion-platform/#see-also","title":"See Also","text":"<ul> <li>JubileeManager - High-level interface (recommended)</li> <li>Position Configuration - <code>motion_platform_positions.json</code> schema and transitions</li> <li>ConfigLoader - <code>system_config.json</code> loading</li> <li>Architecture Guide - System design overview</li> <li>Configuration Guide - Setting up positions and transitions</li> </ul>"},{"location":"api/piston-dispenser/","title":"PistonDispenser","text":"<p>The <code>PistonDispenser</code> class tracks piston inventory for one side-mounted dispenser station. Physical retrieval is executed by the motion platform and manipulator; this class decrements the count after each successful retrieval.</p> Operator GuideAPI Reference <p>Prefer JubileeManager</p> <p>Production workflows should call <code>JubileeManager.move_to_dispenser()</code> and <code>get_piston_from_dispenser()</code> rather than manipulating dispenser state directly.</p>"},{"location":"api/piston-dispenser/#overview","title":"Overview","text":"<p>Each <code>PistonDispenser</code> instance:</p> <ul> <li>Tracks remaining piston count for one dispenser index</li> <li>Exposes the motion platform ready position name (<code>dispenser_ready_N</code>)</li> <li>Decrements count via <code>remove_piston()</code> after validated retrieval</li> </ul>"},{"location":"api/piston-dispenser/#usage-examples","title":"Usage Examples","text":""},{"location":"api/piston-dispenser/#creating-a-dispenser","title":"Creating a Dispenser","text":"<pre><code>from src.PistonDispenser import PistonDispenser\n\ndispenser = PistonDispenser(\n    index=0,\n    num_pistons=10,\n)\nprint(dispenser.ready_pos)  # \"dispenser_ready_0\"\n</code></pre>"},{"location":"api/piston-dispenser/#basic-operations","title":"Basic Operations","text":"<pre><code>print(f\"Pistons available: {dispenser.num_pistons}\")\n\ndispenser.remove_piston()\nprint(f\"Pistons remaining: {dispenser.num_pistons}\")\n\nif dispenser.num_pistons == 0:\n    print(\"Dispenser is empty!\")\n</code></pre> <p><code>remove_piston()</code> raises <code>ValueError</code> when the dispenser is already empty.</p>"},{"location":"api/piston-dispenser/#integration-with-jubileemanager","title":"Integration with JubileeManager","text":"<pre><code>from src.JubileeManager import JubileeManager\n\nmanager = JubileeManager(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10,\n)\nmanager.connect()\n\nmanager.move_to_dispenser()\nmanager.get_piston_from_dispenser()\n\nfor dispenser in manager.piston_dispensers:\n    print(f\"Dispenser {dispenser.index}: {dispenser.num_pistons} pistons\")\n</code></pre>"},{"location":"api/piston-dispenser/#piston-tracking","title":"Piston Tracking","text":"<p>The count decrements only after the state machine completes a validated retrieval:</p> <pre><code>dispenser = PistonDispenser(index=0, num_pistons=5)\nprint(dispenser.num_pistons)  # 5\ndispenser.remove_piston()\nprint(dispenser.num_pistons)  # 4\n</code></pre> <p>Before attempting retrieval, check availability:</p> <pre><code>if dispenser.num_pistons &gt; 0:\n    dispenser.remove_piston()\nelse:\n    print(\"Dispenser empty - needs refilling\")\n</code></pre>"},{"location":"api/piston-dispenser/#position-configuration","title":"Position Configuration","text":"<p>Dispenser ready positions live in <code>motion_platform_positions.json</code>. Each entry uses the <code>DISPENSER_READY</code> type and requires the manipulator tool with a mold that has no top piston:</p> <pre><code>{\n  \"id\": \"dispenser_ready_0\",\n  \"type\": \"DISPENSER_READY\",\n  \"description\": \"Ready position for dispenser station 0.\",\n  \"coordinates\": {\n    \"x\": 298.0,\n    \"y\": 140.0,\n    \"z\": 95.0,\n    \"v\": 34.0\n  },\n  \"requirements\": {\n    \"active_tool_id\": \"manipulator\",\n    \"payload_state\": \"mold_without_top_piston\"\n  }\n}\n</code></pre> <p>Initial dispenser counts are set at connection time via <code>system_config.json</code>:</p> <pre><code>{\n  \"machine\": {\n    \"num_dispensers\": 2,\n    \"pistons_per_dispenser\": 10\n  }\n}\n</code></pre> <p>See Position Configuration for adding new dispenser stations.</p>"},{"location":"api/piston-dispenser/#retrieval-workflow","title":"Retrieval Workflow","text":"<p>When <code>JubileeManager.get_piston_from_dispenser()</code> runs:</p> <ol> <li>Validates the machine is at <code>dispenser_ready_N</code> for the target dispenser</li> <li>Confirms manipulator tool is active and payload is <code>mold_without_top_piston</code></li> <li>Executes the <code>retrieve_piston</code> motion action</li> <li>Decrements <code>num_pistons</code> on success</li> </ol>"},{"location":"api/piston-dispenser/#multiple-dispensers","title":"Multiple Dispensers","text":"<pre><code>manager = JubileeManager(\n    num_piston_dispensers=3,\n    num_pistons_per_dispenser=10,\n)\nmanager.connect()\n\nfor dispenser in manager.piston_dispensers:\n    if dispenser.num_pistons &gt; 0:\n        manager.move_to_dispenser()\n        manager.get_piston_from_dispenser()\n        break\n</code></pre>"},{"location":"api/piston-dispenser/#state-machine-integration","title":"State Machine Integration","text":"<p>Advanced callers can invoke validation directly after moving to a dispenser ready position:</p> <pre><code>result = state_machine.validated_retrieve_piston(\n    manipulator_config=manipulator._get_config_dict()\n)\n\nif result.valid:\n    print(\"Piston retrieved successfully\")\nelse:\n    print(f\"Retrieval failed: {result.reason}\")\n</code></pre> <p>Requirements:</p> <ul> <li>Current position must be <code>dispenser_ready_N</code></li> <li>Manipulator tool must be active</li> <li>Payload must be <code>mold_without_top_piston</code></li> <li>Dispenser must have pistons available</li> </ul>"},{"location":"api/piston-dispenser/#error-handling","title":"Error Handling","text":"<pre><code>try:\n    dispenser.remove_piston()\nexcept ValueError as e:\n    print(f\"Cannot dispense: {e}\")\n</code></pre> <p>Common <code>validated_retrieve_piston</code> failure reasons:</p> <ul> <li>Dispenser has no pistons</li> <li>Not at a <code>dispenser_ready</code> position</li> <li>Wrong payload state or tool active</li> </ul>"},{"location":"api/piston-dispenser/#refilling-dispensers","title":"Refilling Dispensers","text":"<pre><code>def refill_dispenser(dispenser, num_pistons):\n    dispenser.num_pistons = num_pistons\n    print(f\"Dispenser {dispenser.index} refilled with {num_pistons} pistons\")\n</code></pre> <p>Physical refill required</p> <p>Software only tracks inventory. Load pistons into the physical dispenser, then update <code>num_pistons</code> via the refill helper or the web UI <code>PUT /api/dispensers/{index}</code> endpoint.</p>"},{"location":"api/piston-dispenser/#best-practices","title":"Best Practices","text":"<ul> <li>Check <code>num_pistons &gt; 0</code> before retrieval; never call <code>remove_piston()</code> on an empty dispenser.</li> <li>Monitor levels during batch jobs; stop when all dispensers are empty.</li> <li>Prefer the fullest dispenser when multiple stations are available.</li> </ul>"},{"location":"api/piston-dispenser/#class-reference","title":"Class Reference","text":""},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser","title":"PistonDispenser","text":"<pre><code>PistonDispenser(index, num_pistons)\n</code></pre> <p>Tracks piston count and ready position for one side-mounted dispenser.</p> <p>Physical piston retrieval is executed by the motion platform and :class:<code>~src.Manipulator.Manipulator</code>; this class maintains the software inventory count decremented after each successful retrieval.</p> ATTRIBUTE DESCRIPTION <code>index</code> <p>Zero-based dispenser index on the machine.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>Remaining piston count in this dispenser.</p> <p> TYPE: <code>int</code> </p> <code>ready_pos</code> <p>Motion platform position name (e.g. <code>\"dispenser_ready_0\"</code>).</p> <p> TYPE: <code>str</code> </p> Note <p>Piston retrieval workflows go through :meth:<code>~src.MotionPlatformStateMachine.MotionPlatformStateMachine.validated_retrieve_piston</code>. Prefer :class:<code>~src.JubileeManager.JubileeManager</code> for high-level <code>move_to_dispenser()</code> / <code>get_piston_from_dispenser()</code> sequences.</p> <p>Initialize dispenser state.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based dispenser index on the machine.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>Initial piston count loaded in the dispenser.</p> <p> TYPE: <code>int</code> </p> Note <p><code>ready_pos</code> is set automatically to <code>dispenser_ready_{index}</code>.</p>"},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser-attributes","title":"Attributes","text":""},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser.index","title":"index  <code>instance-attribute</code>","text":"<pre><code>index = index\n</code></pre>"},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser.num_pistons","title":"num_pistons  <code>instance-attribute</code>","text":"<pre><code>num_pistons = num_pistons\n</code></pre>"},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser.ready_pos","title":"ready_pos  <code>instance-attribute</code>","text":"<pre><code>ready_pos = f'dispenser_ready_{index}'\n</code></pre>"},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser-functions","title":"Methods:","text":""},{"location":"api/piston-dispenser/#src.PistonDispenser.PistonDispenser.remove_piston","title":"remove_piston","text":"<pre><code>remove_piston()\n</code></pre> <p>Decrement piston count after a piston is dispensed.</p> <p>Called by the motion platform after a validated piston retrieval completes.</p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>num_pistons</code> is already zero.</p> Example <p>Check availability before dispensing::</p> <pre><code>if dispenser.num_pistons &gt; 0:\n    dispenser.remove_piston()\nelse:\n    print(\"Dispenser empty - needs refilling\")\n</code></pre>"},{"location":"api/piston-dispenser/#see-also","title":"See Also","text":"<ul> <li>JubileeManager - High-level dispenser operations</li> <li>MotionPlatformStateMachine - Validated piston retrieval</li> <li>Manipulator - Gripper operations</li> <li>Position Configuration - Adding dispenser ready positions</li> <li>Configuration Guide - Machine setup</li> </ul>"},{"location":"api/position-config/","title":"Position Configuration Reference","text":"<p><code>jubilee_api_config/motion_platform_positions.json</code> is the single file that defines every named location the Jubilee can move to, every action it can perform, and the safety constraints that govern both. The state machine loads this file once at connection time (validated by Pydantic models in <code>src/motion_config.py</code>) and enforces movement between these positions. Invalid or incomplete JSON fails at load with a clear error. Tool names are taken from <code>system_config.json</code> via ConfigLoader, not re-parsed from a second copy of that file.</p> <p>This means no code changes are needed for routine calibration or layout adjustments; edit <code>motion_platform_positions.json</code> instead.</p> <p>Restart required after edits</p> <p>The configuration is read once during <code>connect()</code>. Restart the Python process (or reconnect) after making any changes.</p>"},{"location":"api/position-config/#python-api","title":"Python API","text":"<p>Pydantic models validate the JSON at load time; <code>PositionRegistry</code> builds runtime descriptors for the state machine. Docstrings on these symbols complement the schema sections below.</p>"},{"location":"api/position-config/#src.motion_config","title":"motion_config","text":"<p>Pydantic models for <code>api_config/motion_platform_positions.json</code>.</p> <p>Validated JSON is converted into runtime descriptors by :class:<code>~src.MotionPlatformStateMachine.PositionRegistry</code>. Tool names referenced in position requirements come from :mod:<code>src.ConfigLoader</code>, not a second copy of <code>system_config.json</code>.</p> Note <p>The state machine reads this file once at <code>connect()</code>. Restart or reconnect after editing the JSON on disk.</p>"},{"location":"api/position-config/#src.motion_config-classes","title":"Classes","text":""},{"location":"api/position-config/#src.motion_config.MotionPlatformConfig","title":"MotionPlatformConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Root model for <code>motion_platform_positions.json</code>.</p>"},{"location":"api/position-config/#src.motion_config.MotionPlatformConfig-functions","title":"Methods:","text":""},{"location":"api/position-config/#src.motion_config.MotionPlatformConfig.required_z_height_ids","title":"required_z_height_ids","text":"<pre><code>required_z_height_ids()\n</code></pre> <p>Ensure required z-height ids are present.</p>"},{"location":"api/position-config/#src.motion_config.MotionPositionConfig","title":"MotionPositionConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Single entry from the <code>positions</code> array.</p> ATTRIBUTE DESCRIPTION <code>id</code> <p>Lowercase position identifier (for example <code>scale_ready</code>).</p> <p> TYPE: <code>str</code> </p> <code>type</code> <p>UPPER_CASE type name used for transition expansion.</p> <p> TYPE: <code>str</code> </p> <code>allowed_origins</code> <p>Positions or types permitted as move sources.</p> <p> TYPE: <code>list[str]</code> </p> <code>allowed_destinations</code> <p>Positions or types permitted as move targets.</p> <p> TYPE: <code>list[str]</code> </p> <code>requirements</code> <p>Context fields that must match before arrival.</p> <p> TYPE: <code>dict[str, Any]</code> </p> <code>z_height_policy</code> <p>Allowed and required z-height ids at this position.</p> <p> TYPE: <code>ZHeightPolicyConfig</code> </p> <code>coordinates</code> <p>Optional axis coordinates; <code>z</code> may be <code>USE_Z_HEIGHT_POLICY</code>.</p> <p> TYPE: <code>CoordinatesConfig | None</code> </p> <code>engagement</code> <p>Optional tool-engagement requirements and allowed actions.</p> <p> TYPE: <code>EngagementConfig | None</code> </p>"},{"location":"api/position-config/#src.motion_config.MotionPositionConfig-functions","title":"Methods:","text":""},{"location":"api/position-config/#src.motion_config.MotionPositionConfig.z_policy_when_use_z_height_policy","title":"z_policy_when_use_z_height_policy","text":"<pre><code>z_policy_when_use_z_height_policy()\n</code></pre> <p>Require z-height policy when <code>coordinates.z</code> is <code>USE_Z_HEIGHT_POLICY</code>.</p>"},{"location":"api/position-config/#src.motion_config.MotionActionConfig","title":"MotionActionConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Single entry from the <code>actions</code> array.</p> ATTRIBUTE DESCRIPTION <code>id</code> <p>Action identifier referenced from validated FSM methods.</p> <p> TYPE: <code>str</code> </p> <code>position_scope</code> <p>Position ids or types where the action may run.</p> <p> TYPE: <code>list[str]</code> </p> <code>requirements</code> <p>Context fields that must match before execution.</p> <p> TYPE: <code>dict[str, Any]</code> </p> <code>excludes</code> <p>Inverse of <code>requirements</code>; matching values block the action.</p> <p> TYPE: <code>dict[str, Any]</code> </p> <code>required_tool_id</code> <p>Tool name that must be active, if any.</p> <p> TYPE: <code>str | None</code> </p> <code>requires_tool_engaged</code> <p>Whether the tool must be in the engaged state.</p> <p> TYPE: <code>bool</code> </p> <code>blocked_when_engaged</code> <p>Whether engagement blocks this action.</p> <p> TYPE: <code>bool</code> </p>"},{"location":"api/position-config/#src.motion_config.MotionActionConfig-functions","title":"Methods:","text":""},{"location":"api/position-config/#src.motion_config.MotionActionConfig.safety_critical_action_fields","title":"safety_critical_action_fields","text":"<pre><code>safety_critical_action_fields()\n</code></pre> <p>Enforce scope and requirements on safety-critical actions.</p>"},{"location":"api/position-config/#src.motion_config-functions","title":"Functions:","text":""},{"location":"api/position-config/#src.motion_config.load_motion_platform_config","title":"load_motion_platform_config","text":"<pre><code>load_motion_platform_config(payload)\n</code></pre> <p>Parse and validate a motion platform JSON payload.</p> PARAMETER DESCRIPTION <code>payload</code> <p>Decoded contents of <code>motion_platform_positions.json</code>.</p> <p> TYPE: <code>dict[str, Any]</code> </p> RETURNS DESCRIPTION <code>Validated</code> <p>class:<code>MotionPlatformConfig</code>.</p> <p> TYPE: <code>MotionPlatformConfig</code> </p> RAISES DESCRIPTION <code>ConfigError</code> <p>If validation fails.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry","title":"PositionRegistry","text":"<pre><code>PositionRegistry(positions)\n</code></pre> <p>In-memory index of validated motion platform positions and actions.</p> <p>Built from <code>motion_platform_positions.json</code> via :meth:<code>from_config_file</code>. The state machine uses this registry for transition rules, coordinate checks, and action lookup.</p> Note <p>Reload requires constructing a new registry (typically by reconnecting). Edits to the JSON file are not picked up from an existing instance.</p> <p>Register an initial set of position descriptors.</p> PARAMETER DESCRIPTION <code>positions</code> <p>Iterable of :class:<code>PositionDescriptor</code> instances to index.</p> <p> TYPE: <code>Iterable[PositionDescriptor]</code> </p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry-attributes","title":"Attributes","text":""},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry._positions","title":"_positions  <code>instance-attribute</code>","text":"<pre><code>_positions = {}\n</code></pre>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry._actions","title":"_actions  <code>instance-attribute</code>","text":"<pre><code>_actions = {}\n</code></pre>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry._z_heights","title":"_z_heights  <code>instance-attribute</code>","text":"<pre><code>_z_heights = {}\n</code></pre>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry._coordinate_tolerance","title":"_coordinate_tolerance  <code>instance-attribute</code>","text":"<pre><code>_coordinate_tolerance = {}\n</code></pre>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry._supported_tool_ids","title":"_supported_tool_ids  <code>instance-attribute</code>","text":"<pre><code>_supported_tool_ids = frozenset()\n</code></pre>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.actions","title":"actions  <code>property</code>","text":"<pre><code>actions\n</code></pre> <p>Shallow copy of registered action descriptors keyed by id.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.z_heights","title":"z_heights  <code>property</code>","text":"<pre><code>z_heights\n</code></pre> <p>Shallow copy of z-height entries from config.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.coordinate_tolerance","title":"coordinate_tolerance  <code>property</code>","text":"<pre><code>coordinate_tolerance\n</code></pre> <p>Per-axis coordinate tolerance in millimeters.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.supported_tool_ids","title":"supported_tool_ids  <code>property</code>","text":"<pre><code>supported_tool_ids\n</code></pre> <p>Tool names allowed by validated <code>system_config.json</code>.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry-functions","title":"Methods:","text":""},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.from_config_file","title":"from_config_file  <code>classmethod</code>","text":"<pre><code>from_config_file(path, *, system_config_path=None)\n</code></pre> <p>Load and validate a motion platform config file.</p> <p>Expands type references (for example <code>MOLD_READY</code>) to concrete position ids and attaches actions, z-heights, and coordinate tolerance from the JSON file. Tool ids are taken from :mod:<code>src.ConfigLoader</code>, not <code>system_config_path</code>.</p> PARAMETER DESCRIPTION <code>path</code> <p>Path to <code>motion_platform_positions.json</code>.</p> <p> TYPE: <code>str | Path</code> </p> <code>system_config_path</code> <p>Deprecated; ignored. Retained for call-site compatibility.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>'PositionRegistry'</code> <p>Populated registry ready for the state machine.</p> RAISES DESCRIPTION <code>ConfigError</code> <p>If JSON validation fails.</p> <code>KeyError</code> <p>If a position <code>type</code> is unknown.</p> Warning <p><code>system_config_path</code> is ignored. Tool names always come from the :mod:<code>src.ConfigLoader</code> singleton loaded at import time.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.add_position","title":"add_position","text":"<pre><code>add_position(position)\n</code></pre> <p>Register a position descriptor.</p> PARAMETER DESCRIPTION <code>position</code> <p>Descriptor to add.</p> <p> TYPE: <code>PositionDescriptor</code> </p> RAISES DESCRIPTION <code>ValueError</code> <p>If <code>position.identifier</code> is already registered.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.get","title":"get","text":"<pre><code>get(identifier)\n</code></pre> <p>Return the descriptor for a position id.</p> PARAMETER DESCRIPTION <code>identifier</code> <p>Lowercase position id (for example <code>global_ready</code>).</p> <p> TYPE: <code>str</code> </p> RAISES DESCRIPTION <code>KeyError</code> <p>If the id is unknown.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.has","title":"has","text":"<pre><code>has(identifier)\n</code></pre> <p>Return whether <code>identifier</code> is a known position id.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.find_first_of_type","title":"find_first_of_type","text":"<pre><code>find_first_of_type(position_type)\n</code></pre> <p>Return the first descriptor matching <code>position_type</code>, if any.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.get_action","title":"get_action","text":"<pre><code>get_action(identifier)\n</code></pre> <p>Return the descriptor for an action id.</p> PARAMETER DESCRIPTION <code>identifier</code> <p>Action id from the <code>actions</code> array.</p> <p> TYPE: <code>str</code> </p> RAISES DESCRIPTION <code>KeyError</code> <p>If the action is unknown.</p>"},{"location":"api/position-config/#src.MotionPlatformStateMachine.PositionRegistry.validate_machine_position","title":"validate_machine_position","text":"<pre><code>validate_machine_position(\n    position_id,\n    machine_x,\n    machine_y,\n    machine_z,\n    machine_v,\n    current_z_height_id=None,\n)\n</code></pre> <p>Check reported machine coordinates against a position definition.</p> PARAMETER DESCRIPTION <code>position_id</code> <p>Position whose stored coordinates are the expected pose.</p> <p> TYPE: <code>str</code> </p> <code>machine_x</code> <p>Reported X coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_y</code> <p>Reported Y coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_z</code> <p>Reported Z coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>machine_v</code> <p>Reported V coordinate (mm).</p> <p> TYPE: <code>float</code> </p> <code>current_z_height_id</code> <p>Active z-height when expected Z uses <code>USE_Z_HEIGHT_POLICY</code>.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p><code>None</code> when coordinates match within tolerance, otherwise an</p> <code>str | None</code> <p>error message string.</p> Note <p>When <code>coordinates.z</code> is <code>USE_Z_HEIGHT_POLICY</code>, expected Z is resolved from :attr:<code>z_heights</code> using <code>current_z_height_id</code>.</p>"},{"location":"api/position-config/#when-to-edit-this-file","title":"When to edit this file","text":"Task What to change Machine was moved or a position drifted Update <code>coordinates</code> on the affected position(s) Physical layout changed (e.g., scale repositioned) Update <code>coordinates</code>, verify <code>allowed_origins</code> / <code>allowed_destinations</code> still reflect the real risk of collisions between positions Adding a new mold slot Add a new <code>MOLD_READY</code> position entry following the existing pattern, update <code>position_scope</code> list for <code>pick_up_mold</code>, <code>put_down_mold</code> Adding a new dispenser Add a new <code>DISPENSER_READY</code> position entry and update action <code>position_scope</code> lists Changing required machine state before a move Update <code>requirements</code> on the target position"},{"location":"api/position-config/#updating-position-coordinates","title":"Updating position coordinates","text":"<p>This is the most common edit. After physically calibrating a position on the machine, update its <code>x</code>, <code>y</code>, <code>z</code>, and <code>v</code> values:</p> <pre><code>{\n  \"id\": \"scale_ready\",\n  \"type\": \"SCALE_READY\",\n  ...\n  \"coordinates\": {\n    \"x\": 150.0,\n    \"y\": 143.0,\n    \"z\": 27.0,\n    \"v\": 67.0\n  }\n}\n</code></pre> <p><code>USE_Z_HEIGHT_POLICY</code></p> <p>A <code>z</code> value of <code>\"USE_Z_HEIGHT_POLICY\"</code> means the Z coordinate is taken from the active z-height at runtime rather than being hardcoded. Leave this string as-is unless you specifically want to fix the Z at a literal value.</p>"},{"location":"api/position-config/#file-structure","title":"File structure","text":"<p>The file has four top-level keys:</p> <pre><code>{\n  \"z_heights\":            { ... },\n  \"coordinate_tolerance\": { ... },\n  \"positions\":            [ ... ],\n  \"actions\":              [ ... ]\n}\n</code></pre> Key Purpose <code>z_heights</code> Named Z levels (e.g., <code>\"mold_transfer_safe\"</code>). Positions whose <code>z</code> is <code>\"USE_Z_HEIGHT_POLICY\"</code> resolve their Z from the context's active z-height at runtime. <code>coordinate_tolerance</code> Maximum allowed deviation (mm) between the machine's reported position and a position's stored coordinates before the state machine considers the machine \"out of position\". <code>positions</code> The main array. Every named location is an entry here. <code>actions</code> Tool operations (pick mold, retrieve piston, etc.). Validated and executed without changing the current position."},{"location":"api/position-config/#positions","title":"Positions","text":""},{"location":"api/position-config/#naming-and-type-conventions","title":"Naming and type conventions","text":"<p>Every position has an <code>id</code> (lowercase) and a <code>type</code> (UPPER_CASE). The type determines which group a position belongs to when <code>allowed_origins</code> / <code>allowed_destinations</code> reference it by type name.</p> <code>type</code> <code>id</code> pattern Example <code>GLOBAL_READY</code> <code>global_ready</code> <code>\"global_ready\"</code> <code>SCALE_READY</code> <code>scale_ready</code>, <code>scale_active</code> <code>\"scale_ready\"</code> <code>DISPENSER_READY</code> <code>dispenser_ready_N</code> <code>\"dispenser_ready_0\"</code> <code>MOLD_READY</code> <code>mold_ready_N</code> <code>\"mold_ready_3\"</code> <p>When referencing positions in <code>allowed_origins</code> or <code>allowed_destinations</code>, you can use either a specific ID (<code>\"dispenser_ready_0\"</code>) or a type name (<code>\"DISPENSER_READY\"</code>). A type name expands to all positions of that type at load time. E.g. <code>\"DISPENSER_READY\"</code> evaluates to <code>\"dispenser_ready_N\"</code> for all <code>N</code> dispenser ready positions.</p>"},{"location":"api/position-config/#position-fields","title":"Position fields","text":"<pre><code>{\n  \"id\": \"dispenser_ready_0\",\n  \"type\": \"DISPENSER_READY\",\n  \"description\": \"...\",\n  \"coordinates\": { \"x\": 298.0, \"y\": 140.0, \"z\": 95.0, \"v\": 34.0 },\n  \"allows_tool_engagement\": true,\n  \"allowed_origins\": [ \"DISPENSER_READY\", \"GLOBAL_READY\" ],\n  \"allowed_destinations\": [ \"DISPENSER_READY\", \"GLOBAL_READY\" ],\n  \"requirements\": {\n    \"active_tool_id\": \"manipulator\",\n    \"payload_state\": \"mold_without_top_piston\"\n  },\n  \"z_height_policy\": {\n    \"allowed\": [ \"mold_transfer_safe\" ],\n    \"required\": \"mold_transfer_safe\"\n  },\n  \"engagement\": {\n    \"requirements\": { ... }\n  }\n}\n</code></pre>"},{"location":"api/position-config/#allowed_origins-and-allowed_destinations","title":"<code>allowed_origins</code> and <code>allowed_destinations</code>","text":"<p>These two fields together define the transitions of the state machine. Before any move is executed, the state machine checks both:</p> <ul> <li>The current position's <code>allowed_destinations</code> contains the target position.</li> <li>The target position's <code>allowed_origins</code> contains the current position.</li> </ul> <p>Both must pass. If either fails, the move is rejected with a message describing the violation.</p> <p>Current transition rules:</p> From To <code>GLOBAL_READY</code> <code>GLOBAL_READY</code>, <code>SCALE_READY</code>, <code>DISPENSER_READY</code>, <code>MOLD_READY</code> <code>SCALE_READY</code> <code>SCALE_READY</code>, <code>GLOBAL_READY</code> <code>DISPENSER_READY</code> <code>DISPENSER_READY</code>, <code>GLOBAL_READY</code> <code>MOLD_READY</code> <code>MOLD_READY</code>, <code>GLOBAL_READY</code> <p>Notably: moving between a mold slot and a dispenser requires passing through <code>global_ready</code>.</p>"},{"location":"api/position-config/#requirements","title":"<code>requirements</code>","text":"<p><code>requirements</code> is a dict of <code>MotionContext</code> field names mapped to the value they must have before the move is permitted. This is checked before each move or action executes.</p> <p>The context fields you can use as requirement keys:</p> Key Type Meaning <code>active_tool_id</code> string or <code>null</code> Which tool is currently picked up (<code>\"manipulator\"</code> or <code>null</code>) <code>payload_state</code> string What the manipulator is carrying: <code>\"empty\"</code>, <code>\"mold_without_top_piston\"</code>, <code>\"mold_with_top_piston\"</code> <code>mold_on_scale</code> bool Whether a mold is currently resting on the scale <code>z_height_id</code> string The active named Z-height (e.g., <code>\"mold_transfer_safe\"</code>) <p>A requirement value can be a scalar (must match exactly) or a list (actual value must be one of the listed options).</p> <p>requirements on the target position are a pre-condition, not a post-condition</p> <p><code>requirements</code> is checked before the machine moves. It describes what must already be true in context for the move to be safe. It does not describe what will be true after the move - that is managed by the validated methods in <code>MotionPlatformStateMachine</code>, which update context fields like <code>payload_state</code> and <code>mold_on_scale</code> after a successful operation.</p> <p>Example: <code>dispenser_ready_0</code> requires <code>payload_state: mold_without_top_piston</code>. This prevents the machine from moving to a dispenser if the manipulator is empty or the mold already has its piston.</p>"},{"location":"api/position-config/#z_height_policy","title":"<code>z_height_policy</code>","text":"<p>Controls which Z-heights are valid when arriving at this position.</p> <pre><code>\"z_height_policy\": {\n  \"allowed\": [ \"mold_transfer_safe\" ],\n  \"required\": \"mold_transfer_safe\"\n}\n</code></pre> <ul> <li><code>allowed</code> - a list of z-height names that are acceptable.</li> <li><code>required</code> - if set, the context must be at exactly this z-height. More restrictive than <code>allowed</code>.</li> </ul> <p>If both are omitted or empty, no z-height is permitted for that position, so any moves/actions will always be rejected.</p>"},{"location":"api/position-config/#allows_tool_engagement-and-engagement","title":"<code>allows_tool_engagement</code> and <code>engagement</code>","text":"<p><code>\"allows_tool_engagement\": true</code> marks positions where the tool can be lowered into an engaged state (currently only the scale positions). Attempting to engage the tool at a position where this is <code>false</code> will be rejected.</p> Engagement requirements fallback <p>The <code>engagement</code> block holds a nested <code>requirements</code> dict that is checked specifically during tool engagement and disengagement. If omitted, the position's top-level <code>requirements</code> are used as a fallback.</p> <pre><code>\"engagement\": {\n  \"requirements\": {\n    \"active_tool_id\": \"manipulator\",\n    \"payload_state\": \"mold_without_top_piston\"\n  }\n}\n</code></pre>"},{"location":"api/position-config/#actions","title":"Actions","text":"<p>Actions are tool operations that are validated and executed without changing <code>context.position_id</code>. They are defined in the <code>actions</code> array and referenced by ID from code.</p>"},{"location":"api/position-config/#action-fields","title":"Action fields","text":"<pre><code>{\n  \"id\": \"retrieve_piston\",\n  \"description\": \"...\",\n  \"required_tool_id\": \"manipulator\",\n  \"requires_tool_engaged\": false,\n  \"blocked_when_engaged\": true,\n  \"position_scope\": [ \"dispenser_ready_0\", \"dispenser_ready_1\" ],\n  \"requirements\": {\n    \"active_tool_id\": \"manipulator\",\n    \"payload_state\": \"mold_without_top_piston\"\n  },\n  \"excludes\": {}\n}\n</code></pre> Field Meaning <code>required_tool_id</code> The tool that must be active (<code>\"manipulator\"</code> or omitted for any/none) <code>requires_tool_engaged</code> If <code>true</code>, the FSM must be in the <code>Tool Engaged</code> state <code>blocked_when_engaged</code> If <code>true</code>, the action is rejected while the tool is engaged <code>position_scope</code> List of position IDs (or type names) where this action is permitted. Empty list means any position <code>requirements</code> Same semantics as position requirements - context must match before the action runs <code>excludes</code> Inverse of requirements - context must not match. E.g., <code>\"payload_state\": \"empty\"</code> blocks the action when carrying nothing"},{"location":"api/position-config/#maintaining-state-machine-invariants","title":"Maintaining state machine invariants","text":"<p>When editing this file, keep the following invariants intact so the state machine remains consistent:</p> <ol> <li> <p>Every reachable position must have consistent two-way transitions.    If position A lists B in <code>allowed_destinations</code>, then B must list A in <code>allowed_origins</code>. Asymmetric entries cause confusing one-way failures at runtime.</p> </li> <li> <p><code>requirements</code> must reflect what is physically true at that location.    A position's requirements describe the context that must hold before the machine can safely be there. If you loosen requirements (e.g., removing <code>payload_state</code>), the state machine will permit moves it previously blocked. Make sure this is intentional and safe.</p> </li> <li> <p>Dispenser positions require <code>mold_without_top_piston</code>.    This ensures the machine will never attempt to retrieve a second piston for a mold that already has one.</p> </li> <li> <p><code>scale_active</code> is only entered via <code>place_mold_on_scale</code>, not via a normal move.</p> <p>Never add scale_active to allowed_destinations</p> <p>The state machine sets <code>context.position_id = \"scale_active\"</code> and <code>context.mold_on_scale = True</code> together after the place action succeeds. Adding <code>scale_active</code> to the <code>allowed_destinations</code> of any position would allow the machine to navigate to it with an incorrect procedure, probably causing a collision.</p> </li> <li> <p>Action <code>position_scope</code> lists must stay in sync with <code>DISPENSER_READY</code> and <code>MOLD_READY</code> entries.    When adding a new dispenser or mold slot, update the <code>position_scope</code> list on every action that should be permitted at that position (e.g., <code>retrieve_piston</code>, <code>pick_up_mold</code>, <code>put_down_mold</code>, <code>tamp_mold</code>).</p> </li> </ol>"},{"location":"api/position-config/#adding-labware","title":"Adding labware","text":"New mold slotNew dispenser <ol> <li>Copy an existing <code>MOLD_READY</code> entry and change <code>\"id\"</code> to <code>\"mold_ready_N\"</code> with the next available index.</li> <li>Update <code>coordinates</code> to the calibrated position.</li> <li>Add <code>\"mold_ready_N\"</code> to the <code>position_scope</code> list of every action that should be permitted there (<code>pick_up_mold</code>, <code>put_down_mold</code>, <code>tamp_mold</code>).</li> <li>No changes to <code>allowed_origins</code> or <code>allowed_destinations</code> are needed - all mold slots use the same type reference <code>MOLD_READY</code>.</li> </ol> <ol> <li>Copy an existing <code>DISPENSER_READY</code> entry and change <code>\"id\"</code> to <code>\"dispenser_ready_N\"</code>.</li> <li>Update <code>coordinates</code>.</li> <li>Add <code>\"dispenser_ready_N\"</code> to the <code>position_scope</code> of the <code>retrieve_piston</code> action.</li> <li>As with mold slots, <code>allowed_origins</code> / <code>allowed_destinations</code> use the <code>DISPENSER_READY</code> type reference and require no changes.</li> </ol>"},{"location":"api/position-config/#see-also","title":"See Also","text":"<ul> <li>Configuration Guide - editing workflow for both config files</li> <li>ConfigLoader - <code>system_config.json</code> (tool names referenced by position requirements)</li> <li>MotionPlatformStateMachine - runtime validation and state tracking</li> <li>JubileeManager - high-level interface that loads this file at <code>connect()</code></li> </ul>"},{"location":"api/scale/","title":"Scale","text":"<p>The <code>Scale</code> class provides a serial interface to A&amp;D precision balances (FX/FZ series) for weight measurements in the Jubilee powder dispensing workflow.</p> Operator GuideAPI Reference <p>Prefer JubileeManager</p> <p>Most production code should call <code>JubileeManager.get_weight_stable()</code> or <code>get_weight_unstable()</code> rather than using <code>Scale</code> directly. Use this class when integrating standalone scale tests or debugging serial communication.</p>"},{"location":"api/scale/#overview","title":"Overview","text":"<p>The Scale class:</p> <ul> <li>Connects to the balance via USB serial</li> <li>Reads stable and unstable weight measurements</li> <li>Handles taring and low-level A&amp;D protocol commands</li> <li>Serializes concurrent access with an internal command lock</li> </ul>"},{"location":"api/scale/#usage-examples","title":"Usage Examples","text":""},{"location":"api/scale/#basic-connection-and-reading","title":"Basic Connection and Reading","text":"<pre><code>from src.Scale import Scale\n\nscale = Scale(port=\"/dev/ttyUSB0\")\nscale.connect()\n\nif scale.is_connected:\n    weight = scale.get_weight(stable=True)\n    print(f\"Weight: {weight}g\")\n    scale.disconnect()\n</code></pre>"},{"location":"api/scale/#stable-vs-unstable-readings","title":"Stable vs Unstable Readings","text":"<pre><code>stable_weight = scale.get_weight(stable=True)\nprint(f\"Stable: {stable_weight}g\")\n\nunstable_weight = scale.get_weight(stable=False)\nprint(f\"Unstable: {unstable_weight}g\")\n</code></pre> Mode When to use Stable (<code>stable=True</code>) Recorded measurements and dispensing decisions Unstable (<code>stable=False</code>) Live monitoring, progress display, telemetry"},{"location":"api/scale/#taring-the-scale","title":"Taring the Scale","text":"<pre><code>scale.tare()\nweight = scale.get_weight(stable=True)\nprint(f\"Object weight: {weight}g\")\n</code></pre>"},{"location":"api/scale/#integration-with-jubileemanager","title":"Integration with JubileeManager","text":"<pre><code>from src.JubileeManager import JubileeManager\n\nmanager = JubileeManager(\n    num_piston_dispensers=2,\n    num_pistons_per_dispenser=10,\n)\nmanager.connect(scale_port=\"/dev/ttyUSB0\")\n\nweight = manager.get_weight_stable()\nprint(f\"Weight: {weight}g\")\n\nweight = manager.get_weight_unstable()\nprint(f\"Weight: {weight}g\")\n</code></pre>"},{"location":"api/scale/#weight-monitoring","title":"Weight Monitoring","text":""},{"location":"api/scale/#continuous-monitoring","title":"Continuous Monitoring","text":"<pre><code>import time\n\ndef monitor_weight(scale, duration=10):\n    \"\"\"Monitor weight for specified duration.\"\"\"\n    start_time = time.time()\n    while time.time() - start_time &lt; duration:\n        weight = scale.get_weight(stable=False)\n        print(f\"Weight: {weight:6.2f}g\", end=\"\\r\")\n        time.sleep(0.1)\n    print()\n\nmonitor_weight(scale, duration=30)\n</code></pre>"},{"location":"api/scale/#waiting-for-stability","title":"Waiting for Stability","text":"<pre><code>def wait_for_stable(scale, timeout=30):\n    \"\"\"Wait for weight to stabilize.\"\"\"\n    import time\n\n    start_time = time.time()\n    previous_weight = None\n    stable_count = 0\n    required_stable_readings = 5\n    tolerance = 0.01\n\n    while time.time() - start_time &lt; timeout:\n        weight = scale.get_weight(stable=False)\n\n        if previous_weight is not None:\n            if abs(weight - previous_weight) &lt; tolerance:\n                stable_count += 1\n                if stable_count &gt;= required_stable_readings:\n                    return weight, True\n            else:\n                stable_count = 0\n\n        previous_weight = weight\n        time.sleep(0.2)\n\n    return previous_weight, False\n</code></pre>"},{"location":"api/scale/#configuration","title":"Configuration","text":""},{"location":"api/scale/#software-system_configjson","title":"Software (<code>system_config.json</code>)","text":"<p>The serial port is configured under <code>machine</code>:</p> <pre><code>{\n  \"machine\": {\n    \"scale_port\": \"/dev/ttyUSB0\"\n  }\n}\n</code></pre> <p>Baud rate, timeout, and parity are passed to the <code>Scale</code> constructor (defaults: <code>baudrate=9600</code>, <code>timeout=10</code>). They must match the scale's communication settings.</p>"},{"location":"api/scale/#ad-scale-setup-fx-120i-and-fxfz-series","title":"A&amp;D Scale Setup (FX-120i and FX/FZ Series)","text":"<p>The Jubilee Powder system targets A&amp;D precision balances, particularly the FX-120i. Compatible FX/FZ series scales should work with the settings below.</p>"},{"location":"api/scale/#communication-settings","title":"Communication Settings","text":"Setting Value Notes Baud Rate 9600 (typical) Must match <code>Scale(baudrate=...)</code> Data Bits 8 Standard Parity None Default in <code>Scale.__init__</code> Stop Bits 1 Standard Terminator CRLF Required for A&amp;D protocol"},{"location":"api/scale/#scale-behavior-settings","title":"Scale Behavior Settings","text":"Setting Value Description Stability Bandwidth 1 When weight is considered stable Condition 1 (Medium Response) Response speed vs stability Time/Date Output No Output Keeps data stream clean Zero After Output 0 (Not Used) Disables auto-zero after reading AK Error Code 1 (Output) Enables error-correcting communication"},{"location":"api/scale/#data-format-settings","title":"Data Format Settings","text":"Setting Value Data Format A&amp;D Standard Format Menu configuration sequence <ol> <li>Access the scale configuration menu (see your model manual).</li> <li>Set communication parameters: baud 9600, 8 data bits, no parity, 1 stop bit, CRLF terminator.</li> <li>Set response characteristics: stability bandwidth 1, condition 1 (medium response).</li> <li>Set output: A&amp;D standard format, no time/date, zero-after-output off, AK error code on.</li> <li>Save, exit, and power-cycle the scale.</li> </ol>"},{"location":"api/scale/#verifying-configuration","title":"Verifying Configuration","text":"<pre><code>from src.Scale import Scale\n\nscale = Scale(port=\"/dev/ttyUSB0\", baudrate=9600, timeout=10)\n\ntry:\n    scale.connect()\n    print(\"Scale connected successfully\")\n    weight = scale.get_weight(stable=True)\n    print(f\"Weight reading: {weight}g\")\n    scale.disconnect()\nexcept Exception as e:\n    print(f\"Connection failed - check settings and port: {e}\")\n</code></pre>"},{"location":"api/scale/#finding-the-serial-port","title":"Finding the Serial Port","text":"LinuxWindowsmacOS <pre><code>ls -l /dev/ttyUSB*\nls -l /dev/ttyACM*\ndmesg | grep tty\n</code></pre> <pre><code>Get-WmiObject Win32_SerialPort | Select-Object Name, DeviceID\n</code></pre> <pre><code>ls -l /dev/tty.*\nls -l /dev/cu.*\n</code></pre>"},{"location":"api/scale/#error-handling","title":"Error Handling","text":""},{"location":"api/scale/#connection-failures","title":"Connection Failures","text":"<pre><code>from src.Scale import Scale\n\nscale = Scale(port=\"/dev/ttyUSB0\")\n\ntry:\n    scale.connect()\n    if not scale.is_connected:\n        raise ConnectionError(\"Scale connection failed\")\nexcept Exception as e:\n    print(f\"Error connecting to scale: {e}\")\n</code></pre> <p>Common causes: wrong port, port in use, insufficient permissions, hardware disconnected.</p>"},{"location":"api/scale/#reading-failures","title":"Reading Failures","text":"<pre><code>try:\n    weight = scale.get_weight(stable=True)\n    print(f\"Weight: {weight}g\")\nexcept Exception as e:\n    print(f\"Error reading scale: {e}\")\n</code></pre>"},{"location":"api/scale/#handling-disconnection","title":"Handling Disconnection","text":"<pre><code>def safe_read_weight(scale):\n    if not scale.is_connected:\n        return None\n    try:\n        return scale.get_weight(stable=True)\n    except Exception as e:\n        print(f\"Error reading weight: {e}\")\n        return None\n</code></pre>"},{"location":"api/scale/#advanced-usage","title":"Advanced Usage","text":"Calibration with known weight <pre><code>def calibrate_scale(scale, known_weight):\n    print(\"Remove all items from scale, then press Enter\")\n    input()\n    scale.tare()\n    print(f\"Place {known_weight}g calibration weight on scale, then press Enter\")\n    input()\n    measured = scale.get_weight(stable=True)\n    error = measured - known_weight\n    print(f\"Measured: {measured}g  Expected: {known_weight}g  Error: {error}g\")\n</code></pre> CSV data logging <pre><code>import csv\nimport time\nfrom datetime import datetime\n\ndef log_weight_data(scale, output_file, duration=60, interval=1.0):\n    with open(output_file, \"w\", newline=\"\") as f:\n        writer = csv.writer(f)\n        writer.writerow([\"Timestamp\", \"Weight (g)\", \"Stable\"])\n        start_time = time.time()\n        while time.time() - start_time &lt; duration:\n            timestamp = datetime.now().isoformat()\n            writer.writerow([timestamp, scale.get_weight(stable=False), \"No\"])\n            writer.writerow([timestamp, scale.get_weight(stable=True), \"Yes\"])\n            time.sleep(interval)\n</code></pre>"},{"location":"api/scale/#troubleshooting","title":"Troubleshooting","text":"Symptom Likely cause Fix Scale not responding Wrong port or baud Verify <code>machine.scale_port</code> and scale menu settings Permission denied (Linux) User not in <code>dialout</code> <code>sudo usermod -a -G dialout $USER</code>, re-login Readings always unstable Vibration or bandwidth Increase stability bandwidth; ground the scale Incorrect values Wrong data format Set A&amp;D Standard Format on the scale Timeout errors Busy scale or E02 Wait and retry; check for concurrent access <p>Linux serial permissions</p> <p>Add your user to the <code>dialout</code> group or create a udev rule granting access to the scale's USB vendor ID.</p>"},{"location":"api/scale/#best-practices","title":"Best Practices","text":"<ul> <li>Tare before each measurement when the container changes.</li> <li>Use <code>get_weight(stable=True)</code> for recorded values; <code>stable=False</code> only for live display.</li> <li>Check <code>is_connected</code> before reading.</li> <li>Always call <code>disconnect()</code> in a <code>finally</code> block.</li> </ul>"},{"location":"api/scale/#class-reference","title":"Class Reference","text":""},{"location":"api/scale/#src.Scale.Scale","title":"Scale","text":"<pre><code>Scale(\n    port,\n    baudrate=9600,\n    timeout=10,\n    parity=PARITY_NONE,\n    stopbits=STOPBITS_ONE,\n    bytesize=EIGHTBITS,\n    serial_instance=None,\n)\n</code></pre> <p>Interface to a digital balance connected via USB serial.</p> <p>Communicates using the A&amp;D FX-120i serial protocol. Supports stable and instant weight reads, taring, and direct access to scale commands.</p> ATTRIBUTE DESCRIPTION <code>port</code> <p>Serial device path (for example <code>/dev/ttyUSB0</code>).</p> <p> </p> <code>is_connected</code> <p>Whether the serial port is open and ready.</p> <p> TYPE: <code>bool</code> </p> Note <p>The default scale port is configured in <code>system_config.json</code> under <code>machine.scale_port</code>. Baud rate and timeout are constructor parameters on this class and must match the scale menu settings. Prefer :class:<code>~src.JubileeManager.JubileeManager</code> for production dispensing workflows.</p> Example <p>Stable versus instant reads::</p> <pre><code>stable = scale.get_weight(stable=True)\nlive = scale.get_weight(stable=False)\n</code></pre> <p>Initialize serial connection parameters.</p> PARAMETER DESCRIPTION <code>port</code> <p>Serial port (for example <code>COM1</code> or <code>/dev/ttyUSB0</code>).</p> <p> TYPE: <code>str</code> </p> <code>baudrate</code> <p>Baud rate; must match the scale menu (default 9600).</p> <p> TYPE: <code>int</code> DEFAULT: <code>9600</code> </p> <code>timeout</code> <p>Read timeout in seconds (default 10).</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>parity</code> <p>Parity setting (default <code>PARITY_NONE</code>).</p> <p> TYPE: <code>str</code> DEFAULT: <code>PARITY_NONE</code> </p> <code>stopbits</code> <p>Stop bits setting (default <code>STOPBITS_ONE</code>).</p> <p> TYPE: <code>float</code> DEFAULT: <code>STOPBITS_ONE</code> </p> <code>bytesize</code> <p>Byte size setting (default <code>EIGHTBITS</code>).</p> <p> TYPE: <code>int</code> DEFAULT: <code>EIGHTBITS</code> </p> <code>serial_instance</code> <p>Optional serial-like test double. When provided, :meth:<code>connect</code> uses it instead of opening a real port.</p> <p> TYPE: <code>Serial | None</code> DEFAULT: <code>None</code> </p> Note <p>Production port values come from <code>machine.scale_port</code> in <code>system_config.json</code>.</p>"},{"location":"api/scale/#src.Scale.Scale-attributes","title":"Attributes","text":""},{"location":"api/scale/#src.Scale.Scale.HANDLED_RETRY_ERRORS","title":"HANDLED_RETRY_ERRORS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>HANDLED_RETRY_ERRORS = (\n    ScaleError.E02,\n    ScaleError.E03,\n    ScaleError.E11,\n)\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.SPECIFIC_ERROR_RETRY_CONFIG","title":"SPECIFIC_ERROR_RETRY_CONFIG  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SPECIFIC_ERROR_RETRY_CONFIG = {\n    ScaleError.E02: [(15, 3)],\n    ScaleError.E03: [(15, 3)],\n    ScaleError.E11: [(15, 3)],\n}\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.SPECIFIC_ERROR_DESCRIPTIONS","title":"SPECIFIC_ERROR_DESCRIPTIONS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SPECIFIC_ERROR_DESCRIPTIONS = {\n    ScaleError.E02: \"Not ready\",\n    ScaleError.E03: \"Timeout error\",\n    ScaleError.E11: \"Stability error\",\n}\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.NO_ACK_RESPONSE_WAIT","title":"NO_ACK_RESPONSE_WAIT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NO_ACK_RESPONSE_WAIT = 2.0\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.port","title":"port  <code>instance-attribute</code>","text":"<pre><code>port = port\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.baudrate","title":"baudrate  <code>instance-attribute</code>","text":"<pre><code>baudrate = baudrate\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.parity","title":"parity  <code>instance-attribute</code>","text":"<pre><code>parity = parity\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.stopbits","title":"stopbits  <code>instance-attribute</code>","text":"<pre><code>stopbits = stopbits\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.bytesize","title":"bytesize  <code>instance-attribute</code>","text":"<pre><code>bytesize = bytesize\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.timeout","title":"timeout  <code>instance-attribute</code>","text":"<pre><code>timeout = timeout\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.serial","title":"serial  <code>instance-attribute</code>","text":"<pre><code>serial = None\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._is_connected","title":"_is_connected  <code>instance-attribute</code>","text":"<pre><code>_is_connected = False\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._serial_instance","title":"_serial_instance  <code>instance-attribute</code>","text":"<pre><code>_serial_instance = serial_instance\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._cmd_condition","title":"_cmd_condition  <code>instance-attribute</code>","text":"<pre><code>_cmd_condition = threading.Condition()\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._busy","title":"_busy  <code>instance-attribute</code>","text":"<pre><code>_busy = False\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._waiting_count","title":"_waiting_count  <code>instance-attribute</code>","text":"<pre><code>_waiting_count = 0\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._last_weight_time","title":"_last_weight_time  <code>instance-attribute</code>","text":"<pre><code>_last_weight_time = None\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale._last_weight_message","title":"_last_weight_message  <code>instance-attribute</code>","text":"<pre><code>_last_weight_message = None\n</code></pre>"},{"location":"api/scale/#src.Scale.Scale.is_connected","title":"is_connected  <code>property</code>","text":"<pre><code>is_connected\n</code></pre> <p>Whether the scale serial port is open and connected.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if connected, False otherwise.</p>"},{"location":"api/scale/#src.Scale.Scale-functions","title":"Methods:","text":""},{"location":"api/scale/#src.Scale.Scale.connect","title":"connect","text":"<pre><code>connect()\n</code></pre> <p>Establish a serial connection to the scale.</p> <p>If a <code>serial_instance</code> was supplied at construction time, that object is used directly and no real port is opened. Otherwise a <code>serial.Serial</code> connection is created from the stored parameters.</p> RAISES DESCRIPTION <code>ScaleException</code> <p>If the connection or initial display-on command fails.</p>"},{"location":"api/scale/#src.Scale.Scale.disconnect","title":"disconnect","text":"<pre><code>disconnect()\n</code></pre> <p>Close the serial connection and mark the scale as disconnected.</p>"},{"location":"api/scale/#src.Scale.Scale._acquire_busy","title":"_acquire_busy","text":"<pre><code>_acquire_busy()\n</code></pre> <p>Wait until idle, then mark the scale busy for the calling thread.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if the caller blocked on another in-flight command; False if</p> <code>bool</code> <p>the scale was idle immediately.</p>"},{"location":"api/scale/#src.Scale.Scale._acquire_busy_for_telemetry","title":"_acquire_busy_for_telemetry","text":"<pre><code>_acquire_busy_for_telemetry()\n</code></pre> <p>Acquire the scale for telemetry reads with lower priority than jobs.</p> <p>Yields to other threads queued on the command monitor so dispensing commands are not starved by the 4 Hz telemetry loop.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if the caller blocked at any point; False if idle on entry.</p> Note <p>Behavior is unspecified when multiple telemetry threads call this concurrently; livelock may result.</p>"},{"location":"api/scale/#src.Scale.Scale._release_busy","title":"_release_busy","text":"<pre><code>_release_busy()\n</code></pre> <p>Clear the busy flag and wake one waiting thread.</p> <p>Includes a 0.5 s inter-command sleep to prevent EC,02 \"not ready\" errors when the scale needs a moment after executing a command before it can accept the next one.</p>"},{"location":"api/scale/#src.Scale.Scale._send_command_and_wait","title":"_send_command_and_wait","text":"<pre><code>_send_command_and_wait(\n    cmd, expect_ack, is_dual_ack, no_ack_wait=None\n)\n</code></pre> <p>Send a command and wait for either ACK(s) or a single non-ACK response.</p> PARAMETER DESCRIPTION <code>cmd</code> <p>Command string to send</p> <p> TYPE: <code>str</code> </p> <code>expect_ack</code> <p>Whether command should return ACK(s)</p> <p> TYPE: <code>bool</code> </p> <code>is_dual_ack</code> <p>Whether two ACKs are expected</p> <p> TYPE: <code>bool</code> </p> <code>no_ack_wait</code> <p>Optional wait time before reading non-ACK response</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>(success, payload, error) where payload is bytes for ACK paths and</p> <code>bytes | str</code> <p>decoded string for non-ACK paths.</p>"},{"location":"api/scale/#src.Scale.Scale._handle_specific_error","title":"_handle_specific_error","text":"<pre><code>_handle_specific_error(\n    error, cmd, expect_ack=True, is_dual_ack=False\n)\n</code></pre> <p>Handle specific error codes with unified retry logic. All retry logic is contained within this function.</p> PARAMETER DESCRIPTION <code>error</code> <p>The ScaleError that was received</p> <p> TYPE: <code>ScaleError</code> </p> <code>cmd</code> <p>The command that was being sent</p> <p> TYPE: <code>str</code> </p> <code>expect_ack</code> <p>Whether the command expects an ACK</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>is_dual_ack</code> <p>Whether the command expects dual ACKs</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True if command succeeded after error handling, False otherwise</p> RAISES DESCRIPTION <code>ScaleException</code> <p>If error persists after all retries</p>"},{"location":"api/scale/#src.Scale.Scale._wait_for_ack","title":"_wait_for_ack","text":"<pre><code>_wait_for_ack(timeout=ACK_TIMEOUT, initial_buffer=b'')\n</code></pre> <p>Wait for an ACK response from the scale.</p> PARAMETER DESCRIPTION <code>timeout</code> <p>Timeout in seconds</p> <p> TYPE: <code>float</code> DEFAULT: <code>ACK_TIMEOUT</code> </p> <code>initial_buffer</code> <p>Bytes already received from a previous read that should be searched before polling the serial port again.  This handles the case where two ACKs (dual-ACK command) arrive in the same OS read and the caller needs to forward the tail of the first response into the next call.</p> <p> TYPE: <code>bytes</code> DEFAULT: <code>b''</code> </p> RETURNS DESCRIPTION <code>tuple[bool, bytes, ScaleError | str | None]</code> <p>Tuple of (success: bool, received_data: bytes, error: ScaleError or None)</p> RAISES DESCRIPTION <code>ScaleException</code> <p>If error response received instead of ACK (for non-handled errors)</p>"},{"location":"api/scale/#src.Scale.Scale._send_command","title":"_send_command","text":"<pre><code>_send_command(cmd, expect_data=False)\n</code></pre> <p>Acquire exclusive access to the scale, execute the command, update the weight cache when appropriate, and release access.</p> PARAMETER DESCRIPTION <code>cmd</code> <p>Command string to send</p> <p> TYPE: <code>str</code> </p> <code>expect_data</code> <p>If True, expects a data response after ACK</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Response string from the scale</p> RAISES DESCRIPTION <code>ScaleException</code> <p>If command fails after retries or ACK timeout</p>"},{"location":"api/scale/#src.Scale.Scale._execute_command","title":"_execute_command","text":"<pre><code>_execute_command(cmd, expect_data=False)\n</code></pre> <p>Send a command to the scale with retry logic and ACK handling. Must only be called while the busy flag is held by the calling thread (i.e. from _send_command or get_weight_for_telemetry).</p> PARAMETER DESCRIPTION <code>cmd</code> <p>Command string to send</p> <p> TYPE: <code>str</code> </p> <code>expect_data</code> <p>If True, expects a data response after ACK</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Response string from the scale</p> RAISES DESCRIPTION <code>ScaleException</code> <p>If command fails after retries or ACK timeout</p>"},{"location":"api/scale/#src.Scale.Scale.cancel","title":"cancel","text":"<pre><code>cancel()\n</code></pre> <p>Cancel an in-progress <code>S</code> or <code>SIR</code> weight stream command.</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.query_weight","title":"query_weight","text":"<pre><code>query_weight()\n</code></pre> <p>Request the current weight immediately (<code>Q</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.request_stable_weight","title":"request_stable_weight","text":"<pre><code>request_stable_weight()\n</code></pre> <p>Request weight data when stabilized (<code>S</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.request_instant_weight","title":"request_instant_weight","text":"<pre><code>request_instant_weight()\n</code></pre> <p>Request the current weight immediately (<code>SI</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.request_continuous_weight","title":"request_continuous_weight","text":"<pre><code>request_continuous_weight()\n</code></pre> <p>Request continuous weight streaming (<code>SIR</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.request_stable_weight_escp","title":"request_stable_weight_escp","text":"<pre><code>request_stable_weight_escp()\n</code></pre> <p>Request stabilized weight via <code>ESC+P</code> command.</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.calibrate","title":"calibrate","text":"<pre><code>calibrate()\n</code></pre> <p>Perform internal calibration (<code>CAL</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.calibrate_external","title":"calibrate_external","text":"<pre><code>calibrate_external()\n</code></pre> <p>Calibrate using an external reference weight (<code>EXC</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.display_off","title":"display_off","text":"<pre><code>display_off()\n</code></pre> <p>Turn the scale display off (<code>OFF</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.display_on","title":"display_on","text":"<pre><code>display_on()\n</code></pre> <p>Turn the scale display on (<code>ON</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.power_on","title":"power_on","text":"<pre><code>power_on()\n</code></pre> <p>Alias for :meth:<code>display_on</code>.</p>"},{"location":"api/scale/#src.Scale.Scale.power_off","title":"power_off","text":"<pre><code>power_off()\n</code></pre> <p>Alias for :meth:<code>display_off</code>.</p>"},{"location":"api/scale/#src.Scale.Scale.print_weight","title":"print_weight","text":"<pre><code>print_weight()\n</code></pre> <p>Print the current weight to the scale output (<code>PRT</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw weight data string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.re_zero","title":"re_zero","text":"<pre><code>re_zero()\n</code></pre> <p>Re-zero the scale (<code>R</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.zero","title":"zero","text":"<pre><code>zero()\n</code></pre> <p>Zero the scale with an empty pan (<code>Z</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.sample","title":"sample","text":"<pre><code>sample()\n</code></pre> <p>Trigger a sample capture (<code>SMP</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.tare","title":"tare","text":"<pre><code>tare()\n</code></pre> <p>Tare the scale (<code>T</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.mode","title":"mode","text":"<pre><code>mode()\n</code></pre> <p>Change the weighing mode (<code>U</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.get_id","title":"get_id","text":"<pre><code>get_id()\n</code></pre> <p>Request the scale ID number (<code>?ID</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.get_serial_number","title":"get_serial_number","text":"<pre><code>get_serial_number()\n</code></pre> <p>Request the scale serial number (<code>?SN</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.get_model","title":"get_model","text":"<pre><code>get_model()\n</code></pre> <p>Request the scale model name (<code>?TN</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.get_tare_weight","title":"get_tare_weight","text":"<pre><code>get_tare_weight()\n</code></pre> <p>Request the current tare weight (<code>?PT</code> command).</p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.set_tare_weight","title":"set_tare_weight","text":"<pre><code>set_tare_weight(value, unit='g')\n</code></pre> <p>Set the tare weight (<code>PT</code> command).</p> PARAMETER DESCRIPTION <code>value</code> <p>Tare weight value.</p> <p> TYPE: <code>float</code> </p> <code>unit</code> <p>Weighing unit suffix (default <code>\"g\"</code>).</p> <p> TYPE: <code>str</code> DEFAULT: <code>'g'</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Raw response string from the scale.</p>"},{"location":"api/scale/#src.Scale.Scale.get_weight","title":"get_weight","text":"<pre><code>get_weight(stable=True)\n</code></pre> <p>Get the current weight in grams.</p> PARAMETER DESCRIPTION <code>stable</code> <p>If True, wait for a stable reading (<code>S</code> command). If False, return the instant reading (<code>SI</code> command).</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> RETURNS DESCRIPTION <code>float</code> <p>Parsed weight in grams.</p> RAISES DESCRIPTION <code>ScaleException</code> <p>On protocol violations, overload, or bad units.</p>"},{"location":"api/scale/#src.Scale.Scale.get_weight_for_telemetry","title":"get_weight_for_telemetry","text":"<pre><code>get_weight_for_telemetry()\n</code></pre> <p>Get an unstable weight reading optimized for background telemetry.</p> <p>Differs from :meth:<code>get_weight</code> with <code>stable=False</code> in two ways:</p> <p>Priority yielding: if other threads are queued for the scale monitor, this thread yields until the queue is empty, giving non-telemetry commands priority.</p> <p>Cache shortcut: when this call had to block and the most recent cached weight is less than 500 ms old, the cached value is returned without sending a new command.</p> RETURNS DESCRIPTION <code>float</code> <p>Parsed weight in grams.</p> Note <p>Behavior is unspecified if multiple telemetry threads call this method concurrently; livelock may result.</p>"},{"location":"api/scale/#src.Scale.Scale._decode_serial_line","title":"_decode_serial_line  <code>staticmethod</code>","text":"<pre><code>_decode_serial_line(raw)\n</code></pre> <p>Decode one scale serial line to a stripped ASCII string.</p>"},{"location":"api/scale/#src.Scale.Scale._is_weight_message","title":"_is_weight_message  <code>staticmethod</code>","text":"<pre><code>_is_weight_message(message)\n</code></pre> <p>Return True when <code>message</code> looks like a weight data line.</p> <p>Matches <code>ST,</code> or <code>US,</code> headers accepted by :meth:<code>_parse_weight</code> when <code>expect_stable=False</code>; does not validate unit or overload.</p>"},{"location":"api/scale/#src.Scale.Scale._store_weight_message","title":"_store_weight_message","text":"<pre><code>_store_weight_message(message)\n</code></pre> <p>Cache the latest raw weight response string and timestamp.</p>"},{"location":"api/scale/#src.Scale.Scale._parse_weight","title":"_parse_weight","text":"<pre><code>_parse_weight(data, expect_stable=True)\n</code></pre> <p>Parse an A&amp;D weight data string into grams.</p> <p>Validates header, sign, numeric value, unit, and overload state.</p> PARAMETER DESCRIPTION <code>data</code> <p>Raw data string from the scale.</p> <p> TYPE: <code>str</code> </p> <code>expect_stable</code> <p>If True, require <code>ST</code> header; otherwise allow <code>ST</code> or <code>US</code>.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> RETURNS DESCRIPTION <code>float</code> <p>Parsed weight as a float in grams.</p> RAISES DESCRIPTION <code>ScaleException</code> <p>On malformed data or protocol violations.</p> <code>ScaleOverloadException</code> <p>When the scale reports overload.</p> <code>ScaleHeaderException</code> <p>When the header does not match expectations.</p> <code>ScaleUnitException</code> <p>When the unit is not grams.</p> <code>ScaleMaxWeightException</code> <p>When weight exceeds <code>MAX_WEIGHT</code>.</p>"},{"location":"api/scale/#scaleerror-codes","title":"ScaleError Codes","text":"<p>The <code>ScaleError</code> enum maps A&amp;D <code>EC,&lt;code&gt;</code> responses. See the generated reference above for <code>ScaleError</code> members and descriptions.</p>"},{"location":"api/scale/#see-also","title":"See Also","text":"<ul> <li>JubileeManager - High-level scale operations</li> <li>Configuration Guide - Setting up scale port</li> <li>Results Interpretation - Analyzing weight data</li> </ul>"},{"location":"api/gui/jubilee-gui/","title":"Web Frontend Reference","text":"<p>The Jubilee Powder web frontend is a browser-based interface for powder dispensing and hardness testing operations. It replaces the former Kivy GUI with a React single-page application backed by a FastAPI server.</p>"},{"location":"api/gui/jubilee-gui/#overview","title":"Overview","text":"<p>The frontend provides:</p> <ul> <li>Six navigation screens covering home/dashboard, dispensing, hardness testing, data   browser, manual control, and settings</li> <li>A 4 Hz WebSocket telemetry feed keeping every screen up to date without polling</li> <li>REST API endpoints for discrete commands (connect, start job, cancel/abort)</li> <li>Mock hardware mode controlled by <code>server.mock_hardware</code> in <code>system_config.json</code> or <code>JUBILEE_MOCK_HARDWARE=1</code></li> </ul>"},{"location":"api/gui/jubilee-gui/#progressive-disclosure","title":"Progressive Disclosure","text":"Operator FocusDeveloper Focus <ul> <li>Use Using the Automation UI for end-to-end workflows.</li> <li>Focus on connection setup, job start/stop, and run monitoring.</li> <li>Treat this page as a reference when you need exact API behavior.</li> </ul> <ul> <li>Use this page as the canonical reference for REST, WebSocket, and backend/frontend module boundaries.</li> <li>Start in <code>frontend/server.py</code> and <code>frontend/src/store/jubileeStore.js</code> to trace request and telemetry flows.</li> <li>Use Jubilee Store Reference for client-side state details.</li> </ul>"},{"location":"api/gui/jubilee-gui/#technology-stack","title":"Technology Stack","text":"Layer Technology Frontend React 19, Vite 8, Tailwind CSS 4 State Zustand 5 Routing React Router v7 Backend FastAPI (Python 3.12+), uvicorn Transport REST HTTP + WebSocket Storage JSON job log files"},{"location":"api/gui/jubilee-gui/#architecture","title":"Architecture","text":"<p>The frontend follows an MVVM pattern split across the browser and server:</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Browser                                         \u2502\n\u2502                                                  \u2502\n\u2502  View      \u2500\u2500 React screens + components         \u2502\n\u2502  ViewModel \u2500\u2500 Zustand store (jubileeStore.js)    \u2502\n\u2502                 \u2502 REST (HTTP)    \u2502 WebSocket /ws  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                  \u2502                \u2502\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  FastAPI Server (server.py)                      \u2502\n\u2502  Model     \u2500\u2500 HardwareManager / MockHardwareManager\u2502\n\u2502                 \u2502                                \u2502\n\u2502  JubileeManager \u2500\u2500\u2500 Scale, Dispensers, Jubilee   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> MVVM Layer Component Responsibility View React screens and components Render state, capture user input ViewModel Zustand store (<code>jubileeStore.js</code>) Derived state, REST/WebSocket actions Model FastAPI + <code>HardwareManager</code> Hardware state, physical operations"},{"location":"api/gui/jubilee-gui/#startup","title":"Startup","text":"<p>Use Building and Running the Web UI as the canonical startup guide for dev, production, and kiosk modes.</p>"},{"location":"api/gui/jubilee-gui/#rest-api-reference","title":"REST API Reference","text":"<p>All endpoints are prefixed with <code>/api</code>.</p> <p>Async lifecycle endpoints</p> <p><code>POST /api/hardware/connect</code> and <code>POST /api/job/start</code> return HTTP 202 immediately. Monitor the WebSocket <code>state</code> and <code>job</code> fields for progress; do not poll REST for connection or job completion.</p>"},{"location":"api/gui/jubilee-gui/#configuration-and-status","title":"Configuration and Status","text":"Method Path Description <code>GET</code> <code>/api/config</code> Machine settings from <code>system_config.json</code> <code>GET</code> <code>/api/status</code> Full machine state snapshot <p><code>GET /api/config</code> response (abbreviated):</p> <pre><code>{\n  \"duet_ip\": \"192.168.1.2\",\n  \"scale_port\": \"/dev/ttyUSB0\",\n  \"num_dispensers\": 2,\n  \"pistons_per_dispenser\": 10,\n  \"hardness_tray\": { \"rows\": 2, \"cols\": 6, \"tray_count\": 2 },\n  \"google_drive_enabled\": false,\n  \"mock_hardware\": false\n}\n</code></pre> <p><code>GET /api/status</code> response (abbreviated):</p> <pre><code>{\n  \"connected\":   true,\n  \"state\":       \"idle\",\n  \"jubilee_ip\":  \"jubilee.local\",\n  \"job\":         { ... },\n  \"dispensers\":  [ { \"index\": 0, \"pistons_remaining\": 8 } ],\n  \"clients\":     1\n}\n</code></pre>"},{"location":"api/gui/jubilee-gui/#hardware-lifecycle","title":"Hardware Lifecycle","text":"Method Path Status Description <code>POST</code> <code>/api/hardware/connect</code> 202 Begin hardware connection asynchronously <code>POST</code> <code>/api/hardware/disconnect</code> 200 Stop any running job and disconnect <p><code>POST /api/hardware/connect</code> request body:</p> <pre><code>{\n  \"num_dispensers\":        2,\n  \"pistons_per_dispenser\": 10,\n  \"machine_address\":       \"jubilee.local\",\n  \"scale_port\":            \"/dev/ttyUSB0\"\n}\n</code></pre> <p>The server returns HTTP 202 immediately. Monitor the WebSocket <code>state</code> field for connection progress:</p> <pre><code>DISCONNECTED \u2192 HOMING \u2192 IDLE    (success)\nDISCONNECTED \u2192 HOMING \u2192 ERROR   (failure \u2014 inspect job.error for reason)\n</code></pre>"},{"location":"api/gui/jubilee-gui/#jobs","title":"Jobs","text":"Method Path Status Description <code>POST</code> <code>/api/job/start</code> 202 Enqueue a dispensing or hardness job <code>POST</code> <code>/api/job/stop</code> 200 Graceful stop after the current well/sample <code>POST</code> <code>/api/job/cancel</code> 200 Cancel after current mold/sample completes <code>POST</code> <code>/api/job/abort</code> 200 Emergency stop (M112), enters ERROR state <code>POST</code> <code>/api/job/clear_jam</code> 200 Resume dispensing after a powder jam pause <code>GET</code> <code>/api/job/log</code> 200 Most recent job log (in-memory or from file) <p><code>POST /api/job/start</code> \u2014 dispensing job:</p> <pre><code>{\n  \"job_type\": \"dispensing\",\n  \"wells\": [\n    { \"well_id\": \"0\", \"target_weight\": 50.0 },\n    { \"well_id\": \"3\", \"target_weight\": 45.0 }\n  ]\n}\n</code></pre> <p><code>POST /api/job/start</code> \u2014 hardness job:</p> <pre><code>{\n  \"job_type\": \"hardness\",\n  \"samples\": [\n    { \"tray_index\": 0, \"sample_index\": 0, \"mode\": \"shore_a\" },\n    { \"tray_index\": 0, \"sample_index\": 1, \"mode\": \"shore_d\" }\n  ]\n}\n</code></pre> <p>Valid hardness modes: <code>shore_a</code>, <code>shore_a_d</code>, <code>shore_d</code>.</p> <p>The server returns HTTP 202; track progress via the WebSocket <code>job</code> field.</p> Stop vs Cancel vs Abort Action Behaviour <code>stop</code> Signal job to exit at the next well boundary. Machine stays at current well, returns to IDLE. <code>cancel</code> Cancellation takes effect after the current mold or sample completes. <code>abort</code> Immediate M112 emergency stop. Machine enters ERROR state and must be reconnected before a new job. <code>clear_jam</code> Clears a dispensing jam pause so the job loop can resume. Returns 400 when no jam is active."},{"location":"api/gui/jubilee-gui/#dispensers","title":"Dispensers","text":"Method Path Description <code>GET</code> <code>/api/dispensers</code> List all dispenser statuses <code>PUT</code> <code>/api/dispensers/{index}</code> Update remaining piston count <p><code>PUT</code> body: <code>{ \"num_pistons\": 10 }</code></p>"},{"location":"api/gui/jubilee-gui/#job-log-files","title":"Job Log Files","text":"Method Path Description <code>GET</code> <code>/api/files</code> List all job log files (newest-first) <code>GET</code> <code>/api/files/{filename}</code> Return full JSON content of one log file <p>File names follow the pattern <code>{id}_{date}_{job_type}_{n}.json</code>, e.g. <code>0012_2026-04-12_dispensing_1.json</code>. Files are stored in the path configured by <code>paths.job_files_dir</code> in <code>system_config.json</code>.</p>"},{"location":"api/gui/jubilee-gui/#google-drive-backup","title":"Google Drive Backup","text":"Method Path Description <code>GET</code> <code>/api/drive/status</code> Backup enablement and upload status <p>Only active when <code>google_drive.enabled</code> is <code>true</code> in <code>system_config.json</code>.</p>"},{"location":"api/gui/jubilee-gui/#websocket-protocol","title":"WebSocket Protocol","text":""},{"location":"api/gui/jubilee-gui/#endpoint","title":"Endpoint","text":"<pre><code>ws://&lt;host&gt;/ws\n</code></pre>"},{"location":"api/gui/jubilee-gui/#telemetry-frame-4-hz","title":"Telemetry Frame (4 Hz)","text":"<p>The server pushes a JSON frame to every connected browser four times per second. Each frame is a complete snapshot \u2014 the client replaces its state wholesale so no stale fields from a previous server restart are retained.</p> <pre><code>{\n  \"weight\":     50.123,\n  \"state\":      \"idle\",\n  \"connected\":  true,\n  \"jubilee_ip\": \"jubilee.local\",\n  \"job\": {\n    \"running\":      false,\n    \"job_type\":     \"dispensing\",\n    \"completed\":    6,\n    \"total\":        6,\n    \"current_item\": null,\n    \"error\":        null,\n    \"started_at\":   \"2026-04-16T14:30:00Z\",\n    \"items\":        [ ... ]\n  },\n  \"dispensers\": [\n    { \"index\": 0, \"pistons_remaining\": 4 },\n    { \"index\": 1, \"pistons_remaining\": 10 }\n  ],\n  \"clients\": 1\n}\n</code></pre> <p><code>weight</code> is <code>null</code> when hardware is disconnected. The frame is only sent when at least one browser is connected; the server skips the broadcast when <code>clients == 0</code>.</p>"},{"location":"api/gui/jubilee-gui/#machine-states","title":"Machine States","text":"State Meaning <code>disconnected</code> No hardware connection <code>homing</code> Connection sequence or homing in progress <code>idle</code> Connected and ready for a job <code>running</code> Job is executing <code>error</code> Emergency stop or failure \u2014 reconnect required"},{"location":"api/gui/jubilee-gui/#backend-module-reference","title":"Backend Module Reference","text":"<p><code>server.py</code> is the FastAPI entry point (REST, WebSocket, telemetry loop, job log I/O). <code>hardware_manager.py</code> exposes <code>MockHardwareManager</code> and <code>HardwareManager</code> with identical public APIs. <code>models.py</code> holds shared enums and job-progress state.</p>"},{"location":"api/gui/jubilee-gui/#serverpy","title":"<code>server.py</code>","text":""},{"location":"api/gui/jubilee-gui/#server.ConnectionManager","title":"ConnectionManager","text":"<pre><code>ConnectionManager()\n</code></pre> <p>Tracks every live browser WebSocket client.</p> <p>Maintains the list of active connections and provides broadcast helpers. Dead connections (clients that have closed their tab) are detected during broadcast and removed automatically.</p> ATTRIBUTE DESCRIPTION <code>active</code> <p>Currently connected :class:<code>fastapi.WebSocket</code> instances.</p> <p> TYPE: <code>list[WebSocket]</code> </p>"},{"location":"api/gui/jubilee-gui/#server.ConnectionManager-attributes","title":"Attributes","text":""},{"location":"api/gui/jubilee-gui/#server.ConnectionManager.active","title":"active  <code>instance-attribute</code>","text":"<pre><code>active = []\n</code></pre>"},{"location":"api/gui/jubilee-gui/#server.ConnectionManager.client_count","title":"client_count  <code>property</code>","text":"<pre><code>client_count\n</code></pre> <p>Number of currently active WebSocket clients.</p>"},{"location":"api/gui/jubilee-gui/#server.ConnectionManager-functions","title":"Methods:","text":""},{"location":"api/gui/jubilee-gui/#server.ConnectionManager.connect","title":"connect  <code>async</code>","text":"<pre><code>connect(ws)\n</code></pre> <p>Accept a new WebSocket connection and register it.</p> PARAMETER DESCRIPTION <code>ws</code> <p>The WebSocket instance from FastAPI.</p> <p> TYPE: <code>WebSocket</code> </p>"},{"location":"api/gui/jubilee-gui/#server.ConnectionManager.disconnect","title":"disconnect","text":"<pre><code>disconnect(ws)\n</code></pre> <p>Remove a WebSocket from the active list.</p> PARAMETER DESCRIPTION <code>ws</code> <p>The WebSocket instance to remove. No-op if not registered.</p> <p> TYPE: <code>WebSocket</code> </p>"},{"location":"api/gui/jubilee-gui/#server.ConnectionManager.broadcast","title":"broadcast  <code>async</code>","text":"<pre><code>broadcast(data)\n</code></pre> <p>Send a JSON payload to every active client.</p> <p>Any client whose send raises an exception is assumed to have disconnected and is removed from <code>self.active</code>.</p> PARAMETER DESCRIPTION <code>data</code> <p>JSON-serialisable dict to broadcast.</p> <p> TYPE: <code>dict</code> </p>"},{"location":"api/gui/jubilee-gui/#server.telemetry_loop","title":"telemetry_loop  <code>async</code>","text":"<pre><code>telemetry_loop()\n</code></pre> <p>Broadcast a unified telemetry frame to every connected browser at 4 Hz.</p> <p>Runs indefinitely as an <code>asyncio.Task</code> created in the app lifespan. Weight is only queried when hardware is connected. The broadcast is skipped entirely when no browser tabs are open (<code>ws_mgr.client_count == 0</code>).</p> Note <p>Frame fields mirror :meth:<code>models.JobProgress.to_dict</code> plus live weight, machine state, and dispenser snapshots.</p>"},{"location":"api/gui/jubilee-gui/#hardware_managerpy","title":"<code>hardware_manager.py</code>","text":""},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager","title":"MockHardwareManager","text":"<pre><code>MockHardwareManager()\n</code></pre> <p>Simulated hardware manager for UI development without physical hardware.</p> <p>Scale readings use a sine-wave drift plus Gaussian noise to approximate the output of the A&amp;D FX-120i scale. Job loops advance :class:<code>~models.JobProgress</code> via <code>asyncio.sleep()</code> with per-well delays that match real hardware timing, so the WebSocket telemetry looks realistic during development.</p> Note <p>Selected by <code>server._mock_hardware_enabled()</code> when <code>server.mock_hardware</code> is true or <code>JUBILEE_MOCK_HARDWARE=1</code>.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager-functions","title":"Methods:","text":""},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.connect","title":"connect  <code>async</code>","text":"<pre><code>connect(config)\n</code></pre> <p>Simulate a hardware connection and homing sequence.</p> <p>Transitions <code>state</code> from <code>DISCONNECTED</code> \u2192 <code>HOMING</code> \u2192 <code>IDLE</code> with a 2-second sleep to mimic the real homing sequence.</p> PARAMETER DESCRIPTION <code>config</code> <p>Hardware configuration (dispensers, pistons, addresses).</p> <p> TYPE: <code>HardwareConfig</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.disconnect","title":"disconnect  <code>async</code>","text":"<pre><code>disconnect()\n</code></pre> <p>Clear dispenser state and set machine state to DISCONNECTED.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.get_weight_unstable","title":"get_weight_unstable  <code>async</code>","text":"<pre><code>get_weight_unstable()\n</code></pre> <p>Return a simulated live scale reading with drift and noise.</p> RETURNS DESCRIPTION <code>float</code> <p>Simulated weight in grams (sine drift + Gaussian noise),</p> <p> TYPE: <code>float</code> </p> <code>float</code> <p>always &gt;= 0.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.get_weight_stable","title":"get_weight_stable  <code>async</code>","text":"<pre><code>get_weight_stable()\n</code></pre> <p>Return a simulated stable scale reading with minimal noise.</p> RETURNS DESCRIPTION <code>float</code> <p>Simulated stable weight in grams (Gaussian noise only).</p> <p> TYPE: <code>float</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.get_weight_telemetry","title":"get_weight_telemetry  <code>async</code>","text":"<pre><code>get_weight_telemetry()\n</code></pre> <p>Return weight for telemetry frames.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.get_dispensers","title":"get_dispensers","text":"<pre><code>get_dispensers()\n</code></pre> <p>Return a snapshot of all dispenser statuses.</p> RETURNS DESCRIPTION <code>list[DispenserStatus]</code> <p>list[DispenserStatus]: Copy of the internal dispenser list.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.update_dispenser_pistons","title":"update_dispenser_pistons","text":"<pre><code>update_dispenser_pistons(index, num_pistons)\n</code></pre> <p>Update the remaining piston count for a single dispenser.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based dispenser index.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>New piston count.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success, <code>False</code> if the index is out of range.</p> <p> TYPE: <code>bool</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.run_dispensing_job","title":"run_dispensing_job  <code>async</code>","text":"<pre><code>run_dispensing_job(wells, progress, job_log=None)\n</code></pre> <p>Simulate a powder dispensing job.</p> <p>Iterates over <code>wells</code> sequentially, sleeping 2 s per well to mimic real hardware timing. Updates <code>progress</code> in-place and optionally records simulated actual weights in <code>job_log</code>.</p> PARAMETER DESCRIPTION <code>wells</code> <p>Ordered list of well dicts with <code>well_id</code> and <code>target_weight</code> keys.</p> <p> TYPE: <code>list[dict]</code> </p> <code>progress</code> <p>Shared <code>JobProgress</code> instance updated as wells complete.</p> <p> TYPE: <code>JobProgress</code> </p> <code>job_log</code> <p>Optional <code>JobLog</code> instance for recording results.</p> <p> TYPE: <code>JobLog | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.run_hardness_job","title":"run_hardness_job  <code>async</code>","text":"<pre><code>run_hardness_job(samples, progress, job_log=None)\n</code></pre> <p>Simulate a hardness testing job.</p> <p>Iterates over <code>samples</code> sequentially, sleeping 1.5 s per sample. Simulated hardness results are drawn from a uniform distribution.</p> PARAMETER DESCRIPTION <code>samples</code> <p>Ordered list of sample dicts with <code>tray_index</code>, <code>sample_index</code>, and <code>mode</code> keys.</p> <p> TYPE: <code>list[dict]</code> </p> <code>progress</code> <p>Shared <code>JobProgress</code> instance updated as samples complete.</p> <p> TYPE: <code>JobProgress</code> </p> <code>job_log</code> <p>Optional <code>JobLog</code> instance for recording results.</p> <p> TYPE: <code>JobLog | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.abort","title":"abort  <code>async</code>","text":"<pre><code>abort()\n</code></pre> <p>Simulate an emergency stop. Transitions state to ERROR immediately.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.MockHardwareManager.clear_jam","title":"clear_jam","text":"<pre><code>clear_jam()\n</code></pre> <p>No-op in mock: no real dispensing loop to unblock.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager","title":"HardwareManager","text":"<pre><code>HardwareManager()\n</code></pre> <p>Production wrapper around <code>JubileeManager</code> for real hardware.</p> <p>All blocking serial or network calls are offloaded via <code>asyncio.to_thread()</code> so the uvicorn event loop is never stalled. <code>JubileeManager</code> is imported lazily inside :meth:<code>connect</code> so the server starts cleanly on machines that lack the <code>science_jubilee</code> package.</p> Note <p>Scale telemetry uses :meth:<code>~src.Scale.Scale.get_weight_for_telemetry</code> so the 4 Hz WebSocket loop yields to dispensing commands.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager-functions","title":"Methods:","text":""},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.connect","title":"connect  <code>async</code>","text":"<pre><code>connect(config)\n</code></pre> <p>Initiate hardware connection from a background <code>asyncio.Task</code>.</p> <p>Called after <code>POST /api/hardware/connect</code> returns HTTP 202 so the response is not blocked by homing. State transitions::</p> <pre><code>DISCONNECTED \u2192 HOMING \u2192 IDLE   (success)\nDISCONNECTED \u2192 HOMING \u2192 ERROR  (failure)\n</code></pre> PARAMETER DESCRIPTION <code>config</code> <p>Hardware configuration (dispensers, pistons, IP, serial port).</p> <p> TYPE: <code>HardwareConfig</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>JubileeManager.connect()</code> returns <code>False</code>.</p> <code>Exception</code> <p>Any other connection failure; state is set to <code>ERROR</code>.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.disconnect","title":"disconnect  <code>async</code>","text":"<pre><code>disconnect()\n</code></pre> <p>Disconnect from hardware and reset manager state.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.get_weight_unstable","title":"get_weight_unstable  <code>async</code>","text":"<pre><code>get_weight_unstable()\n</code></pre> <p>Read the current (unstable) scale weight without waiting for stability.</p> RETURNS DESCRIPTION <code>float | None</code> <p>Scale reading in grams, or <code>None</code> if hardware is not connected.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.get_weight_stable","title":"get_weight_stable  <code>async</code>","text":"<pre><code>get_weight_stable()\n</code></pre> <p>Read the scale weight after waiting for a stable reading.</p> RETURNS DESCRIPTION <code>float | None</code> <p>Stable scale reading in grams, or <code>None</code> if not connected.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.get_weight_telemetry","title":"get_weight_telemetry  <code>async</code>","text":"<pre><code>get_weight_telemetry()\n</code></pre> <p>Read scale weight for telemetry frames.</p> <p>Uses <code>Scale.get_weight_for_telemetry()</code> so telemetry yields to higher-priority scale commands used during dispensing.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.get_dispensers","title":"get_dispensers","text":"<pre><code>get_dispensers()\n</code></pre> <p>Return the current status of all connected piston dispensers.</p> RETURNS DESCRIPTION <code>list[DispenserStatus]</code> <p>list[DispenserStatus]: Per-dispenser piston counts, or an empty list</p> <code>list[DispenserStatus]</code> <p>if hardware is not connected.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.update_dispenser_pistons","title":"update_dispenser_pistons","text":"<pre><code>update_dispenser_pistons(index, num_pistons)\n</code></pre> <p>Update the remaining piston count for a single dispenser.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based dispenser index.</p> <p> TYPE: <code>int</code> </p> <code>num_pistons</code> <p>New piston count.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> on success, <code>False</code> if hardware is not connected or</p> <p> TYPE: <code>bool</code> </p> <code>bool</code> <p>the index is out of range.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.run_dispensing_job","title":"run_dispensing_job  <code>async</code>","text":"<pre><code>run_dispensing_job(wells, progress, job_log=None)\n</code></pre> <p>Execute a real powder dispensing job via <code>JubileeManager</code>.</p> <p>Iterates over <code>wells</code> sequentially, calling <code>JubileeManager.dispense_to_well()</code> in a thread for each well. Updates <code>progress</code> in-place after each well completes.</p> PARAMETER DESCRIPTION <code>wells</code> <p>Ordered list of well dicts with <code>well_id</code> and <code>target_weight</code> keys.</p> <p> TYPE: <code>list[dict]</code> </p> <code>progress</code> <p>Shared <code>JobProgress</code> instance updated as wells complete.</p> <p> TYPE: <code>JobProgress</code> </p> <code>job_log</code> <p>Optional <code>JobLog</code> instance attached to the manager for recording actual weights.</p> <p> TYPE: <code>JobLog | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If <code>dispense_to_well()</code> returns <code>False</code> for any well.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.run_hardness_job","title":"run_hardness_job  <code>async</code>","text":"<pre><code>run_hardness_job(samples, progress, job_log=None)\n</code></pre> <p>Execute a hardness testing job via <code>JubileeManager</code>.</p> PARAMETER DESCRIPTION <code>samples</code> <p>Ordered list of sample dicts with <code>tray_index</code>, <code>sample_index</code>, and <code>mode</code> keys.</p> <p> TYPE: <code>list[dict]</code> </p> <code>progress</code> <p>Shared <code>JobProgress</code> instance updated as samples complete.</p> <p> TYPE: <code>JobProgress</code> </p> <code>job_log</code> <p>Optional <code>JobLog</code> instance for recording results.</p> <p> TYPE: <code>JobLog | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.abort","title":"abort  <code>async</code>","text":"<pre><code>abort()\n</code></pre> <p>Send M112 to the Duet controller and transition to ERROR state.</p> <p>Delegates to <code>JubileeManager.abort()</code>, which bypasses the motion state machine intentionally. State is set to <code>ERROR</code> here so the job <code>finally</code> block cannot silently reset it back to <code>IDLE</code>.</p>"},{"location":"api/gui/jubilee-gui/#hardware_manager.HardwareManager.clear_jam","title":"clear_jam","text":"<pre><code>clear_jam()\n</code></pre> <p>Unblock the dispensing loop after the operator clears a powder jam.</p> <p>Delegates to <code>JubileeManager.clear_jam()</code> which sets the threading event that <code>execute_fill_powder</code> is waiting on.</p>"},{"location":"api/gui/jubilee-gui/#modelspy","title":"<code>models.py</code>","text":""},{"location":"api/gui/jubilee-gui/#models.MachineState","title":"MachineState","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>String enum representing the five possible hardware states.</p> <p>Serialised as a lowercase string in WebSocket telemetry frames and REST responses so the React frontend can compare states without importing this enum.</p> ATTRIBUTE DESCRIPTION <code>IDLE</code> <p>Connected and ready for a job.</p> <p> </p> <code>HOMING</code> <p>Connection or homing in progress.</p> <p> </p> <code>RUNNING</code> <p>Job loop executing.</p> <p> </p> <code>ERROR</code> <p>Emergency stop or failure; reconnect required.</p> <p> </p> <code>DISCONNECTED</code> <p>No hardware session.</p> <p> </p>"},{"location":"api/gui/jubilee-gui/#models.HardwareConfig","title":"HardwareConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Sent from the Settings screen when the user clicks Connect.</p> <p>Omitted or null fields are filled from <code>system_config.json</code> on the server by :func:<code>server._merge_hardware_config</code>.</p> ATTRIBUTE DESCRIPTION <code>num_dispensers</code> <p>Piston dispenser count.</p> <p> TYPE: <code>int | None</code> </p> <code>pistons_per_dispenser</code> <p>Initial piston inventory per dispenser.</p> <p> TYPE: <code>int | None</code> </p> <code>machine_address</code> <p>Duet hostname or IP.</p> <p> TYPE: <code>str | None</code> </p> <code>scale_port</code> <p>Serial device path for the balance.</p> <p> TYPE: <code>str | None</code> </p>"},{"location":"api/gui/jubilee-gui/#models.DispenserStatus","title":"DispenserStatus","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-dispenser status reported in telemetry frames and REST responses.</p> ATTRIBUTE DESCRIPTION <code>index</code> <p>Zero-based dispenser index.</p> <p> TYPE: <code>int</code> </p> <code>pistons_remaining</code> <p>Software piston count remaining in the dispenser.</p> <p> TYPE: <code>int</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress","title":"JobProgress","text":"<pre><code>JobProgress()\n</code></pre> <p>Mutable in-memory job state shared between server endpoints and the hardware manager.</p> <p>A single instance is created at module level in <code>server.py</code> and passed by reference to the hardware manager's <code>run_dispensing_job</code> / <code>run_hardness_job</code> methods so that both the job loop and REST endpoints can read and mutate progress atomically.</p> ATTRIBUTE DESCRIPTION <code>running</code> <p><code>True</code> while the job loop is executing.</p> <p> TYPE: <code>bool</code> </p> <code>job_type</code> <p><code>\"dispensing\"</code> or <code>\"hardness\"</code>, set at job start.</p> <p> TYPE: <code>str | None</code> </p> <code>completed</code> <p>Number of wells/samples successfully processed.</p> <p> TYPE: <code>int</code> </p> <code>total</code> <p>Total wells/samples in the current job.</p> <p> TYPE: <code>int</code> </p> <code>current_item</code> <p>ID of the well/sample currently being processed.</p> <p> TYPE: <code>str | None</code> </p> <code>error</code> <p>Error message string if the job failed, otherwise <code>None</code>.</p> <p> TYPE: <code>str | None</code> </p> <code>started_at</code> <p>ISO-8601 UTC timestamp set when the job starts.</p> <p> TYPE: <code>str | None</code> </p> <code>items</code> <p>Ordered list of item dicts from the original job request.</p> <p> TYPE: <code>list</code> </p> <code>jam_detected</code> <p><code>True</code> when a powder jam pause is active.</p> <p> TYPE: <code>bool</code> </p> <code>jam_well_id</code> <p>Well identifier associated with an active jam.</p> <p> TYPE: <code>str | None</code> </p> Example <p>Start a dispensing job and mark the first well complete::</p> <pre><code>progress = JobProgress()\nprogress.start_job(\n    \"dispensing\",\n    [{\"well_id\": \"0\", \"target_weight\": 50.0}],\n    started_at=\"2026-04-16T14:30:00Z\",\n)\nprogress.mark_item_active(0)\nprogress.mark_item_complete(0, actual_weight=49.8)\n</code></pre>"},{"location":"api/gui/jubilee-gui/#models.JobProgress-functions","title":"Methods:","text":""},{"location":"api/gui/jubilee-gui/#models.JobProgress.item_id","title":"item_id  <code>staticmethod</code>","text":"<pre><code>item_id(item)\n</code></pre> <p>Return the stable display ID used by progress tracking.</p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.start_job","title":"start_job","text":"<pre><code>start_job(job_type, items, started_at)\n</code></pre> <p>Initialize all progress fields at job start.</p> PARAMETER DESCRIPTION <code>job_type</code> <p><code>\"dispensing\"</code> or <code>\"hardness\"</code>.</p> <p> TYPE: <code>str</code> </p> <code>items</code> <p>Ordered item dicts from the job request.</p> <p> TYPE: <code>list[dict]</code> </p> <code>started_at</code> <p>ISO-8601 UTC timestamp for <code>started_at</code>.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.mark_item_active","title":"mark_item_active","text":"<pre><code>mark_item_active(index)\n</code></pre> <p>Mark one item active and reset other active slots to incomplete.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based index into <code>self.items</code>. No-op when out of range.</p> <p> TYPE: <code>int</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.mark_item_complete","title":"mark_item_complete","text":"<pre><code>mark_item_complete(index, **updates)\n</code></pre> <p>Mark one item complete and attach optional result fields.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based index into <code>self.items</code>. No-op when out of range.</p> <p> TYPE: <code>int</code> </p> <code>**updates</code> <p>Extra fields merged into the item (e.g. <code>actual_weight</code>, <code>result_shore_a</code>). Increments <code>completed</code>.</p> <p> TYPE: <code>Any</code> DEFAULT: <code>{}</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.mark_item_error","title":"mark_item_error","text":"<pre><code>mark_item_error(index, sample_error, **updates)\n</code></pre> <p>Mark one item as an error without incrementing <code>completed</code>.</p> PARAMETER DESCRIPTION <code>index</code> <p>Zero-based index into <code>self.items</code>. No-op when out of range.</p> <p> TYPE: <code>int</code> </p> <code>sample_error</code> <p>Error message stored on the item.</p> <p> TYPE: <code>str</code> </p> <code>**updates</code> <p>Extra fields merged into the item before status is set.</p> <p> TYPE: <code>Any</code> DEFAULT: <code>{}</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.set_jam","title":"set_jam","text":"<pre><code>set_jam(well_id)\n</code></pre> <p>Mark a powder jam as active for the given well.</p> PARAMETER DESCRIPTION <code>well_id</code> <p>Well identifier shown in the jam dialog.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.clear_jam","title":"clear_jam","text":"<pre><code>clear_jam()\n</code></pre> <p>Clear an active jam so the UI dialog is dismissed.</p>"},{"location":"api/gui/jubilee-gui/#models.JobProgress.to_dict","title":"to_dict","text":"<pre><code>to_dict()\n</code></pre> <p>Serialise all fields to a JSON-compatible dict for WebSocket telemetry.</p> RETURNS DESCRIPTION <code>dict</code> <p>All progress fields suitable for inclusion in a telemetry frame.</p> <p> TYPE: <code>dict</code> </p>"},{"location":"api/gui/jubilee-gui/#react-application","title":"React Application","text":""},{"location":"api/gui/jubilee-gui/#screens","title":"Screens","text":"Route Component Description <code>/</code> <code>HomeScreen</code> Live job dashboard; shows running or last job <code>/dispensing</code> <code>PowderDispensingScreen</code> Well selection, target weights, job submission <code>/hardness</code> <code>HardnessTestingScreen</code> Sample selection, test mode assignment, job start <code>/data</code> <code>DataScreen</code> Browse and inspect historical job log files <code>/manual</code> <code>ManualControlScreen</code> Gateway to the Jubilee machine web UI (DWC) <code>/settings</code> <code>SettingsScreen</code> Hardware configuration and connect/disconnect"},{"location":"api/gui/jubilee-gui/#persistent-layout","title":"Persistent Layout","text":"<p>Every route is wrapped in <code>RootLayout</code>, which renders:</p> <ul> <li><code>NavRail</code> \u2014 left-side vertical navigation, always visible</li> <li><code>BottomBar</code> \u2014 live weight display, machine state badge, and active tab label;   reads directly from the Zustand store</li> <li>The active screen fills the remaining area</li> </ul> <p>On mount, <code>RootLayout</code> calls <code>connectWs()</code> so all screens have live telemetry.</p>"},{"location":"api/gui/jubilee-gui/#key-components","title":"Key Components","text":"Component Purpose <code>WellGrid</code> Interactive 4\u00d76 grid; variants: <code>dispensing</code>, <code>hardness</code>, <code>result</code> <code>ArcProgress</code> Circular arc showing completion percentage, count, elapsed time <code>NavRail</code> Navigation bar with route links and connection status <code>BottomBar</code> Persistent status strip with live weight and machine state"},{"location":"api/gui/jubilee-gui/#grid-state-ownership","title":"Grid state ownership","text":"<p>Grid interaction state is owned in Zustand (<code>frontend/src/store/jubileeStore.js</code>) and rendered by <code>WellGrid</code> / <code>SampleTrayGrid</code> presentational components.</p>"},{"location":"api/gui/jubilee-gui/#see-also","title":"See Also","text":"<ul> <li>Jubilee Store \u2014 Zustand ViewModel reference</li> <li>Using the Automation UI \u2014 User guide</li> <li>Architecture \u2014 System architecture overview</li> <li>JubileeManager \u2014 Hardware coordination layer</li> </ul>"},{"location":"api/gui/jubilee-view-model/","title":"Jubilee Store Reference","text":"<p><code>jubileeStore.js</code> is the Zustand store that acts as the ViewModel layer in the React frontend. It is the single source of truth for all shared application state and exposes actions that wrap REST API calls to the FastAPI backend.</p>"},{"location":"api/gui/jubilee-view-model/#architecture-role","title":"Architecture Role","text":"<pre><code>View      \u2014 React screens and components\n    \u2195  useJubileeStore(selector)\nViewModel \u2014 Zustand store (jubileeStore.js)\n    \u2195  fetch (REST)   \u2195  WebSocket /ws (4 Hz)\nModel     \u2014 FastAPI server + HardwareManager (Python)\n</code></pre> <p>The store is not a thin data cache. It also:</p> <ul> <li>Owns the single <code>WebSocket</code> instance and handles reconnect logic</li> <li>Normalises REST error responses into a consistent <code>{ ok, error? }</code> return shape</li> <li>Keeps UI-specific derived state (e.g. <code>wsConnected</code>)</li> </ul> <p>Minimise re-renders</p> <p>Subscribe to individual slices with selectors (<code>useJubileeStore((s) =&gt; s.telemetry)</code>) rather than destructuring the whole store in every component.</p>"},{"location":"api/gui/jubilee-view-model/#quick-start","title":"Quick Start","text":"<pre><code>import { useJubileeStore } from '../store/jubileeStore'\n\nconst telemetry = useJubileeStore((s) =&gt; s.telemetry)\nconst submitJob = useJubileeStore((s) =&gt; s.submitJob)\n</code></pre> State ReferenceActions Reference"},{"location":"api/gui/jubilee-view-model/#state-shape","title":"State Shape","text":""},{"location":"api/gui/jubilee-view-model/#telemetry","title":"<code>telemetry</code>","text":"<p>Live snapshot received from the WebSocket at 4 Hz. Replaced wholesale on every frame so stale fields from a previous server restart are never retained.</p> <pre><code>{\n  weight:     null,          // float | null  \u2014 scale reading in grams\n  state:      null,          // MachineState string | null\n  connected:  false,         // true when state is idle, running, or homing\n  jubilee_ip: 'jubilee.local',\n  job:        null,          // JobProgress object | null\n  dispensers: [],            // DispenserStatus[]\n  clients:    null,          // number of connected browser tabs\n}\n</code></pre>"},{"location":"api/gui/jubilee-view-model/#hardwarestatus","title":"<code>hardwareStatus</code>","text":"<p>Last full machine snapshot from <code>GET /api/status</code>. <code>null</code> until the first successful fetch. Refreshed on mount and after hardware lifecycle actions.</p>"},{"location":"api/gui/jubilee-view-model/#joblog","title":"<code>jobLog</code>","text":"<p>Normalised most-recent job log from <code>GET /api/job/log</code>. Uses the same shape as <code>telemetry.job</code>. <code>null</code> when no job has run in the current server session and no log files exist.</p> <p><code>HomeScreen</code> uses <code>jobLog</code> to display the last completed job when no job is currently running.</p>"},{"location":"api/gui/jubilee-view-model/#wsconnected","title":"<code>wsConnected</code>","text":"<p><code>true</code> when the WebSocket connection to <code>/ws</code> is open. Becomes <code>false</code> briefly during reconnect.</p>"},{"location":"api/gui/jubilee-view-model/#statuserror-joblogerror","title":"<code>statusError</code> / <code>jobLogError</code>","text":"<p>Error message strings from the most recent failed <code>loadStatus()</code> or <code>fetchJobLog()</code> call respectively. <code>null</code> on success.</p>"},{"location":"api/gui/jubilee-view-model/#actions","title":"Actions","text":"<p>All async actions return <code>{ ok: true }</code> on success or <code>{ ok: false, error: string }</code> on failure. Components are expected to check <code>ok</code> and surface <code>error</code> to the user.</p>"},{"location":"api/gui/jubilee-view-model/#websocket-lifecycle","title":"WebSocket Lifecycle","text":"Action Description <code>connectWs()</code> Opens <code>/ws</code>; reconnects after 1.5 s on close. Called once from <code>RootLayout</code>. <code>disconnectWs()</code> Closes the socket and cancels pending reconnect. Called on unmount."},{"location":"api/gui/jubilee-view-model/#status","title":"Status","text":""},{"location":"api/gui/jubilee-view-model/#loadstatus","title":"<code>loadStatus()</code>","text":"<p>Fetches <code>GET /api/status</code> into <code>hardwareStatus</code>.</p> <pre><code>await store.loadStatus()\n</code></pre>"},{"location":"api/gui/jubilee-view-model/#hardware-lifecycle","title":"Hardware Lifecycle","text":""},{"location":"api/gui/jubilee-view-model/#connecthardwareconfig","title":"<code>connectHardware(config)</code>","text":"<p><code>POST /api/hardware/connect</code> \u2014 returns <code>{ ok, error? }</code>. Connection progress appears in <code>telemetry.state</code>:</p> <pre><code>disconnected \u2192 homing \u2192 idle    (success)\ndisconnected \u2192 homing \u2192 error   (failure)\n</code></pre> <pre><code>const { ok, error } = await store.connectHardware({\n  num_dispensers:        2,\n  pistons_per_dispenser: 10,\n  machine_address:       'jubilee.local',  // null reads from system_config.json\n  scale_port:            '/dev/ttyUSB0',\n})\n</code></pre>"},{"location":"api/gui/jubilee-view-model/#disconnecthardware","title":"<code>disconnectHardware()</code>","text":"<p><code>POST /api/hardware/disconnect</code> \u2014 stops any running job and refreshes <code>hardwareStatus</code>.</p>"},{"location":"api/gui/jubilee-view-model/#job-actions","title":"Job Actions","text":"DispensingHardness <pre><code>const { ok } = await store.submitJob('dispensing', [\n  { well_id: '0', target_weight: 50.0 },\n  { well_id: '3', target_weight: 45.0 },\n])\n</code></pre> <pre><code>const { ok } = await store.submitJob('hardness', [\n  { tray_index: 0, sample_index: 0, mode: 'shore_a' },\n  { tray_index: 0, sample_index: 1, mode: 'shore_d' },\n])\n</code></pre> <p>Valid hardness modes: <code>shore_a</code>, <code>shore_a_d</code>, <code>shore_d</code>.</p> Action Endpoint Behaviour <code>stopJob()</code> <code>POST /api/job/stop</code> Finish current well/sample, return to <code>idle</code> <code>cancelJob()</code> <code>POST /api/job/cancel</code> Same server semantics as stop; user-facing \"cancel\" <code>abortJob()</code> <code>POST /api/job/abort</code> Emergency M112; machine enters <code>error</code> <code>fetchJobLog()</code> <code>GET /api/job/log</code> Updates <code>jobLog</code> <p>The server validates machine state (<code>idle</code>) before accepting jobs. Invalid payloads return HTTP 422 (Pydantic validation).</p>"},{"location":"api/gui/jubilee-view-model/#data-flow","title":"Data Flow","text":"WebSocket (4 Hz)REST (on demand) <pre><code>Server telemetry_loop()\n    \u2192 ws.send_json(frame)\n        \u2192 ws.onmessage in jubileeStore.connectWs()\n            \u2192 set({ telemetry: { weight, state, connected, ... } })\n                \u2192 All subscribed components re-render\n</code></pre> <p>Every action follows the same pattern:</p> <pre><code>async actionName(args) {\n  try {\n    const data = await apiFunction(args)\n    return { ok: true }\n  } catch (err) {\n    return { ok: false, error: err.message }\n  }\n}\n</code></pre> <p>Errors from the server's <code>detail</code> field are extracted by the <code>request()</code> helper in <code>jubileeApi.js</code> and surfaced as <code>err.message</code>.</p> WebSocket reconnect <pre><code>ws.onclose / ws.onerror\n    \u2192 set({ wsConnected: false })\n    \u2192 setTimeout(connectWs, 1500)\n</code></pre>"},{"location":"api/gui/jubilee-view-model/#jubileeapijs","title":"<code>jubileeApi.js</code>","text":"<p>Thin HTTP wrapper consumed by the store. All functions return the parsed JSON body on success and throw an <code>Error</code> with the server's <code>detail</code> string on non-2xx responses. The Vite dev-server proxy forwards <code>/api/*</code> to <code>http://localhost:8000</code>, so the same <code>BASE = '/api'</code> URL works identically in development and production.</p> Function Method Path <code>fetchStatus()</code> GET <code>/api/status</code> <code>connectHardware(c)</code> POST <code>/api/hardware/connect</code> <code>disconnectHardware()</code> POST <code>/api/hardware/disconnect</code> <code>startJob(body)</code> POST <code>/api/job/start</code> <code>stopJob()</code> POST <code>/api/job/stop</code> <code>cancelJob()</code> POST <code>/api/job/cancel</code> <code>abortJob()</code> POST <code>/api/job/abort</code> <code>fetchJobLog()</code> GET <code>/api/job/log</code> <code>fetchDispensers()</code> GET <code>/api/dispensers</code> <code>updateDispenser(i,n)</code> PUT <code>/api/dispensers/{index}</code>"},{"location":"api/gui/jubilee-view-model/#see-also","title":"See Also","text":"<ul> <li>Web Frontend Reference - FastAPI server, REST API, and React screens</li> <li>Using the Automation UI - User guide</li> <li>Architecture - System architecture overview</li> </ul>"},{"location":"concepts/architecture/","title":"System Architecture","text":"<p>This document explains the architecture of Jubilee Powder at a conceptual level.</p>"},{"location":"concepts/architecture/#overview","title":"Overview","text":"<p>Jubilee Powder uses a layered architecture where each layer provides a different level of abstraction and control.</p>"},{"location":"concepts/architecture/#architecture-diagram","title":"Architecture Diagram","text":"<pre><code>graph TB\n    subgraph \"User Interface Layer\"\n        A[User Scripts]\n        subgraph \"Web Frontend\"\n            B[React Screens / Components]\n            Z[Zustand Store]\n        end\n    end\n\n    subgraph \"API Layer\"\n        C[FastAPI Server]\n        CM[HardwareManager]\n    end\n\n    subgraph \"Coordination Layer\"\n        D[JubileeManager]\n    end\n\n    subgraph \"Validation Layer\"\n        E[MotionPlatformStateMachine]\n        F[MovementExecutor]\n    end\n\n    subgraph \"Component Layer\"\n        G[Manipulator]\n        H[PistonDispenser]\n        I[Scale]\n    end\n\n    subgraph \"Hardware Layer\"\n        J[Jubilee Machine]\n        K[Scale Hardware]\n        L[Deck/Labware]\n    end\n\n    subgraph \"Configuration\"\n        M[ConfigLoader]\n        N[JSON Config Files]\n    end\n\n    B -- useJubileeStore --&gt; Z\n    Z -- REST / WebSocket --&gt; C\n    C --&gt; CM\n    CM --&gt; D\n    A --&gt; D\n    D --&gt; E\n    D --&gt; G\n    D --&gt; I\n    E --&gt; F\n    F --&gt; J\n    G --&gt; E\n    H --&gt; E\n    I --&gt; K\n    E --&gt; L\n    M --&gt; N\n    D --&gt; M\n    E --&gt; M</code></pre>"},{"location":"concepts/architecture/#layer-details","title":"Layer Details","text":""},{"location":"concepts/architecture/#1-user-interface-layer","title":"1. User Interface Layer","text":"<p>Purpose: Entry point for automation tasks and user interaction</p> <p>Components:</p> <ul> <li>User Scripts: Python scripts written by users to automate specific tasks</li> <li>Web Frontend: Browser-based React application for interactive control and monitoring</li> </ul> <p>Responsibilities:</p> <ul> <li>Display system state and progress</li> <li>Accept user input (well selection, weights, configuration)</li> <li>Provide visual feedback</li> <li>Submit jobs and monitor progress in real time</li> </ul> <p>Web Frontend Architecture (MVVM):</p> <p>The browser application follows an MVVM pattern:</p> Layer Component Responsibility View React screens + components Render state, capture user input ViewModel Zustand store Derived state, REST/WebSocket actions Model FastAPI server + HardwareManager Hardware state, physical operations <p>See Web Frontend Reference and Jubilee Store Reference for details.</p>"},{"location":"concepts/architecture/#2-api-layer-web-frontend-only","title":"2. API Layer (Web Frontend Only)","text":"<p>Purpose: Bridge the React frontend to the hardware coordination layer</p> <p>Components:</p> <ul> <li>FastAPI Server (<code>frontend/server.py</code>): REST endpoints and WebSocket telemetry</li> <li>HardwareManager (<code>frontend/src/hardware_manager.py</code>): Async wrapper around   <code>JubileeManager</code> that offloads all blocking calls to <code>asyncio.to_thread()</code></li> </ul> <p>Responsibilities:</p> <ul> <li>Serve REST endpoints for discrete commands (connect, start/stop/abort job, etc.)</li> <li>Push a continuous 4 Hz telemetry frame to all connected browsers via WebSocket</li> <li>Translate between the async FastAPI event loop and the synchronous <code>JubileeManager</code> API</li> <li>Manage job lifecycle (start, progress tracking, <code>JobLog</code> persistence)</li> <li>Optionally upload completed logs to Google Drive when <code>google_drive.enabled</code> is true</li> </ul> <p>Key Design:</p> <ul> <li>A <code>MockHardwareManager</code> with an identical public API enables UI development without   physical hardware</li> <li>WebSocket telemetry eliminates polling: every connected browser tab receives live   state updates at 4 Hz without issuing repeated REST calls</li> <li>Job progress is tracked in a shared <code>JobProgress</code> object threaded through server   endpoints and the hardware manager</li> <li>Completed jobs are written as JSON by <code>JobLog</code> (<code>metadata</code> + <code>state.molds</code> or   <code>state.samples</code>). When Drive backup is enabled, <code>JobDriveBackup</code> exports CSV   and image artifacts before upload</li> </ul>"},{"location":"concepts/architecture/#3-coordination-layer","title":"3. Coordination Layer","text":"<p>Purpose: Coordinate complex multi-component operations</p> <p>Components:</p> <ul> <li>JubileeManager: Central orchestrator for all operations</li> </ul> <p>Responsibilities:</p> <ul> <li>Connect to and manage all hardware components</li> <li>Provide high-level API for common operations</li> <li>Coordinate multi-step operations (e.g., dispense_to_well)</li> <li>Store all hardware state (dispensers, pistons, positions)</li> <li>Handle errors and provide meaningful feedback</li> </ul> <p>Key Design Decisions:</p> <ul> <li>Single point of access for most operations</li> <li>Owns the state machine (cannot be bypassed)</li> <li>Provides both convenience methods and component access</li> <li>All hardware state lives here</li> </ul>"},{"location":"concepts/architecture/#4-validation-layer","title":"4. Validation Layer","text":"<p>Purpose: Ensure all operations are safe and valid</p> <p>Components:</p> <ul> <li>MotionPlatformStateMachine: Validates and executes movements</li> <li>MovementExecutor: Executes validated movements on hardware</li> </ul> <p>Responsibilities:</p> <ul> <li>Track current system state (position, tool, payload)</li> <li>Validate requested movements against current state</li> <li>Enforce movement constraints and safe zones</li> <li>Provide detailed error messages for invalid requests</li> <li>Execute validated movements</li> </ul> <p>State Machine States:</p> <p>The state machine tracks:</p> <ul> <li>Position: Current named position (e.g., \"global_ready\", \"scale_ready\")</li> <li>Active Tool: Which tool is currently picked up (or None)</li> <li>Payload: What the manipulator is holding (<code>empty</code>, <code>mold_without_top_piston</code>, <code>mold_with_top_piston</code>)</li> </ul> <p>Validation Rules:</p> <ul> <li>Tool must be picked up to move to certain positions</li> <li>Payload state affects which movements are allowed</li> <li>Some operations require specific starting positions</li> </ul>"},{"location":"concepts/architecture/#5-component-layer","title":"5. Component Layer","text":"<p>Purpose: Represent individual hardware components</p> <p>Components:</p> <ul> <li>Manipulator: Gripper tool with vertical axis</li> <li>PistonDispenser: Container for pistons with tracking</li> <li>Scale: Weight measurement device</li> </ul> <p>Responsibilities:</p> <ul> <li>Encapsulate component-specific logic</li> <li>Provide component-specific operations</li> <li>Maintain component state</li> <li>Interact with validation layer for movements</li> </ul>"},{"location":"concepts/architecture/#6-hardware-layer","title":"6. Hardware Layer","text":"<p>Purpose: Physical hardware interface</p> <p>Components:</p> <ul> <li>Jubilee Machine: CNC motion platform (via science-jubilee library)</li> <li>Scale Hardware: Precision balance (via serial connection)</li> <li>Deck/Labware: Physical deck layout and labware definitions (<code>Mold</code>, <code>MoldSet</code> in <code>trickler_labware</code>)</li> </ul> <p>Responsibilities:</p> <ul> <li>Execute physical movements</li> <li>Report sensor readings</li> <li>Handle low-level communication protocols</li> </ul>"},{"location":"concepts/architecture/#7-configuration","title":"7. Configuration","text":"<p>Purpose: Centralize system configuration</p> <p>Components:</p> <ul> <li>ConfigLoader: Loads and provides access to configuration</li> <li>JSON Config Files: Define positions, deck layouts, system parameters</li> </ul> <p>Configuration Files:</p> <ul> <li><code>motion_platform_positions.json</code>: State machine positions and transitions</li> <li><code>system_config.json</code>: System-level settings</li> <li><code>mold_labware.json</code>: Deck layout and labware definitions</li> <li><code>weight_well_deck.json</code>: Well-specific configurations</li> </ul>"},{"location":"concepts/architecture/#data-flow","title":"Data Flow","text":""},{"location":"concepts/architecture/#example-web-ui-multi-well-dispensing-job","title":"Example: Web UI Multi-Well Dispensing Job","text":"<p>This example traces how a multi-well dispensing job flows through the web frontend:</p> <pre><code>sequenceDiagram\n    participant U as User (Browser)\n    participant S as Zustand Store\n    participant API as FastAPI Server\n    participant HM as HardwareManager\n    participant JM as JubileeManager\n    participant H as Hardware\n\n    U-&gt;&gt;S: submitJob('dispensing', wells)\n    S-&gt;&gt;API: POST /api/job/start\n    API--&gt;&gt;S: 202 Accepted\n    API-&gt;&gt;HM: run_dispensing_job(wells, progress)\n\n    loop For each well\n        HM-&gt;&gt;JM: dispense_to_well(well_id, weight)\n        JM-&gt;&gt;H: move, pick, fill, piston\n        H--&gt;&gt;JM: complete\n        JM--&gt;&gt;HM: True\n        HM-&gt;&gt;HM: progress.completed += 1\n        API-&gt;&gt;S: WebSocket frame (job.completed, job.current_item)\n        S-&gt;&gt;U: re-render arc + well grid\n    end\n\n    HM--&gt;&gt;API: job loop exits\n    API-&gt;&gt;S: WebSocket frame (job.running = false)\n    S-&gt;&gt;U: show completed state</code></pre>"},{"location":"concepts/architecture/#example-direct-script-operation","title":"Example: Direct Script Operation","text":"<p>For comparison, a direct script operation (without the web frontend):</p> <pre><code>sequenceDiagram\n    participant U as User Script\n    participant JM as JubileeManager\n    participant SM as StateMachine\n    participant M as Manipulator\n    participant S as Scale\n    participant H as Hardware\n\n    U-&gt;&gt;JM: dispense_to_well(\"0\", 50.0)\n    JM-&gt;&gt;SM: validated_move_to_mold_slot(\"0\")\n    SM-&gt;&gt;SM: Check current state\n    SM-&gt;&gt;SM: Validate movement\n    SM-&gt;&gt;H: Execute movement\n    H--&gt;&gt;SM: Movement complete\n    SM--&gt;&gt;JM: Success\n\n    JM-&gt;&gt;M: pick_mold(\"0\")\n    M-&gt;&gt;SM: Update payload state\n    M-&gt;&gt;H: Execute gripper\n\n    JM-&gt;&gt;SM: validated_move_to_scale()\n    SM-&gt;&gt;H: Execute movement\n\n    JM-&gt;&gt;M: place_mold_on_scale()\n    M-&gt;&gt;H: Execute gripper\n    M-&gt;&gt;SM: Update payload state\n\n    JM-&gt;&gt;SM: validated_fill_powder(50.0)\n    SM-&gt;&gt;S: Read weight\n    SM-&gt;&gt;H: Control trickler\n    SM-&gt;&gt;S: Read weight\n    Note over SM,S: Repeat until target reached\n\n    JM-&gt;&gt;M: pick_mold_from_scale()\n    JM-&gt;&gt;SM: validated_move_to_dispenser()\n    JM-&gt;&gt;JM: get_piston_from_dispenser()\n    JM-&gt;&gt;SM: validated_move_to_mold_slot(\"0\")\n    JM-&gt;&gt;M: place_mold(\"0\")\n\n    JM--&gt;&gt;U: True (success)</code></pre>"},{"location":"concepts/architecture/#key-design-principles","title":"Key Design Principles","text":""},{"location":"concepts/architecture/#safety-through-validation","title":"Safety Through Validation","text":"<p>All movements pass through the state machine</p> <p>Direct G-code or science-jubilee moves that bypass the state machine will desynchronize internal position tracking. Use validated paths for all automated operation.</p> <p>All movements must pass through the state machine validator. This prevents:</p> <ul> <li>Moving to unsafe positions</li> <li>Collisions between tools and labware</li> <li>Invalid state transitions</li> <li>Operating on wrong component</li> </ul>"},{"location":"concepts/architecture/#progressive-disclosure","title":"Progressive Disclosure","text":"<p>The architecture supports multiple levels of complexity:</p> <ul> <li>Simple: Use JubileeManager methods (most users)</li> <li>Advanced: Access state machine directly (power users)</li> <li>Expert: Access components and machine directly (developers)</li> </ul>"},{"location":"concepts/architecture/#state-tracking","title":"State Tracking","text":"<p>The system maintains comprehensive state:</p> <ul> <li>Physical position of motion platform</li> <li>Active tool and payload</li> <li>Component states (dispenser piston counts, etc.)</li> <li>Configuration data</li> </ul>"},{"location":"concepts/architecture/#fail-safe-defaults","title":"Fail-Safe Defaults","text":"<p>When operations fail:</p> <ul> <li>Clear error messages explain what went wrong</li> <li>System state remains consistent</li> <li>No silent failures</li> <li>Failed operations return <code>False</code> or an invalid <code>MoveValidationResult</code></li> </ul>"},{"location":"concepts/architecture/#configuration-driven","title":"Configuration-Driven","text":"<p>Config is the source of truth</p> <p>Machine behavior comes from checked-in JSON under <code>jubilee_api_config/</code>, loaded via <code>ConfigLoader</code> and <code>PositionRegistry</code>. See the Configuration Guide.</p> <p>Physical parameters are in configuration files, not code:</p> <ul> <li>Easy to adapt to different setups</li> <li>No recompilation needed</li> <li>Version control for configurations</li> <li>Validation of configuration data</li> </ul>"},{"location":"concepts/architecture/#extending-the-system","title":"Extending the System","text":"<p>To add new operations, hardware, or positions, follow the same layered pattern:</p> <ul> <li>New high-level operations belong in <code>JubileeManager</code>, implemented as calls to <code>validated_*</code> state machine methods.</li> <li>New hardware components follow the existing <code>PistonDispenser</code> / <code>Scale</code> pattern: a class that encapsulates component state and uses the state machine for movements.</li> <li>New named positions are added to <code>motion_platform_positions.json</code> with transitions, constraints, and tool requirements. See the Configuration Guide for details.</li> </ul> <p>See JubileeManager and MotionPlatformStateMachine for implementation specifics.</p>"},{"location":"concepts/architecture/#see-also","title":"See Also","text":"<ul> <li>Glossary for terminology</li> <li>JubileeManager API</li> <li>State Machine Details</li> <li>Web Frontend Reference for the REST API and React application</li> <li>Jubilee Store Reference for the ViewModel layer</li> <li>Best Practices for safe operation</li> </ul>"},{"location":"concepts/best-practices/","title":"Best Practices","text":"<p>This page covers must-know information for safely operating the Jubilee system. Read this before your first run and refer back to it whenever you modify hardware or configuration.</p>"},{"location":"concepts/best-practices/#duet3d-controller","title":"Duet3D Controller","text":"<p>The Jubilee uses a Duet3D board (typically Duet 3 Mini or Duet 3 MB6HC) running RepRapFirmware. The Duet3D is a precision motion controller and requires careful handling.</p>"},{"location":"concepts/best-practices/#critical-rules","title":"Critical Rules","text":"<p>Never hot-plug stepper motor cables</p> <p>Connecting or disconnecting stepper motor cables while the Duet3D is powered can permanently damage the stepper drivers. Always power off the Duet3D completely before swapping or re-seating motor connectors.</p> <p>Never hot-plug endstop cables</p> <p>Hot-plugging endstops can damage the input pins. Power off before making any endstop wiring changes.</p> <p>Wait for full boot before sending commands</p> <p>After powering on, wait for the Duet3D status LED to stabilize and for the web interface to become responsive before connecting or issuing G-code.</p>"},{"location":"concepts/best-practices/#web-interface","title":"Web Interface","text":"<p>The Duet3D exposes a web interface (Duet Web Control, DWC) at the machine's IP address. Keep this open in a browser tab during operation - it lets you:</p> <ul> <li>Issue an immediate emergency stop (the red stop button)</li> <li>Monitor real-time axis positions and speeds</li> <li>View the console for firmware error messages</li> <li>Check motor current and driver status</li> </ul> <p>If you cannot reach the web interface, the machine should be considered unreachable and you should not attempt to run scripts against it.</p>"},{"location":"concepts/best-practices/#homing","title":"Homing","text":"<p>The Jubilee must be homed after every power cycle. The software calls <code>homeall</code> automatically during <code>connect()</code>, but be aware:</p> <ul> <li>Check the deck before homing. Homing moves axes to their endstops at high speed. Any obstruction on the deck can cause a collision.</li> </ul>"},{"location":"concepts/best-practices/#jubilee-hardware","title":"Jubilee Hardware","text":""},{"location":"concepts/best-practices/#general-mechanical-safety","title":"General Mechanical Safety","text":"<ul> <li>Keep hands and tools clear of the motion envelope during any move. The Jubilee has no collision detection - it will drive into an obstruction without stopping.</li> <li>Do not place items on the deck outside of their designated positions defined in the configuration. The state machine validates moves against named positions, not arbitrary coordinates. Objects outside those positions are invisible to the software.</li> <li>Secure all cables with sufficient slack to cover the full range of motion. A taut cable can be torn loose, damaging connectors or the board.</li> <li>Inspect the tool-changer coupling before each session. The Jubilee uses a kinematic tool-changer; if the tool is not fully seated, it can fall or move unpredictably during operation.</li> </ul>"},{"location":"concepts/best-practices/#tool-changes","title":"Tool Changes","text":"<ul> <li>Only one tool can be active at a time. This is enforced by software, but physically ensure the tools are always returned to the correct rack position.</li> <li>If a tool drop is suspected (the carriage moved without the tool following), issue an emergency stop immediately and inspect before resuming.</li> <li>In normal operation, never manually push a tool onto the carriage while the machine is powered and connected. Use the software-driven pickup sequence.</li> </ul>"},{"location":"concepts/best-practices/#deck-layout","title":"Deck Layout","text":"<p>Deck layout must match configuration</p> <p>The deck configuration (<code>jubilee_api_config/motion_platform_positions.json</code>) defines exact positions for wells, scale, dispensers, and the tool dock. If you move any labware, you MUST update the configuration to match. Otherwise, the machine will not detect the change.</p> <p>Position calibration is required maintenance. Re-verify positions after any significant physical disturbance to the machine (for example, disassembly and reassembly, new deck or labware, or collisions).</p>"},{"location":"concepts/best-practices/#operational-best-practices","title":"Operational Best Practices","text":""},{"location":"concepts/best-practices/#before-every-run","title":"Before Every Run","text":"<ol> <li>Confirm the Jubilee user interface is connected and homed with the appropriate number of pistons in each dispenser.</li> <li>Confirm the deck is clear of any items that should not be there.</li> <li>Confirm the scale is powered on and responding.</li> <li>Confirm the piston dispensers are loaded with the correct number of pistons.</li> </ol>"},{"location":"concepts/best-practices/#first-run-of-a-new-script","title":"First Run of a New Script","text":"<ul> <li>Always supervise the first run. Sit at the machine with your hand near the emergency stop (either the power switch or user interface button).</li> <li>Run at reduced feedrate during the first execution to give yourself time to react. You can lower feedrates by adjusting the speed multiplier in configuration or by issuing <code>M220 S50</code> (50% speed) via Duet3D Web Interface before starting.</li> <li>Run a single operation before a full batch. Verify one well completes correctly before running all wells.</li> </ul>"},{"location":"concepts/best-practices/#emergency-stop","title":"Emergency Stop","text":"<p>There are several ways to stop the machine mid-run:</p> <ol> <li>Click the red stop button in the Duet Web Control interface (immediate, stops all motion).</li> <li>Press the red stop button on the user interface home screen.</li> <li>Turn off the Jubilee power switch.</li> </ol> <p>After an emergency stop...</p> <p>...the machine is in an unknown position. You must manually park the tool, cycle power to the Jubilee, and re-home all axes before resuming operation.</p>"},{"location":"concepts/best-practices/#long-runs-and-unattended-operation","title":"Long Runs and Unattended Operation","text":"<ul> <li>Do not leave the machine fully unattended for long runs until you have validated the full sequence multiple times.</li> <li>If you must leave, verify the first several wells completed correctly and check that the dispenser has enough pistons for the remaining wells.</li> </ul>"},{"location":"concepts/best-practices/#software-and-scripting","title":"Software and Scripting","text":""},{"location":"concepts/best-practices/#check-return-values","title":"Check Return Values","text":"<p>Most <code>JubileeManager</code> methods return <code>True</code> on success and <code>False</code> on failure (or sometimes <code>MoveValidationResult</code>). Do not assume success - check the return value and handle failures explicitly.</p> <pre><code>success = manager.dispense_to_well(\"0\", 50.0)\nif not success:\n    print(\"Dispense failed - inspect before continuing\")\n    manager.disconnect()\n    raise RuntimeError(\"Dispense operation failed\")\n</code></pre>"},{"location":"concepts/best-practices/#do-not-bypass-the-state-machine","title":"Do Not Bypass the State Machine","text":"<p>The <code>MotionPlatformStateMachine</code> validates all movements for safety. Do not send raw G-code moves through the science-jubilee library in parallel with the state machine - the state machine will not process these moves and its internal position will become incorrect.</p>"},{"location":"concepts/best-practices/#configuration-changes","title":"Configuration Changes","text":"<p>When modifying <code>motion_platform_positions.json</code> or other configuration files, restart the Python process before the next run. The configuration is loaded once at launch and is not reloaded automatically.</p> <p>If you adjust physical positions on the machine, update the configuration and verify the new positions by jogging to them manually via Duet Web Interface before running the full automation.</p>"},{"location":"concepts/best-practices/#see-also","title":"See Also","text":"<p>How-To Guides</p> <ul> <li>Configuration Guide - how to edit position files and system parameters</li> <li>Run Operations on New Data - step-by-step guide for first-time data runs</li> <li>Using the Automation UI - interactive operation through the browser interface</li> </ul> <p>API Reference</p> <ul> <li>JubileeManager - high-level API, including return value semantics</li> <li>MotionPlatformStateMachine - validation layer details and state tracking</li> <li>Architecture Overview - layered architecture and design principles</li> </ul>"},{"location":"concepts/glossary/","title":"Glossary","text":"<p>This glossary defines key terms used throughout the Jubilee Powder system.</p> <p>How to use this page</p> <p>Terms are grouped by topic. Payload enums and naming conventions at the end are especially important when reading API docs or job logs.</p>"},{"location":"concepts/glossary/#general-terms","title":"General Terms","text":"Jubilee Tool-changing CNC motion platform developed by the Machine Agency. The base hardware for this automation system. Motion Platform Physical CNC (Computer Numerical Control) system that provides X, Y, Z positioning and tool changing capabilities. Deck Working surface of the Jubilee where labware (molds, dispensers, scale, etc.) is positioned. The deck layout is defined in configuration files. Labware Physical items placed on the deck, such as well plates, dispensers, scales, and other equipment."},{"location":"concepts/glossary/#core-components","title":"Core Components","text":""},{"location":"concepts/glossary/#jubileemanager","title":"JubileeManager","text":"<p>The primary high-level interface for controlling the Jubilee system. Coordinates operations between multiple hardware components and provides simplified APIs for common tasks.</p> <p>When to use: For all standard operations. This should be your first choice for interacting with the system.</p>"},{"location":"concepts/glossary/#motionplatformstatemachine","title":"MotionPlatformStateMachine","text":"<p>A state machine that validates and executes all movements. Ensures safety by tracking system state and preventing invalid operations.</p> <p>When to use: For advanced control when JubileeManager doesn't provide the specific operation you need.</p>"},{"location":"concepts/glossary/#manipulator","title":"Manipulator","text":"<p>A custom toolhead with a gripper and vertical axis (V-axis). Used for picking up and placing molds and other objects.</p> <p>Key features: - Gripper for holding objects - Vertical axis for precise height control - Integrated with state machine for validated movements</p>"},{"location":"concepts/glossary/#pistondispenser","title":"PistonDispenser","text":"<p>A container that holds and dispenses cylindrical pistons. Tracks the number of available pistons and provides them one at a time.</p> <p>Key features: - Holds multiple pistons in a stack - Dispenses from the top of the stack - Tracks available piston count</p>"},{"location":"concepts/glossary/#mold-trickler_labware","title":"Mold (<code>trickler_labware</code>)","text":"<p>Weight-based labware type extending <code>science_jubilee</code> <code>Well</code>. Tracks powder weight in grams, exposes <code>well_id</code> for job logging, and links to a state-machine ready position (e.g. <code>mold_ready_0</code>).</p>"},{"location":"concepts/glossary/#trickler","title":"Trickler","text":"<p>The powder filling mechanism used to add powder to a mold while it is on the scale.</p> <p>Terminology policy: - Use fill or add powder when referring to powder transfer into a mold - Reserve dispense for piston-dispenser operations</p>"},{"location":"concepts/glossary/#joblog","title":"JobLog","text":"<p>Persistent per-job JSON record written when a dispensing or hardness job ends. Tracks planned items, per-well or per-sample results, and job outcome (<code>successful</code>, <code>cancelled</code>, <code>aborted</code>).</p> <p>Key features:</p> <ul> <li>Sequential job IDs and standardized filenames (<code>{id:04d}_{date}_{type}_{count}.json</code>)</li> <li>Dispensing state in <code>state.molds</code>; hardness state in <code>state.samples</code></li> <li>Optional Google Drive export of JSON, CSV, and hardness images</li> </ul>"},{"location":"concepts/glossary/#scale","title":"Scale","text":"<p>A precision balance for weighing objects. Connected via USB serial connection.</p> <p>Capabilities: - Stable weight reading (waits for stability) - Unstable weight reading (immediate) - High precision measurements</p>"},{"location":"concepts/glossary/#hardnesstester","title":"HardnessTester","text":"<p>A segment-based LCD display reader that recognizes 7-segment digits without traditional OCR. Useful for reading scale displays, meters, and other LCD equipment.</p> <p>Key features: - Segment detection instead of OCR - Works with low-contrast displays - Calibration system for accurate reading - No machine learning dependencies</p>"},{"location":"concepts/glossary/#state-machine-concepts","title":"State Machine Concepts","text":""},{"location":"concepts/glossary/#state","title":"State","text":"<p>The current condition of the system, including: - Physical position of the motion platform - Which tool is active (picked up) - What the manipulator is holding (payload)</p>"},{"location":"concepts/glossary/#validation","title":"Validation","text":"<p>The process of checking whether a requested operation is safe and valid given the current state.</p> <p>Validation checks: - Is the current position correct for this operation? - Is the right tool picked up? - Is the payload state compatible? - Are constraints satisfied?</p>"},{"location":"concepts/glossary/#named-position","title":"Named Position","text":"<p>A predefined location on the deck with a specific name (e.g., \"global_ready\", \"scale_ready\"). Named positions are defined in the configuration file.</p> <p>Examples: - <code>global_ready</code>: Safe position away from all labware - <code>scale_ready</code>: Position to access the scale - <code>mold_ready_0</code>: Position to access mold 0</p>"},{"location":"concepts/glossary/#transition","title":"Transition","text":"<p>A validated movement from one named position to another. The state machine defines which transitions are allowed.</p>"},{"location":"concepts/glossary/#context","title":"Context","text":"<p>The state machine's internal representation of the current system state. Includes: - Current position - Active tool - Payload state - Reference to hardware components</p>"},{"location":"concepts/glossary/#payload-states","title":"Payload States","text":"<code>empty</code> Manipulator is not holding any object. Default state after homing or after placing an object. <code>mold_without_top_piston</code> Manipulator is holding a mold that does not contain a top piston. <code>mold_with_top_piston</code> Manipulator is holding a mold that contains a top piston."},{"location":"concepts/glossary/#tool-concepts","title":"Tool Concepts","text":""},{"location":"concepts/glossary/#tool","title":"Tool","text":"<p>An interchangeable attachment that the Jubilee can pick up and put down. In this system, the primary tool is the Manipulator.</p>"},{"location":"concepts/glossary/#tool-pickup","title":"Tool Pickup","text":"<p>The process of mechanically engaging with a tool at its parking position and locking it to the carriage.</p>"},{"location":"concepts/glossary/#tool-parking","title":"Tool Parking","text":"<p>The process of placing a tool back at its designated parking position and releasing it.</p>"},{"location":"concepts/glossary/#active-tool","title":"Active Tool","text":"<p>The tool currently picked up and ready for use. Only one tool can be active at a time.</p>"},{"location":"concepts/glossary/#movement-concepts","title":"Movement Concepts","text":""},{"location":"concepts/glossary/#homing","title":"Homing","text":"<p>The process of moving axes to their reference positions (endstops) to establish a coordinate system. Must be done before any precision operations.</p> <p>Types: - Home All: Homes X, Y, Z, and U axes - Home Manipulator (V-axis): Homes the manipulator's V axis   - Can be performed while holding a mold without a top piston   - Starts at v=2 (tamper inserted into mold)   - Ends at v=-7 (tamper touching bottom of mold)   - Uses the mold itself as a reference for accurate positioning - Rehome: Re-establishes reference after an error</p>"},{"location":"concepts/glossary/#feed-rate","title":"Feed Rate","text":"<p>The speed at which the motion platform moves. Can be set to different values for different operations.</p> <p>Common values: - SLOW: For precise operations - MEDIUM: For normal operations (default) - FAST: For rapid positioning</p>"},{"location":"concepts/glossary/#safe-zone","title":"Safe Zone","text":"<p>An area of the deck where movement is known to be safe. The state machine uses safe zones to prevent collisions.</p>"},{"location":"concepts/glossary/#backup-and-export","title":"Backup and Export","text":""},{"location":"concepts/glossary/#jobdrivebackup","title":"JobDriveBackup","text":"<p>Google Drive uploader that turns a completed <code>JobLog</code> JSON file into a per-job Drive folder (JSON, derived CSV, hardness images). Used when <code>google_drive.enabled</code> is true in <code>system_config.json</code>.</p> <p>When to use: Automatic background backup after job completion; status is surfaced on the Settings screen.</p>"},{"location":"concepts/glossary/#configuration-terms","title":"Configuration Terms","text":""},{"location":"concepts/glossary/#configuration-file","title":"Configuration File","text":"<p>A JSON file that defines system parameters, positions, and constraints. Changes to configuration do not require code changes.</p> <p>Key configuration files: - <code>motion_platform_positions.json</code>: Defines positions and transitions - <code>system_config.json</code>: System-level settings - <code>mold_labware.json</code>: Deck layout and labware - <code>weight_well_deck.json</code>: Well-specific parameters</p>"},{"location":"concepts/glossary/#deck-layout","title":"Deck Layout","text":"<p>The arrangement of labware on the Jubilee deck, defined in configuration files. Includes positions and dimensions of all items.</p>"},{"location":"concepts/glossary/#system-config","title":"System Config","text":"<p>Global system parameters such as: - Duet controller IP address - Serial port assignments - Default feed rates - Tool configurations</p>"},{"location":"concepts/glossary/#operation-terms","title":"Operation Terms","text":""},{"location":"concepts/glossary/#dispense-operation","title":"Dispense Operation","text":"<p>A complete workflow that: 1. Picks up a mold from a well 2. Moves to the scale 3. Fills the mold with powder to a target weight 4. Retrieves a piston from a dispenser 5. Returns the mold to the well</p>"},{"location":"concepts/glossary/#tamping","title":"Tamping","text":"<p>The process of compressing powder in a mold held by the manipulator before inserting the top piston. Tamping serves two purposes:</p> <ol> <li>Volume reduction: Compresses powder to allow the top piston to fit when powder volume would otherwise prevent insertion</li> <li>Airborne particulate reduction: Reduces the amount of powder that becomes airborne when the piston is inserted</li> </ol> <p>After tamping, the V axis is automatically re-homed to ensure axis accuracy. Typically performed at the <code>scale_ready</code> position after filling the mold.</p>"},{"location":"concepts/glossary/#well-id","title":"Well ID","text":"<p>A unique identifier for a mold position using numerical indexing (e.g., \"0\", \"1\", \"2\"). Used to reference specific locations in the deck layout.</p>"},{"location":"concepts/glossary/#target-weight","title":"Target Weight","text":"<p>The desired weight of material to dispense into a mold, measured in grams.</p>"},{"location":"concepts/glossary/#error-and-validation-terms","title":"Error and Validation Terms","text":""},{"location":"concepts/glossary/#validation-result","title":"Validation Result","text":"<p>The result of a validation check, indicating whether an operation is allowed and providing a reason if not.</p> <p>Fields: - <code>valid</code>: Boolean indicating if operation is allowed - <code>reason</code>: String explaining why operation was rejected (if invalid)</p>"},{"location":"concepts/glossary/#tool-state-error","title":"Tool State Error","text":"<p>An exception raised when an operation is attempted with the wrong tool state (e.g., tool not picked up, wrong tool active).</p>"},{"location":"concepts/glossary/#movement-constraint","title":"Movement Constraint","text":"<p>A rule that limits when or how a movement can be performed. Examples: - \"Must have manipulator tool picked up\" - \"Cannot move with mold_with_top_piston payload\"</p>"},{"location":"concepts/glossary/#naming-conventions-and-legacy-aliases","title":"Naming Conventions and Legacy Aliases","text":"<p>Use these terms consistently across documentation:</p> <ul> <li>Top piston is canonical. Avoid legacy <code>cap</code> terminology.</li> <li>Mold is canonical for payload/object terminology. Legacy <code>WeightWell</code> references should be treated as mold equivalents when encountered in older comments.</li> <li>Payload enums should use <code>empty</code>, <code>mold_without_top_piston</code>, <code>mold_with_top_piston</code>.</li> <li>\"Must be at global_ready position first\"</li> </ul>"},{"location":"concepts/glossary/#lcd-display-reading-terms","title":"LCD Display Reading Terms","text":""},{"location":"concepts/glossary/#7-segment-display","title":"7-Segment Display","text":"<p>A type of electronic display that uses seven segments arranged in a figure-eight pattern to display decimal numbers. Each segment can be turned on or off independently.</p> <p>Segment layout: - Top, Top-Left, Top-Right, Middle, Bottom-Left, Bottom-Right, Bottom</p>"},{"location":"concepts/glossary/#segment-detection","title":"Segment Detection","text":"<p>The process of determining which segments in a 7-segment display are active (lit) by analyzing pixel brightness in predefined regions.</p>"},{"location":"concepts/glossary/#calibration","title":"Calibration","text":"<p>The process of defining the pixel coordinates (ROI boundaries) for each digit in an LCD display. Required once per display setup.</p> <p>Types: - Manual Calibration: User provides exact pixel coordinates - Auto-Detection: System attempts to find digits automatically</p>"},{"location":"concepts/glossary/#roi-region-of-interest","title":"ROI (Region of Interest)","text":"<p>A rectangular area in an image that contains one digit. Defined by (x1, y1, x2, y2) pixel coordinates.</p>"},{"location":"concepts/glossary/#segment-pattern","title":"Segment Pattern","text":"<p>A 7-bit tuple representing which segments are active in a digit. Example: <code>(1, 1, 1, 0, 1, 1, 1)</code> represents digit \"0\".</p>"},{"location":"concepts/glossary/#lookup-table","title":"Lookup Table","text":"<p>A dictionary mapping segment patterns to digit strings. Used to recognize digits after segment detection.</p>"},{"location":"concepts/glossary/#grayscale-conversion","title":"Grayscale Conversion","text":"<p>The first image preprocessing step in the LCD reading pipeline. The BGR camera frame is converted to grayscale before unsharp masking (sharpening, on by default), CLAHE, and thresholding are applied.</p>"},{"location":"concepts/glossary/#sharpening-unsharp-masking","title":"Sharpening (Unsharp Masking)","text":"<p>Edge recovery applied by default after grayscale conversion and before CLAHE. Uses Gaussian blur and weighted subtraction to sharpen segment boundaries. Controlled by <code>sharpen_strength</code> (default 31) and <code>sharpen_blur_radius</code> on <code>HardnessTester</code>; set <code>sharpen_strength</code> to <code>0.0</code> to skip.</p>"},{"location":"concepts/glossary/#clahe-contrast-limited-adaptive-histogram-equalization","title":"CLAHE (Contrast Limited Adaptive Histogram Equalization)","text":"<p>An image enhancement algorithm that improves local contrast by dividing the image into small tiles and applying histogram equalization to each.</p>"},{"location":"concepts/glossary/#segment-threshold","title":"Segment Threshold","text":"<p>The proportion of pixels that must be active in a segment region for it to be considered \"ON\". Default: 0.5 (50%)</p>"},{"location":"concepts/glossary/#hardware-terms","title":"Hardware Terms","text":""},{"location":"concepts/glossary/#duet-controller","title":"Duet Controller","text":"<p>The controller board that runs the Jubilee machine. Provides G-code interpretation and motor control.</p>"},{"location":"concepts/glossary/#g-code","title":"G-code","text":"<p>The programming language used to control CNC machines. The Duet controller interprets G-code commands.</p>"},{"location":"concepts/glossary/#endstop","title":"Endstop","text":"<p>A sensor that detects when an axis has reached its reference position. Used during homing.</p>"},{"location":"concepts/glossary/#serial-port","title":"Serial Port","text":"<p>A communication interface used to connect to the scale. Typically appears as <code>/dev/ttyUSB0</code> on Linux.</p>"},{"location":"concepts/glossary/#ip-address","title":"IP Address","text":"<p>The network address of the Jubilee's Duet controller. Used to establish network communication.</p>"},{"location":"concepts/glossary/#software-development-terms","title":"Software Development Terms","text":""},{"location":"concepts/glossary/#type-hints","title":"Type Hints","text":"<p>Python annotations that specify the expected types of function parameters and return values. Used throughout the codebase for better code quality.</p>"},{"location":"concepts/glossary/#docstring","title":"Docstring","text":"<p>Documentation embedded in Python code that describes what a function, class, or module does. This project uses Google-style docstrings.</p>"},{"location":"concepts/glossary/#google-docstring-style","title":"Google Docstring Style","text":"<p>A specific format for writing docstrings that includes sections for arguments, returns, raises, examples, etc.</p>"},{"location":"concepts/glossary/#see-also","title":"See Also","text":"<ul> <li>Architecture Overview - Understand how these components work together</li> <li>Quick Start Guide - See these terms in context</li> <li>API Reference - Detailed documentation of all components</li> </ul>"},{"location":"getting-started/overview/","title":"Overview","text":""},{"location":"getting-started/overview/#what-is-jubilee-powder","title":"What is Jubilee Powder?","text":"<p>Jubilee Powder is a comprehensive software system for controlling the Jubilee Motion Platform to automate powder dispensing and Shore hardness testing. It provides both a browser-based interface and a Python API for coordinating operations across multiple hardware components.</p>"},{"location":"getting-started/overview/#core-components","title":"Core Components","text":""},{"location":"getting-started/overview/#hardware","title":"Hardware","text":"<p>The system integrates several hardware components:</p> <ul> <li>Jubilee Motion Platform: A tool-changing CNC motion system</li> <li>Precision Scale: For weighing materials with high accuracy (typically A&amp;D FX-120i)</li> <li>Piston Dispensers: For storing and dispensing cylindrical pistons, placed in molds after dispensing powder</li> <li>Manipulator Tool: A custom toolhead with grabber and vertical axis</li> </ul>"},{"location":"getting-started/overview/#software-layers","title":"Software Layers","text":"<p>The software is organized in layers from high-level to low-level:</p> <ol> <li>Automation UI (User Interface)</li> <li>Browser-based interface for dispensing and hardness jobs</li> <li>Visual mold selection and configuration</li> <li>Real-time progress monitoring</li> <li> <p>Built-in safety checks</p> </li> <li> <p>Frontend Coordination Layer</p> </li> <li>Zustand-based <code>Jubilee Store</code> for browser state and actions</li> <li> <p>FastAPI server for REST and WebSocket transport</p> </li> <li> <p>JubileeManager (Core API)</p> </li> <li>Highest-level programming API</li> <li>Error handling and recovery</li> <li> <p>Hardware state management</p> </li> <li> <p>MotionPlatformStateMachine (Validation Layer)</p> </li> <li>Validates all movements for safety</li> <li>Manages system state</li> <li> <p>Enforces movement constraints</p> </li> <li> <p>Hardware Drivers (Bottom Layer)</p> </li> <li>Direct hardware communication</li> <li>Low-level control primitives</li> <li>Communicates with Duet3D Controller</li> </ol>"},{"location":"getting-started/overview/#key-concepts","title":"Key Concepts","text":""},{"location":"getting-started/overview/#state-machine-validation","title":"State Machine Validation","text":"<p>All movements are validated through a state machine that:</p> <ul> <li>Tracks the current position and tool state</li> <li>Validates requested movements are safe</li> <li>Prevents invalid state transitions</li> <li>Ensures proper sequencing of operations</li> </ul>"},{"location":"getting-started/overview/#tool-management","title":"Tool Management","text":"<p>The Jubilee uses a tool-changing system where:</p> <ul> <li>Tools are picked up and parked at specific positions</li> <li>Only one tool can be active at a time</li> <li>Tools must be at specific positions for certain operations</li> </ul>"},{"location":"getting-started/overview/#payload-tracking","title":"Payload Tracking","text":"<p>The system tracks what the manipulator is holding:</p> <code>empty</code> No mold held <code>mold_without_top_piston</code> Holding a mold without top piston <code>mold_with_top_piston</code> Holding a mold with top piston <p>This enables safe movement validation based on current load.</p>"},{"location":"getting-started/overview/#design-philosophy","title":"Design Philosophy","text":""},{"location":"getting-started/overview/#safety-first","title":"Safety First","text":"<ul> <li>All movements are validated before execution</li> <li>State machine prevents unsafe operations</li> <li>Clear error messages for invalid requests</li> </ul>"},{"location":"getting-started/overview/#ease-of-use","title":"Ease of Use","text":"<ul> <li>High-level API for common operations</li> <li>Progressive disclosure (simple things easy, complex things possible)</li> <li>GUI for user-modifiable operations</li> <li>Python API</li> </ul>"},{"location":"getting-started/overview/#flexibility","title":"Flexibility","text":"<ul> <li>JSON-based configuration</li> <li>Extensible architecture</li> <li>Multiple access levels (high-level to low-level)</li> <li>MVVM architecture for GUI extensibility</li> </ul>"},{"location":"getting-started/overview/#usage-options","title":"Usage Options","text":"Automation UIPython API <p>For interactive machine operation with visual feedback, use the web interface documented in Using the Automation UI.</p> <p>Features:</p> <ul> <li>Visual mold and sample selection</li> <li>Real-time weight and job monitoring</li> <li>Hardware configuration and connection lifecycle</li> <li>Safety checks and emergency controls</li> </ul> <p>For scripted tasks and custom workflows:</p> <pre><code>from src.JubileeManager import JubileeManager\n\nmanager = JubileeManager(num_piston_dispensers=2, num_pistons_per_dispenser=10)\nmanager.connect()\nmanager.dispense_to_well(\"0\", 50.0)\nmanager.disconnect()\n</code></pre> <p>See Quick Start Guide for details.</p>"},{"location":"getting-started/overview/#system-requirements","title":"System Requirements","text":""},{"location":"getting-started/overview/#hardware-requirements","title":"Hardware Requirements","text":"<ul> <li>Jubilee Motion Platform with Duet3D controller</li> <li>USB connection to precision scale (A&amp;D FX-120i preferred, other A&amp;D scales may work)</li> <li>Network connection to Jubilee controller</li> <li>Sufficient workspace for deck layout</li> </ul>"},{"location":"getting-started/overview/#software-requirements","title":"Software Requirements","text":"<ul> <li>Python 3.12 or later</li> <li>Node.js 20.19+ or 22.12+ (Vite 8 requirement; <code>deploy/install.sh</code> installs Node 22 LTS automatically)</li> <li>Linux-based operating system (for hardware integration)</li> <li>Dependencies listed in <code>requirements.txt</code></li> </ul>"},{"location":"getting-started/overview/#installation","title":"Installation","text":"<ol> <li> <p>Clone the repository:</p> <pre><code>git clone https://github.com/bennett-lahn/jubilee-powder.git\ncd jubilee-powder\n</code></pre> </li> <li> <p>Create a virtual environment:</p> <pre><code>python -m venv jubilee-env\nsource jubilee-env/bin/activate  # On Linux/Mac\n# or\njubilee-env\\Scripts\\activate  # On Windows\n</code></pre> </li> <li> <p>Install dependencies:</p> <pre><code>pip install -r requirements.txt\n</code></pre> </li> <li> <p>Configure your system:</p> <p>Configuration must match your physical setup</p> <p>Edit files in <code>jubilee_api_config/</code>. You may need to update homing files, tool pickup/place macros, and position config to match your unique setup. Set the Jubilee IP address, deck layout, and tool positions before connecting.</p> <ul> <li>Configuration Guide</li> <li>Position Config API</li> </ul> </li> </ol>"},{"location":"getting-started/overview/#see-also","title":"See Also","text":"<ul> <li>To launch the web UI: Building and Running for dev mode, production mode, and Raspberry Pi kiosk setup</li> <li>For GUI users: GUI User Guide</li> <li>For programmers: Quick Start Guide</li> <li>Before your first run: Best Practices for hardware safety rules</li> <li>System design: Architecture Guide</li> <li>Common tasks: How-To Guides</li> </ul>"},{"location":"getting-started/quickstart/","title":"Quick Start Guide","text":"<p>This guide will walk you through creating your first Jubilee powder dispensing script.</p>"},{"location":"getting-started/quickstart/#prerequisites","title":"Prerequisites","text":"<p>Before you start</p> <p>Confirm each item below before running your first script:</p> <ul> <li>[ ] Jubilee Powder installed (see Overview)</li> <li>[ ] Jubilee machine powered on and network-accessible</li> <li>[ ] Scale connected via USB</li> <li>[ ] Configuration files set up in <code>jubilee_api_config/</code></li> <li>[ ] Deck clear of obstructions (homing runs during <code>connect()</code>)</li> </ul>"},{"location":"getting-started/quickstart/#your-first-script","title":"Your First Script","text":""},{"location":"getting-started/quickstart/#step-1-import-jubileemanager","title":"Step 1: Import JubileeManager","text":"<pre><code>from src.JubileeManager import JubileeManager\n</code></pre> <p>The <code>JubileeManager</code> class is your primary interface to the Jubilee system.</p>"},{"location":"getting-started/quickstart/#step-2-create-manager-instance","title":"Step 2: Create Manager Instance","text":"<pre><code>manager = JubileeManager(\n    num_piston_dispensers=2,       # Number of piston dispensers\n    num_pistons_per_dispenser=10   # Pistons per dispenser\n)\n</code></pre>"},{"location":"getting-started/quickstart/#step-3-connect-to-hardware","title":"Step 3: Connect to Hardware","text":"<pre><code>from pathlib import Path\n\n# Connect to the system\n# Option 1: Use default (automatically finds config in project root)\nconnected = manager.connect(\n    machine_address=\"192.168.1.100\",  # Your Jubilee IP address\n    scale_port=\"/dev/ttyUSB0\"         # Scale serial port\n    # state_machine_config defaults to project_root/jubilee_api_config/motion_platform_positions.json\n)\n\n# Option 2: Specify custom path (absolute or relative to project root)\n# project_root = Path(__file__).parent.parent\n# connected = manager.connect(\n#     machine_address=\"192.168.1.100\",\n#     scale_port=\"/dev/ttyUSB0\",\n#     state_machine_config=str(project_root / \"jubilee_api_config\" / \"motion_platform_positions.json\")\n# )\n\nif not connected:\n    print(\"Failed to connect!\")\n    exit(1)\n\nprint(\"Connected successfully!\")\n</code></pre> <p>Warning</p> <p>The <code>connect()</code> method will automatically:</p> <ul> <li>Connect to the Jubilee machine</li> <li>Connect to the scale</li> <li>Initialize the state machine</li> <li>Home all axes</li> <li>Leave tools parked until an operation requires one</li> </ul> <p>Your Jubilee will move if this command is run. Ensure the deck is clear before connecting.</p>"},{"location":"getting-started/quickstart/#step-4-perform-operations","title":"Step 4: Perform Operations","text":"<pre><code># Dispense to a well with target weight\nsuccess = manager.dispense_to_well(\n    well_id=\"0\",            # Well identifier (numerical index 0-17)\n    target_weight=50.0      # Target weight in grams\n)\n\nif success:\n    print(\"Dispense operation completed successfully!\")\nelse:\n    print(\"Dispense operation failed!\")\n</code></pre>"},{"location":"getting-started/quickstart/#step-5-clean-up","title":"Step 5: Clean Up","text":"<pre><code># Always disconnect when done\nmanager.disconnect()\nprint(\"Disconnected from hardware\")\n</code></pre>"},{"location":"getting-started/quickstart/#complete-example","title":"Complete Example","text":"<p>Here's the complete script:</p> <pre><code>from src.JubileeManager import JubileeManager\n\ndef main():\n    # Create manager\n    manager = JubileeManager(\n        num_piston_dispensers=2,\n        num_pistons_per_dispenser=10\n    )\n\n    # Connect\n    if not manager.connect(\n        machine_address=\"192.168.1.100\",\n        scale_port=\"/dev/ttyUSB0\"\n    ):\n        print(\"Failed to connect!\")\n        return\n\n    try:\n        # Read weight\n        initial_weight = manager.get_weight_stable()\n        print(f\"Initial weight: {initial_weight}g\")\n\n        # Dispense operation\n        success = manager.dispense_to_well(\n            well_id=\"7\",\n            target_weight=0.5 # in grams\n        )\n\n        if success:\n            print(\"Operation completed!\")\n        else:\n            print(\"Operation failed!\")\n\n    finally:\n        # Always disconnect\n        manager.disconnect()\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"getting-started/quickstart/#understanding-what-happened","title":"Understanding What Happened","text":"<p>When you run this script:</p> <ol> <li>Connection Phase:</li> <li>Connects to Jubilee controller via network</li> <li>Connects to scale via USB serial</li> <li>Loads configuration from JSON files</li> <li>Homes all machine axes (X, Y, Z, U, V)</li> <li> <p>Loads tools and keeps them parked until requested by operations</p> </li> <li> <p>Operation Phase:</p> </li> <li>Moves to the specified mold location</li> <li>Picks up the mold from the well</li> <li>Moves to the scale</li> <li>Places mold on scale</li> <li>Fills powder to target weight</li> <li>Picks up mold from scale</li> <li>(Optional) Tamps powder to compress it (V-axis is re-homed after tamping)</li> <li>Gets a piston from dispenser</li> <li>Returns mold to well</li> </ol> <p>Note: The V-axis can be homed while holding a mold without a top piston. During homing, the tamper moves from v=2 (inserted into mold) to v=-7 (touching mold bottom), using the mold itself as a positioning reference.</p> <ol> <li>Cleanup Phase:</li> <li>Disconnects from all hardware</li> <li>Releases resources</li> </ol>"},{"location":"getting-started/quickstart/#common-issues","title":"Common Issues","text":""},{"location":"getting-started/quickstart/#connection-fails","title":"Connection Fails","text":"<p>If <code>manager.connect()</code> returns <code>False</code>:</p> <ul> <li>Check Jubilee IP: Verify the IP address is correct</li> <li>Check Network: Ensure your computer can reach the Jubilee (try <code>ping 192.168.1.100</code>)</li> <li>Check Scale Port: Verify the scale is connected (<code>ls /dev/ttyUSB*</code> on Linux)</li> <li>Check Permissions: Ensure you have permission to access the serial port</li> </ul>"},{"location":"getting-started/quickstart/#homing-fails","title":"Homing Fails","text":"<p>If homing fails during connection:</p> <ul> <li>Check Endstops: Ensure all endstops are functioning</li> <li>Check Tool State: Verify tool is not already picked up</li> <li>Check Deck: Ensure the deck is clear of obstructions</li> </ul>"},{"location":"getting-started/quickstart/#dispense-operation-fails","title":"Dispense Operation Fails","text":"<p>If <code>dispense_to_well()</code> returns <code>False</code>:</p> <ul> <li>Check Well ID: Verify the well ID exists in your deck configuration</li> <li>Check Dispenser: Ensure the dispenser has available pistons</li> <li>Check Scale: Verify the scale is responding to commands</li> </ul>"},{"location":"getting-started/quickstart/#advanced-features","title":"Advanced Features","text":""},{"location":"getting-started/quickstart/#tamping","title":"Tamping","text":"<p><code>dispense_to_well()</code> always tamps powder before piston insertion, using <code>tamp_depth_default</code> and <code>tamp_speed_default</code> from the <code>manipulator</code> section of <code>jubilee_api_config/system_config.json</code>. Adjust those values in config rather than passing extra arguments to <code>dispense_to_well()</code> (it accepts only <code>well_id</code> and <code>target_weight</code>).</p> <p>See the Manipulator API for manual tamp control outside the full dispense workflow.</p>"},{"location":"getting-started/quickstart/#see-also","title":"See Also","text":"<ul> <li>System architecture</li> <li>How-to guides for specific tasks</li> <li>JubileeManager API reference</li> <li>State machine for advanced control</li> <li>Best Practices for safe operation</li> </ul>"},{"location":"getting-started/quickstart/#tips","title":"Tips","text":"<p>Use Try-Finally</p> <p>Always use <code>try-finally</code> blocks to ensure <code>disconnect()</code> is called even if an error occurs.</p> <p>Check Return Values</p> <p>Most JubileeManager methods return <code>True/False</code> to indicate success. Always check these values.</p> <p>Start Simple</p> <p>Begin with simple operations like reading the scale before attempting complex multi-step operations.</p> <p>Hardware Safety</p> <p>Always monitor the first run of any new script. Be ready to press the emergency stop if needed. Use a lower feedrate so collisions can be stopped before they damage the system.</p>"},{"location":"how-to/configuration/","title":"Configuration Guide","text":"<p>Machine behavior is configured from checked-in JSON in <code>jubilee_api_config/</code>. Production code should read these values through typed loaders, not inline defaults.</p>"},{"location":"how-to/configuration/#overview","title":"Overview","text":"<code>system_config.json</code> Machine, server, safety, manipulator, trickler, and hardness tester settings. <code>motion_platform_positions.json</code> Named positions, transitions, and z-height policies. <p>Loaders:</p> <ul> <li>System config: <code>src/ConfigLoader.py</code> (<code>config.system</code> and typed accessors)</li> <li>Motion positions: <code>PositionRegistry.from_config_file()</code> via <code>src/motion_config.py</code></li> </ul> <p>If required fields are missing or the wrong type, load fails with <code>ConfigError</code>.</p> <p>No silent fallbacks in production code</p> <p>Do not use <code>dict.get(..., default)</code> or inline defaults for required machine, safety, or trickler settings. Missing config must fail at load time.</p>"},{"location":"how-to/configuration/#typical-edits","title":"Typical Edits","text":"Google Drive backupMachine addressDefault feedrateTamp defaults and limits <p>Enable upload of completed job logs (requires service account credentials and a parent folder ID):</p> <pre><code>{\n  \"google_drive\": {\n    \"enabled\": true,\n    \"credentials_file\": \"jubilee_api_config/service_account.json\",\n    \"drive_folder_id\": \"your-drive-folder-id\",\n    \"retry_interval_seconds\": 300\n  }\n}\n</code></pre> <p>When <code>enabled</code> is true, <code>drive_folder_id</code> must be non-empty. See Interpreting Results for export layout.</p> <p>Update <code>machine.duet_ip</code> in <code>system_config.json</code>:</p> <pre><code>{\n  \"machine\": {\n    \"duet_ip\": \"192.168.1.200\"\n  }\n}\n</code></pre> <p>Update <code>machine.default_feedrate</code>:</p> <pre><code>{\n  \"machine\": {\n    \"default_feedrate\": 2800\n  }\n}\n</code></pre> <p>Update values in <code>manipulator</code>:</p> <pre><code>{\n  \"manipulator\": {\n    \"tamp_depth_min\": 10.0,\n    \"tamp_depth_max\": 60.0,\n    \"tamp_speed_min\": 500,\n    \"tamp_speed_max\": 5000,\n    \"tamp_depth_default\": 40.0,\n    \"tamp_speed_default\": 2000\n  }\n}\n</code></pre>"},{"location":"how-to/configuration/#validation-workflow","title":"Validation Workflow","text":"<ol> <li>Edit the JSON value in <code>jubilee_api_config/</code>.</li> <li>If adding a new required field, add it to the corresponding Pydantic model in <code>src/ConfigLoader.py</code>.</li> <li>Read it from typed config access in code (<code>config.system...</code> or a typed accessor).</li> <li>Restart the Python process (config is loaded once at launch).</li> <li>Run tests and smoke checks.</li> </ol> <p>Verify physical positions after edits</p> <p>After changing positions in <code>motion_platform_positions.json</code>, jog to the new coordinates in Duet Web Control before running a full automation job.</p>"},{"location":"how-to/configuration/#do-not-do-this","title":"Do Not Do This","text":"<ul> <li>Do not use fallback defaults for machine, safety, or trickler config in production code.</li> <li>Do not use <code>dict.get(..., default)</code> as a substitute for required config.</li> <li>Do not move labware without updating <code>motion_platform_positions.json</code> to match.</li> </ul>"},{"location":"how-to/configuration/#see-also","title":"See Also","text":"<ul> <li>Position Config API</li> <li>ConfigLoader API</li> <li>Architecture</li> <li>Best Practices</li> </ul>"},{"location":"how-to/reading-lcd-displays/","title":"Reading LCD Displays","text":"<p>This guide walks through using the HardnessTester to read 7-segment LCD displays using segment detection instead of traditional OCR.</p>"},{"location":"how-to/reading-lcd-displays/#overview","title":"Overview","text":"<p>The HardnessTester provides automated reading of 7-segment LCD displays by:</p> <ul> <li>Detecting which segments are active in each digit</li> <li>Matching segment patterns to recognize numbers</li> <li>Providing reliable readings even with low-contrast displays</li> <li>Working without OCR or machine learning dependencies</li> </ul> <p>This approach is much more reliable than traditional OCR for LCD displays.</p>"},{"location":"how-to/reading-lcd-displays/#prerequisites","title":"Prerequisites","text":"<p>Before you start</p> <ul> <li>[ ] Camera positioned with a clear view of the LCD</li> <li>[ ] <code>opencv-python</code> installed (<code>pip install opencv-python</code>)</li> <li>[ ] One-time calibration completed (see First-Time Setup)</li> <li>[ ] Consistent, indirect lighting for camera to prevent glare </li> </ul>"},{"location":"how-to/reading-lcd-displays/#quick-start","title":"Quick Start","text":""},{"location":"how-to/reading-lcd-displays/#install-dependencies","title":"Install Dependencies","text":"<pre><code>pip install opencv-python\n</code></pre> <p>For Raspberry Pi (optional): <pre><code>pip install picamera2\n</code></pre></p>"},{"location":"how-to/reading-lcd-displays/#basic-usage","title":"Basic Usage","text":"<pre><code>from src.HardnessTester import HardnessTester\nfrom src.ConfigLoader import config\n\nreader = HardnessTester.from_system_config(\n    tester_mode=\"shore_a\",\n    cfg=config.system.hardness_testers.shore_a,\n)\n\nif reader.load_assigned_calibration():\n    result = reader.read_display()\n    print(f\"LCD shows: {result}\")  # e.g., \"1234\"\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#first-time-setup","title":"First-Time Setup","text":"<p>Calibration is required once per display setup</p> <p>Accuracy depend on digit ROIs and segment polygons saved during calibration. Re-calibrate if the camera or display position changes.</p>"},{"location":"how-to/reading-lcd-displays/#step-1-capture-test-image","title":"Step 1: Capture Test Image","text":"<p>Capture an image of your LCD display for calibration:</p> <pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n)\nframe = reader.capture_image(save=True, output_path='lcd_calibration_test.jpg')\nprint(\"Image saved - review it before calibration\")\n</code></pre> <p>Check the saved image to ensure: - LCD digits are clearly visible - Lighting is adequate - Camera is focused</p>"},{"location":"how-to/reading-lcd-displays/#step-2-run-calibration","title":"Step 2: Run Calibration","text":"<p>The calibration process opens an interactive GUI to map the digit and segment positions:</p> <pre><code>reader.calibrate(frame=frame, save_calibration=True)\n</code></pre> <p>The system will: 1. Save debug images showing preprocessing steps 2. Open an interactive GUI where you click and drag to draw a bounding box around each digit, then click to set the segment polygon vertices for each of the 7 segments per digit 3. Save calibration to the configured <code>calibration_path</code> (for Shore A: <code>jubilee_api_config/lcd_calibration_shore_a.json</code>) 4. Test the calibration immediately and print the result</p> <p>Tip</p> <p>Open the debug image <code>calibration_step5_binary.png</code> in an image viewer before calibrating. In the binary image, active LCD segments appear as dark regions on a bright background, which helps you place segment polygons accurately.</p>"},{"location":"how-to/reading-lcd-displays/#production-usage","title":"Production Usage","text":"<p>Once calibrated, reading displays is simple:</p> <pre><code>from src.HardnessTester import HardnessTester\n\n# Initialize with calibration\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n)\nreader.load_calibration()\n\n# Read display\nresult = reader.read_display()\n\nif result and '?' not in result:\n    print(f\"Reading: {result}\")\n    # Convert to number if needed\n    value = int(result)\nelse:\n    print(\"Reading failed or unclear\")\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#reading-from-static-images","title":"Reading from Static Images","text":"<p>Test with saved images:</p> <pre><code>from src.HardnessTester import test_with_image\n\nresult = test_with_image(\n    \"lcd_photo.jpg\",\n    \"jubilee_api_config/lcd_calibration_shore_a.json\",\n)\nprint(f\"Result: {result}\")\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#continuous-monitoring","title":"Continuous Monitoring","text":"<p>Read from camera in a loop:</p> <pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n)\nreader.load_calibration()\n\nwhile True:\n    result = reader.read_display()\n    print(f\"Current reading: {result}\")\n\n    # Wait before next reading\n    import time\n    time.sleep(1)\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#advanced-features","title":"Advanced Features","text":""},{"location":"how-to/reading-lcd-displays/#debug-mode","title":"Debug Mode","text":"<p>Enable debug output to troubleshoot recognition issues:</p> <pre><code>result = reader.read_display(debug=True, debug_prefix=\"debug\")\n</code></pre> <p>This saves images at each preprocessing step: - <code>debug_step1_original.png</code> - Raw camera capture - <code>debug_step2_gray.png</code> - Grayscale conversion - <code>debug_step3_sharpened.png</code> - Unsharp masking (sharpening; on by default) - <code>debug_step4_clahe.png</code> - Enhanced contrast - <code>debug_step5_binary.png</code> - Binary threshold - <code>debug_step6_cleaned.png</code> - Final cleaned image</p>"},{"location":"how-to/reading-lcd-displays/#viewing-segment-patterns","title":"Viewing Segment Patterns","text":"<p>See which segments are detected for debugging. Both <code>analyze_segment</code> and <code>recognize_digit</code> take the full binary frame and a digit index - they no longer accept a cropped ROI:</p> <pre><code>result = reader.read_display(debug=True)\n\n# Show detailed segment analysis\nframe = reader.capture_image()\nbinary = reader.preprocess_frame(frame)\n\nfor i in range(reader.num_digits):\n    segments = {\n        name: reader.analyze_segment(binary, i, name)\n        for name in reader.SEGMENT_ORDER\n    }\n    digit = reader.recognize_digit(binary, i, debug=True)\n    print(f\"Digit {i}: {segments} =&gt; {digit}\")\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#adjusting-sensitivity","title":"Adjusting Sensitivity","text":"<p>If segments aren't being detected reliably:</p> <pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n)\nreader.load_calibration()\n\n# More sensitive (detects dimmer segments)\nreader.segment_threshold = 0.3\n\n# Less sensitive (only bright segments)\nreader.segment_threshold = 0.7\n\nresult = reader.read_display()\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#understanding-how-it-works","title":"Understanding How It Works","text":""},{"location":"how-to/reading-lcd-displays/#segment-based-recognition","title":"Segment-Based Recognition","text":"<p>Instead of OCR, the system:</p> <ol> <li>Preprocesses the image to enhance LCD contrast</li> <li>Extracts each digit's region of interest (ROI)</li> <li>Analyzes seven segments per digit (top, top-left, top-right, middle, bottom-left, bottom-right, bottom)</li> <li>Counts active pixels in each segment</li> <li>Matches the 7-bit pattern to a digit (0-9)</li> </ol>"},{"location":"how-to/reading-lcd-displays/#segment-layout","title":"Segment Layout","text":"<p>Each 7-segment digit has this structure:</p> <pre><code>     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n     \u2502    top     \u2502\n     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n  \u250c\u2500\u2500\u2500\u2510       \u250c\u2500\u2500\u2500\u2510\n  \u2502 TL\u2502       \u2502 TR\u2502\n  \u2514\u2500\u2500\u2500\u2518       \u2514\u2500\u2500\u2500\u2518\n     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n     \u2502   middle   \u2502\n     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n  \u250c\u2500\u2500\u2500\u2510       \u250c\u2500\u2500\u2500\u2510\n  \u2502 BL\u2502       \u2502 BR\u2502\n  \u2514\u2500\u2500\u2500\u2518       \u2514\u2500\u2500\u2500\u2518\n     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n     \u2502   bottom   \u2502\n     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>For example, digit \"5\" has these segments active:</p> <ul> <li>Top (active)</li> <li>Top-left (active)</li> <li>Middle (active)</li> <li>Bottom-right (active)</li> <li>Bottom (active)</li> </ul> <p>Pattern: <code>(1, 1, 0, 1, 0, 1, 1)</code> \u2192 Recognized as \"5\"</p>"},{"location":"how-to/reading-lcd-displays/#preprocessing-pipeline","title":"Preprocessing Pipeline","text":"<ol> <li>Grayscale: Converts BGR to grayscale as the primary preprocessing path</li> <li>Sharpening: Unsharp masking applied by default to recover blurred segment edges</li> <li>CLAHE: Adaptive histogram equalization enhances local contrast</li> <li>Thresholding: Otsu's method creates binary image</li> <li>Morphological Cleaning: Removes noise</li> </ol> <p>This pipeline (including sharpening) is specifically optimized for LCD displays.</p>"},{"location":"how-to/reading-lcd-displays/#troubleshooting","title":"Troubleshooting","text":""},{"location":"how-to/reading-lcd-displays/#all-digits-show","title":"All digits show '?'","text":"<p>The segments aren't being recognized. Check:</p> <ol> <li>Review debug images: Run with <code>debug=True</code> and check preprocessing steps</li> <li>Verify calibration: Ensure digit ROIs are accurate</li> <li>Adjust threshold: Try <code>reader.segment_threshold = 0.3</code> for more sensitivity</li> <li>Check lighting: Improve lighting or lock camera exposure</li> </ol>"},{"location":"how-to/reading-lcd-displays/#some-digits-correct-others-wrong","title":"Some digits correct, others wrong","text":"<p>Calibration might be slightly off for some digits:</p> <ol> <li>Re-run calibration with more precise coordinates</li> <li>Check that all digits are the same size</li> <li>Ensure digit ROIs don't overlap</li> </ol>"},{"location":"how-to/reading-lcd-displays/#reading-changes-with-lighting","title":"Reading changes with lighting","text":"<p>Camera auto-exposure may be adjusting between captures. On Raspberry Pi with Picamera2, the camera settings are fixed after the first capture and the system waits for auto-exposure to settle before starting reads. On other platforms, use consistent external lighting to stabilize the scene.</p>"},{"location":"how-to/reading-lcd-displays/#unrecognized-segment-pattern","title":"Unrecognized segment pattern","text":"<p>If you see <code>Digit 0: (1, 0, 0, 1, 1, 0, 1) \u2192 ?</code>:</p> <p>This pattern isn't in the lookup table. Possible causes: - Display uses non-standard segment layout - Segment ROI positions don't match your display - Damaged or partially lit segments</p>"},{"location":"how-to/reading-lcd-displays/#reading-is-slow","title":"Reading is slow","text":"<p>Optimize for your use case:</p> <pre><code># Capture once, read multiple times\nframe = reader.capture_image()\n\n# Read without debug output\nresult = reader.read_display(frame=frame, debug=False)\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#best-practices","title":"Best Practices","text":""},{"location":"how-to/reading-lcd-displays/#for-reliable-readings","title":"For Reliable Readings","text":"<ol> <li>Calibrate with high-quality test image</li> <li>Use consistent lighting</li> <li>Lock camera exposure if possible</li> <li>Position camera perpendicular to display</li> <li>Ensure camera is in focus</li> </ol>"},{"location":"how-to/reading-lcd-displays/#during-operation","title":"During Operation","text":"<ul> <li>Check for <code>'?'</code> in results (unrecognized digits)</li> <li>Monitor first few readings to verify accuracy</li> <li>Re-calibrate if display position changes</li> <li>Keep camera lens clean</li> </ul>"},{"location":"how-to/reading-lcd-displays/#error-handling","title":"Error Handling","text":"<pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=4,\n)\n\nif not reader.load_calibration():\n    print(\"Calibration failed - run calibration first\")\n    exit(1)\n\nresult = reader.read_display()\n\nif result is None:\n    print(\"Camera capture failed\")\nelif '?' in result:\n    print(f\"Unclear reading: {result}\")\nelse:\n    print(f\"Valid reading: {result}\")\n    # Use result\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#tips-for-success","title":"Tips for Success","text":""},{"location":"how-to/reading-lcd-displays/#get-good-calibration","title":"Get Good Calibration","text":"<ul> <li>Use high contrast image for calibration</li> <li>Measure coordinates precisely</li> <li>Test calibration immediately</li> <li>Save calibration file for reuse</li> </ul>"},{"location":"how-to/reading-lcd-displays/#handle-different-displays","title":"Handle Different Displays","text":"<p>Different displays may need: - Adjusted segment threshold (0.3-0.7) - Re-calibration to map the new digit ROIs and segment polygons</p>"},{"location":"how-to/reading-lcd-displays/#monitor-accuracy","title":"Monitor Accuracy","text":"<p>Track reading accuracy:</p> <pre><code># Test against known values\nknown_value = \"1234\"\nresult = reader.read_display()\n\nif result == known_value:\n    print(\"[PASS] Accurate reading\")\nelse:\n    print(f\"[FAIL] Mismatch: expected {known_value}, got {result}\")\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#working-with-different-display-types","title":"Working with Different Display Types","text":""},{"location":"how-to/reading-lcd-displays/#3-digit-displays","title":"3-Digit Displays","text":"<pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=3,\n)\n# Calibrate for 3 digits\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#6-digit-displays","title":"6-Digit Displays","text":"<pre><code>from src.HardnessTester import HardnessTester\n\nreader = HardnessTester(\n    calibration_path=\"jubilee_api_config/lcd_calibration_shore_a.json\",\n    num_digits=6,\n)\n# Calibrate for 6 digits\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#displays-with-decimal-points","title":"Displays with Decimal Points","text":"<p>Current version doesn't detect decimal points. Work around by: - Knowing decimal position (e.g., reading \"1234\" as \"12.34\") - Post-processing the result</p> <pre><code>result = reader.read_display()  # \"1234\"\nvalue = int(result) / 100  # 12.34\n</code></pre>"},{"location":"how-to/reading-lcd-displays/#see-also","title":"See Also","text":"<ul> <li>HardnessTester API Reference</li> <li>Glossary: LCD terms</li> <li>Architecture</li> </ul>"},{"location":"how-to/reading-lcd-displays/#getting-help","title":"Getting Help","text":"<p>If readings are not working:</p> <ol> <li>Run with <code>debug=True</code> and review preprocessing images</li> <li>Check segment patterns to see what is being misdetected</li> <li>Adjust segment threshold or ROI positions</li> <li>Re-calibrate with a better test image</li> <li>Modify <code>system_config.json</code> CV parameters</li> <li>Consult API Reference for advanced options</li> </ol>"},{"location":"how-to/results/","title":"Interpreting Results","text":"<p>Job outputs are stored as JSON logs by <code>JobLog</code> and exposed by the backend file API.</p>"},{"location":"how-to/results/#overview","title":"Overview","text":"<p>Results are available in two forms:</p> <ul> <li>Live progress during a job (<code>GET /api/job/log</code>)</li> <li>Persisted job files after completion (<code>GET /api/files</code>, <code>GET /api/files/{filename}</code>)</li> </ul> <p>Files are stored under the configured job files directory (<code>paths.job_files_dir</code> in <code>system_config.json</code>, typically <code>frontend/api/files/</code>).</p> <p>On-disk file naming</p> <p>Completed logs use the pattern <code>{id:04d}_{YYYY-MM-DD}_{type}_{count}.json</code> where <code>id</code> is a sequential integer, <code>type</code> is <code>dispensing</code> or <code>hardness</code>, and <code>count</code> is the number of planned items. Hardness images for job <code>0012</code> live under <code>images/0012/</code>.</p>"},{"location":"how-to/results/#persisted-job-shape","title":"Persisted Job Shape","text":"<p><code>JobLog</code> writes two top-level sections:</p> <ul> <li><code>metadata</code> (job-level info like date, outcome, job type, <code>units_completed</code>)</li> <li><code>state</code> with item arrays (<code>molds</code> for dispensing, <code>samples</code> for hardness)</li> </ul> <p>Typical normalized fields used by the UI:</p> <pre><code>{\n  \"job_type\": \"dispensing\",\n  \"date\": \"2026-06-07\",\n  \"status\": \"successful\",\n  \"completed\": 6,\n  \"total\": 6,\n  \"items\": [\n    {\n      \"well_id\": \"0\",\n      \"target_weight\": 50.0,\n      \"actual_weight\": 49.98,\n      \"status\": \"complete\"\n    }\n  ]\n}\n</code></pre> <p>For hardness items, fields include <code>tray_index</code>, <code>sample_index</code>, <code>mode</code>, per-pass results (<code>result_shore_a</code>, <code>result_shore_d</code>), per-pass statuses, optional image paths, and <code>sample_error</code>.</p> Dispensing on diskHardness on disk <pre><code>{\n  \"metadata\": {\n    \"id\": 12,\n    \"date\": \"2026-06-07\",\n    \"job_type\": \"dispensing\",\n    \"outcome\": \"successful\",\n    \"units_completed\": 2\n  },\n  \"state\": {\n    \"molds\": [\n      {\n        \"well_id\": \"0\",\n        \"target_weight\": 50.0,\n        \"actual_weight\": 49.98,\n        \"status\": \"complete\"\n      }\n    ]\n  }\n}\n</code></pre> <pre><code>{\n  \"metadata\": {\n    \"id\": 13,\n    \"date\": \"2026-06-07\",\n    \"job_type\": \"hardness\",\n    \"outcome\": \"successful\",\n    \"units_completed\": 1\n  },\n  \"state\": {\n    \"samples\": [\n      {\n        \"tray_index\": 0,\n        \"sample_index\": 0,\n        \"mode\": \"shore_a_d\",\n        \"result_shore_a\": 61.0,\n        \"result_shore_d\": 58.5,\n        \"status_shore_a\": \"complete\",\n        \"status_shore_d\": \"complete\",\n        \"status\": \"complete\"\n      }\n    ]\n  }\n}\n</code></pre>"},{"location":"how-to/results/#what-to-check","title":"What to Check","text":"Check What to look for Outcome <code>successful</code>, <code>cancelled</code>, or <code>aborted</code> Completion <code>completed / total</code> Per-item status <code>complete</code>, <code>active</code>, <code>incomplete</code>, <code>error</code> Dispensing accuracy <code>actual_weight</code> vs <code>target_weight</code> Hardness completeness Expected passes (<code>shore_a</code>, <code>shore_d</code>, or both) <p>Start with outcome and completion</p> <p>A <code>successful</code> job with <code>completed == total</code> and all items <code>complete</code> is a clean run. Investigate any <code>error</code> or <code>incomplete</code> items before trusting the batch.</p>"},{"location":"how-to/results/#inspect-results","title":"Inspect Results","text":"APIAutomation UIGoogle Drive backup <pre><code>curl http://localhost:8000/api/files\ncurl http://localhost:8000/api/files/&lt;filename&gt;.json\n</code></pre> <ol> <li>Open the Data screen.</li> <li>Select a job JSON file.</li> <li>Review arc completion and per-cell statuses in the result grid.</li> </ol> <p>When <code>google_drive.enabled</code> is true, completed logs are exported to Drive as a per-job folder containing the JSON, derived CSV files, and hardness images. Check upload status on the Settings screen (<code>last_upload</code>, <code>last_error</code>).</p> <p>Required config fields when enabled:</p> <ul> <li><code>google_drive.credentials_file</code></li> <li><code>google_drive.drive_folder_id</code> (non-empty)</li> <li><code>google_drive.retry_interval_seconds</code></li> </ul> <p>Drive export requires complete metadata</p> <p>Upload fails if <code>metadata.job_type</code>, <code>metadata.id</code>, or the expected <code>state.molds</code> / <code>state.samples</code> section is missing from the JSON file.</p>"},{"location":"how-to/results/#see-also","title":"See Also","text":"<ul> <li>Using the Jubilee Powder UI</li> <li>Run Operations on New Data</li> <li>Web Frontend Reference</li> </ul>"},{"location":"how-to/run-new-data/","title":"Run Operations on New Data","text":"<p>Use this workflow when you want to run jobs against a new recipe set or a different mold or sample selection.</p>"},{"location":"how-to/run-new-data/#overview","title":"Overview","text":"Web UI (recommended)Python scripts <p>For daily operation, use the browser interface:</p> <ol> <li>Building and Running the Web UI - start the server</li> <li>Using the Jubilee Powder UI - configure, connect, and submit jobs</li> <li>Interpreting Results - review job logs on the Data screen</li> </ol> <p>For scripted runs, use <code>JubileeManager</code> with config-driven setup. See Quick Start Guide.</p>"},{"location":"how-to/run-new-data/#prerequisites","title":"Prerequisites","text":"<p>Before your first run on new data</p> <ul> <li>[ ] <code>system_config.json</code> has correct machine IP, scale port, dispenser counts, and tamp or trickler settings</li> <li>[ ] <code>motion_platform_positions.json</code> contains the positions used by your workflow</li> <li>[ ] Deck is clear and labware matches configuration</li> <li>[ ] Machine is idle and connected (or ready to connect)</li> </ul> <p>See Configuration Guide and Position Config API.</p>"},{"location":"how-to/run-new-data/#steps","title":"Steps","text":""},{"location":"how-to/run-new-data/#1-verify-configuration","title":"1. Verify configuration","text":"<p>Confirm <code>jubilee_api_config/</code> matches your physical setup. If you moved labware, update position config first.</p>"},{"location":"how-to/run-new-data/#2-prepare-input-payload","title":"2. Prepare input payload","text":"<p>Dispensing job payload shape:</p> <pre><code>{\n  \"job_type\": \"dispensing\",\n  \"wells\": [\n    { \"well_id\": \"0\", \"target_weight\": 50.0 },\n    { \"well_id\": \"1\", \"target_weight\": 45.0 }\n  ]\n}\n</code></pre> <p>Hardness job payload shape:</p> <pre><code>{\n  \"job_type\": \"hardness\",\n  \"samples\": [\n    { \"tray_index\": 0, \"sample_index\": 0, \"mode\": \"shore_a\" },\n    { \"tray_index\": 0, \"sample_index\": 1, \"mode\": \"shore_d\" }\n  ]\n}\n</code></pre>"},{"location":"how-to/run-new-data/#3-run-and-monitor","title":"3. Run and monitor","text":"<ol> <li>Start from idle state.</li> <li>Submit one small test job first (single well or sample).</li> <li>Monitor progress on the Home screen (<code>running</code>, <code>completed</code>, <code>current_item</code>, <code>error</code>).</li> </ol> <p>Supervise the first run</p> <p>Sit at the machine with access to the emergency stop for the first execution on new data. See Best Practices.</p>"},{"location":"how-to/run-new-data/#4-review-output","title":"4. Review output","text":"<p>Results are written as job log files in <code>frontend/api/files/</code>. Use the Data screen or <code>/api/files</code> endpoints to inspect them.</p> <p>See Interpreting Results.</p>"},{"location":"how-to/run-new-data/#5-scale-up","title":"5. Scale up","text":"<p>After a successful validation batch, run the full recipe set.</p>"},{"location":"how-to/run-new-data/#safety-notes","title":"Safety Notes","text":"<ul> <li>Do not bypass configured motion transitions.</li> <li>If the machine enters <code>error</code>, reconnect before starting a new job.</li> <li>Prefer small validation batches before full production runs.</li> </ul>"},{"location":"how-to/run-new-data/#see-also","title":"See Also","text":"<ul> <li>Using the Jubilee Powder UI</li> <li>Interpreting Results</li> <li>Web Frontend Reference</li> <li>Best Practices</li> </ul>"},{"location":"how-to/running/","title":"Building and Running the Web UI","text":"<p>The web UI has two operating modes. Use dev mode when actively changing frontend code; use production mode for normal lab operation and for the Raspberry Pi kiosk.</p> DevelopmentProduction <p>Vite dev server + uvicorn (two processes). Hot reload for frontend changes.</p> <p>Open: <code>http://localhost:5173</code> (Vite proxies <code>/api</code> and <code>/ws</code> to port 8000)</p> <p>Single uvicorn process serves both the REST/WebSocket API and the built React app.</p> <p>Open: <code>http://localhost:8000</code></p> <p>Prerequisites</p> <ul> <li>Python virtual environment with <code>requirements.txt</code> installed</li> <li>Node.js 20.19+ or 22.12+ for frontend builds and dev server</li> <li><code>jubilee_api_config/</code> configured for your machine</li> </ul>"},{"location":"how-to/running/#development-mode","title":"Development mode","text":"<p>Requires two terminals running simultaneously from the project root.</p> <p>Terminal 1 - backend: <pre><code>uvicorn frontend.server:app --host 0.0.0.0 --port 8000 --reload\n</code></pre></p> <p>To run without physical hardware, either set <code>\"mock_hardware\": true</code> under <code>server</code> in <code>jubilee_api_config/system_config.json</code>, or start the backend with:</p> <pre><code>JUBILEE_MOCK_HARDWARE=1 uvicorn frontend.server:app --host 0.0.0.0 --port 8000 --reload\n</code></pre> <p>When the env var is set, it overrides the JSON value. <code>GET /api/config</code> reports the effective mode in <code>mock_hardware</code>.</p> <p>Terminal 2 - frontend dev server: <pre><code>cd frontend\nnpm install   # only needed the first time\nnpm run dev\n</code></pre></p> <p>Node.js version</p> <p>Vite 8 requires Node.js 20.19+ or 22.12+. The version that ships with Raspberry Pi OS via <code>apt</code> (Node 18) is too old. <code>deploy/install.sh</code> handles this automatically by pulling Node 22 LTS from NodeSource. For local development on other machines, check your version with <code>node --version</code> and upgrade if needed.</p> <p>WSL: use Linux Node and npm</p> <p>If <code>which npm</code> points at <code>/mnt/c/Program Files/nodejs/npm</code>, or <code>npm run dev</code> fails with Cannot find native binding / <code>@rolldown/binding-linux-x64-gnu</code>, dependencies were installed for the wrong OS. Install Node 22 inside WSL (for example fnm), ensure <code>node -v</code> is 22+ and <code>which npm</code> is under your Linux home (not <code>/mnt/c</code>), then reinstall:</p> <pre><code>cd frontend\nrm -rf node_modules package-lock.json\nnpm install\n</code></pre> <p>Open <code>http://localhost:5173</code> in your browser. The Vite dev server proxies <code>/api</code> and <code>/ws</code> requests to the uvicorn backend on port 8000, so both processes must be running at the same time. Hot-module replacement means frontend changes are reflected in the browser immediately without restarting anything.</p>"},{"location":"how-to/running/#production-mode","title":"Production mode","text":"<p>In production a single uvicorn process serves both the REST/WebSocket API and the compiled React app. There is no Vite dev server.</p>"},{"location":"how-to/running/#step-1-build-the-frontend","title":"Step 1 - build the frontend","text":"<p>Run once (and again after any frontend code change):</p> <pre><code>cd frontend\nnpm install   # only needed the first time or after package.json changes\nnpm run build\n</code></pre> <p>This writes static files to <code>frontend/dist/</code>. The backend detects that directory on startup and begins serving it automatically.</p>"},{"location":"how-to/running/#step-2-start-the-backend","title":"Step 2 - start the backend","text":"<pre><code>uvicorn frontend.server:app --host 0.0.0.0 --port 8000\n</code></pre> <p>Open <code>http://localhost:8000</code>. Both the UI and the API are served from the same port. If <code>frontend/dist/</code> does not exist the server still starts, but navigating to the root returns a 404 until you run <code>npm run build</code>.</p> <p>No --reload in production</p> <p>Omit <code>--reload</code> in production. It watches the filesystem and adds unnecessary overhead.</p>"},{"location":"how-to/running/#raspberry-pi-kiosk-auto-launch","title":"Raspberry Pi kiosk (auto-launch)","text":"<p>The <code>deploy/</code> directory contains everything needed to run the system as a polished auto-launching kiosk on Raspberry Pi OS (Bullseye / X11).</p>"},{"location":"how-to/running/#one-time-install","title":"One-time install","text":"<pre><code>cd ~/jubilee-powder\nsudo bash deploy/install.sh\nsudo reboot\n</code></pre> <p><code>install.sh</code> does the following in order:</p> <ol> <li>Installs system packages (<code>chromium-browser</code>, <code>unclutter</code>, Node.js, Python venv tools)</li> <li>Builds the React frontend (<code>npm run build</code>)</li> <li>Creates <code>.venv/</code> and installs all Python dependencies</li> <li>Installs and enables the <code>jubilee-backend</code> systemd service (auto-restarts on failure)</li> <li>Writes an LXDE autostart config that opens Chromium in kiosk mode after login</li> <li>Enables desktop auto-login via <code>raspi-config</code></li> </ol> <p>After the reboot the Pi follows this sequence automatically:</p> <ul> <li>Desktop auto-login fires</li> <li>systemd starts <code>jubilee-backend</code> (uvicorn on port 8000), waits for the network</li> <li>LXDE autostart runs <code>deploy/kiosk.sh</code>, which polls <code>GET /api/status</code> until the backend is ready, then opens Chromium full-screen on <code>http://localhost:8000</code></li> </ul> <p>The result is a touch-friendly full-screen UI with no visible desktop, browser chrome, or terminal.</p>"},{"location":"how-to/running/#updating-after-a-git-pull","title":"Updating after a git pull","text":"<p>Run this script after every <code>git pull</code> on the Pi - it rebuilds the frontend, updates Python dependencies, and restarts the backend service:</p> <pre><code>cd ~/jubilee-powder\nsudo bash deploy/update.sh\n</code></pre> <p>If you only changed Python files and want to skip the frontend rebuild, you can restart the service directly:</p> <pre><code>sudo systemctl restart jubilee-backend@pi\n</code></pre> <p>If you only changed frontend files and want to skip the service restart, rebuild without sudo:</p> <pre><code>cd ~/jubilee-powder/frontend\nnpm run build\n</code></pre> <p>uvicorn serves files directly from <code>frontend/dist/</code> so the next browser refresh picks up the new build without a restart.</p>"},{"location":"how-to/running/#managing-the-backend-service","title":"Managing the backend service","text":"<pre><code># Check service health\nsystemctl status jubilee-backend@pi\n\n# Follow live logs\njournalctl -u jubilee-backend@pi -f\n\n# Restart (e.g. after a Python code change)\nsudo systemctl restart jubilee-backend@pi\n</code></pre> <p>Replace <code>pi</code> with your username if the account name differs.</p>"},{"location":"how-to/running/#manual-launch-and-troubleshooting-pi","title":"Manual launch and troubleshooting (Pi)","text":"<p>If the automatic kiosk does not come up after reboot, diagnose and recover one layer at a time.</p>"},{"location":"how-to/running/#1-check-and-start-the-backend-manually","title":"1. Check and start the backend manually","text":"<pre><code># See why the service failed\nsystemctl status jubilee-backend@pi\njournalctl -u jubilee-backend@pi -n 50\n\n# If it is stopped, start it\nsudo systemctl start jubilee-backend@pi\n\n# Or bypass systemd entirely and run uvicorn directly\ncd ~/jubilee-powder\n.venv/bin/uvicorn frontend.server:app --host 0.0.0.0 --port 8000\n</code></pre> <p>Confirm the backend is up before doing anything else:</p> <pre><code>curl http://localhost:8000/api/status\n</code></pre> <p>A JSON response means the backend is healthy. A \"connection refused\" error means uvicorn is not running.</p>"},{"location":"how-to/running/#2-open-the-browser-manually","title":"2. Open the browser manually","text":"<p>Once the backend is running, open a terminal on the Pi desktop and run:</p> <pre><code>chromium-browser http://localhost:8000\n</code></pre> <p>To test full kiosk mode manually (exits with Alt+F4 or closing the window is blocked - use Ctrl+Alt+T to open a terminal):</p> <pre><code>chromium-browser --kiosk http://localhost:8000\n</code></pre>"},{"location":"how-to/running/#3-re-run-the-kiosk-script-manually","title":"3. Re-run the kiosk script manually","text":"<p><code>deploy/kiosk.sh</code> is the same script that autostart calls. Running it from a terminal on the desktop reproduces exactly what autostart would do:</p> <pre><code>bash ~/jubilee-powder/deploy/kiosk.sh\n</code></pre> <p>It will print \"Waiting for backend...\" and then open Chromium once the backend responds.</p>"},{"location":"how-to/running/#4-check-the-autostart-configuration","title":"4. Check the autostart configuration","text":"<p>If the backend works fine but Chromium never opens automatically at login:</p> <pre><code># Verify the autostart file exists and has the right content\ncat ~/.config/lxsession/LXDE-pi/autostart\n\n# Re-run install.sh to regenerate it\ncd ~/jubilee-powder\nsudo bash deploy/install.sh\n</code></pre> <p>The autostart file should contain a line like <code>@/home/pi/jubilee-powder/deploy/kiosk.sh</code>. If the path does not match where the repo was cloned, edit the line to match.</p>"},{"location":"how-to/running/#5-check-that-the-frontend-was-built","title":"5. Check that the frontend was built","text":"<p>If the backend starts but navigating to <code>http://localhost:8000</code> returns a 404:</p> <pre><code>ls ~/jubilee-powder/frontend/dist/\n</code></pre> <p>If the directory is missing or empty, the build was never run. Build it now:</p> <pre><code>cd ~/jubilee-powder/frontend\nnpm run build\n</code></pre> <p>No backend restart is needed - uvicorn picks up the new <code>dist/</code> on the next request.</p>"},{"location":"how-to/running/#quick-reference","title":"Quick reference","text":"Task Command Start dev backend <code>uvicorn frontend.server:app --port 8000 --reload</code> Start dev frontend <code>cd frontend &amp;&amp; npm run dev</code> Build for production <code>cd frontend &amp;&amp; npm run build</code> Start production server <code>uvicorn frontend.server:app --host 0.0.0.0 --port 8000</code> First-time Pi install <code>sudo bash deploy/install.sh</code> Update after git pull (Pi) <code>sudo bash deploy/update.sh</code> Rebuild UI on Pi <code>cd frontend &amp;&amp; npm run build</code> View backend logs (Pi) <code>journalctl -u jubilee-backend@pi -f</code> Restart backend (Pi) <code>sudo systemctl restart jubilee-backend@pi</code>"},{"location":"how-to/running/#see-also","title":"See Also","text":"<ul> <li>Using the Jubilee Powder UI</li> <li>Configuration Guide</li> <li>Best Practices</li> </ul>"},{"location":"how-to/using-gui/","title":"Using the Jubilee Powder UI","text":"<p>This guide walks through using the Jubilee Powder web interface for powder dispensing and hardness testing operations.</p>"},{"location":"how-to/using-gui/#overview","title":"Overview","text":"<p>The Jubilee Powder UI is a browser-based interface that provides:</p> <ul> <li>Hardware configuration and connection management</li> <li>Powder dispensing job setup and real-time monitoring</li> <li>Hardness testing job setup and monitoring</li> <li>Historical job log browsing</li> </ul> <p>Prerequisites</p> <ul> <li>Web server running (see Building and Running the Web UI)</li> <li>Jubilee powered on and network-reachable</li> <li>Scale connected; dispenser piston counts configured in Settings</li> </ul>"},{"location":"how-to/using-gui/#choose-your-depth","title":"Choose Your Depth","text":"Operator WorkflowDeveloper Notes <p>Follow this page top-to-bottom for setup, job execution, monitoring, and troubleshooting.</p> <ul> <li>Backend API contract: Web Frontend Reference</li> <li>Frontend state model: Jubilee Store Reference</li> <li>System design context: Architecture</li> </ul>"},{"location":"how-to/using-gui/#starting-the-ui","title":"Starting the UI","text":"<p>Use Building and Running the Web UI for the exact startup commands.</p> <p>Quick summary:</p> <ul> <li>Dev mode: run backend (<code>uvicorn</code>) and frontend (<code>npm run dev</code>) in two terminals.</li> <li>Production mode: run backend only; it serves the built frontend.</li> <li>Mock hardware: set <code>server.mock_hardware: true</code> in <code>system_config.json</code>, or use <code>JUBILEE_MOCK_HARDWARE=1</code>.</li> </ul>"},{"location":"how-to/using-gui/#interface-layout","title":"Interface Layout","text":"<p>Every screen shares the same persistent layout:</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  NavRail  \u2502   Screen content                        \u2502\n\u2502           \u2502                                         \u2502\n\u2502  Home     \u2502   (active screen fills this area)       \u2502\n\u2502  Dispense \u2502                                         \u2502\n\u2502  Hardness \u2502                                         \u2502\n\u2502  Data     \u2502                                         \u2502\n\u2502  Manual   \u2502                                         \u2502\n\u2502  Settings \u2502                                         \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  BottomBar: weight  |  state badge  |  screen name  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>The NavRail on the left provides navigation between screens. The BottomBar at the bottom always shows the live scale weight and machine connection state.</p>"},{"location":"how-to/using-gui/#machine-state-colors","title":"Machine State Colors","text":"State Badge Meaning Disconnected Gray No hardware connection Connecting... Yellow Homing sequence in progress Connected Green Ready for a job Running Yellow Job executing Error Red Failure \u2014 must reconnect"},{"location":"how-to/using-gui/#initial-setup","title":"Initial Setup","text":""},{"location":"how-to/using-gui/#1-configure-hardware","title":"1. Configure Hardware","text":"<p>Open the Settings screen (bottom of the NavRail).</p> <p>Enter your hardware configuration:</p> <ul> <li>Number of Powder Dispensers \u2014 how many piston dispenser trays are loaded</li> <li>Pistons per Dispenser \u2014 piston capacity of each tray (total is shown as a hint)</li> <li>Jubilee IP Address or Hostname \u2014 e.g. <code>jubilee.local</code> or <code>192.168.1.100</code></li> <li>Scale Serial Port \u2014 e.g. <code>/dev/ttyUSB0</code> (Linux) or <code>COM3</code> (Windows)</li> </ul> <p>Note</p> <p>Settings fields are locked while hardware is connected. Reconfiguration requires disconnecting first.</p>"},{"location":"how-to/using-gui/#2-connect","title":"2. Connect","text":"<p>Click Connect to Jubilee.</p> <p>The server returns immediately and begins the connection sequence in the background. Watch the state badge in the BottomBar:</p> <ul> <li><code>Connecting\u2026</code> (yellow) \u2014 homing sequence running</li> <li><code>Connected</code> (green) \u2014 ready for a job</li> <li><code>Error</code> (red) \u2014 connection failed; check the IP address, serial port, and hardware power</li> </ul> <p>Connection typically takes 30-60 seconds due to homing.</p>"},{"location":"how-to/using-gui/#running-a-powder-dispensing-job","title":"Running a Powder Dispensing Job","text":"<p>Navigate to the Dispensing screen.</p>"},{"location":"how-to/using-gui/#step-1-select-wells","title":"Step 1: Select Wells","text":"<p>Click individual wells in the dispensing grid to select them (18 mold slots in a 3\u00d77 layout with scale access gaps in the bottom row). Click again to deselect.</p> <p>Toolbar shortcuts:</p> <ul> <li>Select All \u2014 select all 18 wells</li> <li>Clear \u2014 deselect everything</li> </ul> <p>The selection count is shown in the toolbar.</p>"},{"location":"how-to/using-gui/#step-2-set-target-weights","title":"Step 2: Set Target Weights","text":"<p>Click Set Weights (only enabled when wells are selected).</p> <p>Enter the target weight in grams. The same weight is applied to all currently selected wells. Repeat the process with a different selection to assign different weights to different wells.</p> <p>Well colours in the grid reflect their state:</p> Colour Meaning Dark (default) Not selected Highlighted Selected, no weight set With label Selected, target weight assigned"},{"location":"how-to/using-gui/#step-3-start-job","title":"Step 3: Start Job","text":"<p>Click Start Job (enabled only when the machine is idle and all selected wells have a target weight set).</p> <p>The job is submitted to the server and begins immediately. The screen transitions to the Home screen automatically \u2014 or you can navigate there to monitor progress.</p> <p>Tip</p> <p>To assign different weights to different wells, select a subset, set the weight, then select a different subset and set a different weight before clicking Start Job.</p>"},{"location":"how-to/using-gui/#running-a-hardness-test","title":"Running a Hardness Test","text":"<p>Navigate to the Hardness screen.</p>"},{"location":"how-to/using-gui/#step-1-select-samples","title":"Step 1: Select Samples","text":"<p>Click wells in the hardness tray grid to select sample positions to test. Use Select All or Clear in the toolbar as needed.</p>"},{"location":"how-to/using-gui/#step-2-assign-test-mode","title":"Step 2: Assign Test Mode","text":"<p>With samples selected, click one of the mode buttons:</p> Button Mode Description Shore A <code>shore_a</code> Standard Shore A hardness test A + D <code>shore_a_d</code> Combined Shore A and D measurement Shore D <code>shore_d</code> Standard Shore D hardness test <p>Repeat to assign different modes to different samples.</p>"},{"location":"how-to/using-gui/#step-3-start-test","title":"Step 3: Start Test","text":"<p>Click Start Test. Progress is monitored on the Home screen.</p>"},{"location":"how-to/using-gui/#monitoring-a-running-job","title":"Monitoring a Running Job","text":"<p>Navigate to the Home screen during a job.</p>"},{"location":"how-to/using-gui/#display-layout","title":"Display Layout","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Arc progress  \u2502  Well/sample result grid         \u2502\n\u2502                \u2502                                  \u2502\n\u2502  Completed/    \u2502  Color per cell:                 \u2502\n\u2502  Total         \u2502    Gray    = excluded            \u2502\n\u2502  Elapsed time  \u2502    Blue    = pending             \u2502\n\u2502                \u2502    Amber   = active (current)    \u2502\n\u2502  [Cancel]      \u2502    Green   = complete            \u2502\n\u2502  [Abort]       \u2502                                  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\u2502  Status line                                      \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"how-to/using-gui/#stopping-a-job","title":"Stopping a Job","text":"<p>Two stop options are available while a job is running:</p> <ul> <li>Cancel \u2014 graceful stop. The machine finishes the current mold, stows the active   tool, and returns to <code>idle</code>. Cancellation is applied only after the current mold   or sample completes. A confirmation dialog is shown before proceeding.</li> <li>Abort \u2014 emergency stop. Sends an M112 signal to the controller. The machine halts   immediately and enters <code>error</code> state. Use only when necessary; the machine must be   fully reconnected before starting a new job.</li> </ul> <p>Warning</p> <p>The Abort button requires two presses to confirm. On the first press the button highlights red and changes to \"Press Again!\". A second press within 3 seconds fires the emergency stop. If not confirmed within 3 seconds the button disarms itself.</p>"},{"location":"how-to/using-gui/#browsing-historical-data","title":"Browsing Historical Data","text":"<p>Navigate to the Data screen.</p> <p>The file list shows all job log files in <code>frontend/api/files/</code>, sorted newest-first. Each row shows the filename, size, and last-modified date.</p> <p>Click any <code>.json</code> file to open a read-only job result view identical in layout to the Home screen, showing the arc progress and per-well/sample result grid with outcome colour coding.</p> <p>Click Back in the header to return to the file list. The Refresh button reloads the file list from the server.</p>"},{"location":"how-to/using-gui/#settings-screen-reference","title":"Settings Screen Reference","text":"Field Default (from <code>system_config.json</code>) Description Number of Powder Dispensers <code>machine.num_dispensers</code> (1) Number of piston dispenser trays Pistons per Dispenser <code>machine.pistons_per_dispenser</code> (2) Pistons per tray Jubilee IP / Hostname <code>machine.duet_ip</code> (<code>192.168.1.2</code>) Machine network address Scale Serial Port <code>machine.scale_port</code> (<code>/dev/ttyUSB0</code>) Serial port for the A&amp;D FX-120i scale"},{"location":"how-to/using-gui/#manual-control","title":"Manual Control","text":"<p>Warning</p> <p>Manual control is for advanced users only. Using the Jubilee's built-in web interface (Duet Web Control) moves the hardware outside the positions tracked by the automation state machine. Recalibration is required before running automated jobs afterward.</p> <p>Navigate to the Manual screen and click Open Jubilee Web UI. A confirmation dialog explains the implications. On confirmation:</p> <ol> <li>The automation system disconnects cleanly (state machine reset)</li> <li>The Jubilee web UI opens in a new browser tab at <code>http://{jubilee_ip}</code></li> </ol> <p>After using manual control:</p> <ol> <li>Return to Settings</li> <li>Disconnect then reconnect</li> <li>Verify hardware positions are correct before starting a new job</li> </ol>"},{"location":"how-to/using-gui/#troubleshooting","title":"Troubleshooting","text":""},{"location":"how-to/using-gui/#connection-fails-immediately","title":"Connection fails immediately","text":"<ul> <li>Verify the IP address or hostname in Settings</li> <li>Confirm the Jubilee is powered on and reachable (<code>ping jubilee.local</code>)</li> <li>Check the scale serial port is correct and the cable is connected</li> <li>Look at the server terminal output for more detail</li> </ul>"},{"location":"how-to/using-gui/#machine-stuck-in-homing-state","title":"Machine stuck in <code>homing</code> state","text":"<p>The homing sequence runs until all axes are found. If it never transitions to <code>idle</code>:</p> <ul> <li>Check for physical obstructions on the axes</li> <li>Review the server logs for <code>JubileeManager.connect()</code> errors</li> <li>Disconnect and reconnect to retry</li> </ul>"},{"location":"how-to/using-gui/#job-fails-to-start","title":"Job fails to start","text":"<p><code>Start Job</code> is disabled unless:</p> <ul> <li>Machine state is <code>idle</code></li> <li>At least one well is selected</li> <li>All selected wells have a target weight set</li> </ul> <p>If the button click produces an error message in the status line, it will show the server's rejection reason (e.g. \"Machine is not idle\").</p>"},{"location":"how-to/using-gui/#out-of-pistons","title":"Out of pistons","text":"<p>The server does not currently enforce piston count before job submission. If a dispenser runs out of pistons mid-job, the job will fail at that well.</p> <p>To update piston counts after a manual reload, use the REST endpoint directly:</p> <pre><code>curl -X PUT http://localhost:8000/api/dispensers/0 \\\n     -H \"Content-Type: application/json\" \\\n     -d '{\"num_pistons\": 10}'\n</code></pre>"},{"location":"how-to/using-gui/#scale-shows-in-bottombar","title":"Scale shows <code>---</code> in BottomBar","text":"<p>Scale weight is only read when hardware is connected. Confirm the machine state is <code>idle</code> or <code>running</code>. If connected but reading zero, check the serial connection.</p>"},{"location":"how-to/using-gui/#browser-tab-shows-stale-data-after-server-restart","title":"Browser tab shows stale data after server restart","text":"<p>The WebSocket reconnects automatically within 1.5 s of a server restart. If the screen still looks stale after a few seconds, reload the browser tab.</p>"},{"location":"how-to/using-gui/#see-also","title":"See Also","text":"<ul> <li>Web Frontend Reference \u2014 REST API and architecture details</li> <li>Jubilee Store Reference \u2014 ViewModel state and actions</li> <li>Architecture \u2014 Full system architecture</li> <li>Using the Jubilee Web UI \u2014 Duet Web Control guide</li> </ul>"},{"location":"how-to/web-ui/","title":"Using the Jubilee Web Interfaces","text":"<p>This guide explains the two web interfaces available for the Jubilee system.</p>"},{"location":"how-to/web-ui/#overview","title":"Overview","text":"Jubilee Powder UIDuet Web Control (DWC) <p>The custom React browser application for running automated dispensing and hardness testing jobs. This is the primary interface for day-to-day operation.</p> <p>See Using the Automation UI for the full user guide.</p> <p>The built-in Jubilee controller interface for low-level manual control. Use this for troubleshooting, manual jogs, and G-code commands.</p> <p>The Automation UI Manual Control screen safely disconnects before opening DWC. See Manual Control.</p> <p>Manual control invalidates automation state</p> <p>Any manual movements made through DWC move the machine outside the positions tracked by the automation state machine. Always disconnect and reconnect the automation system after using DWC before running automated jobs.</p>"},{"location":"how-to/web-ui/#duet-web-control-dwc","title":"Duet Web Control (DWC)","text":"<p>The Jubilee Powder UI provides a Manual Control screen that safely disconnects the automation system before opening DWC in a new browser tab. This is the recommended way to access DWC \u2014 see Using the Automation UI for details. The steps below cover direct DWC access when the automation server is not running.</p>"},{"location":"how-to/web-ui/#accessing-dwc","title":"Accessing DWC","text":"<ol> <li>Open a web browser</li> <li>Navigate to your Jubilee's IP address: <code>http://192.168.1.100</code> (use your configured IP)</li> <li>The Duet Web Control interface will load</li> </ol>"},{"location":"how-to/web-ui/#dwc-interface-overview","title":"DWC Interface Overview","text":"<p>The DWC provides low-level control of the Jubilee:</p> <ul> <li>Machine Control: Manual jog controls for all axes</li> <li>Console: Send G-code commands directly</li> <li>Status: View current positions, temperatures, and states</li> <li>Files: Upload and manage G-code files</li> <li>Macros: Run predefined macro files</li> </ul>"},{"location":"how-to/web-ui/#common-dwc-tasks","title":"Common DWC Tasks","text":""},{"location":"how-to/web-ui/#manual-movement","title":"Manual Movement","text":"<ol> <li>Click the Machine Control tab</li> <li>Select the axis to move (X, Y, Z, U, V)</li> <li>Choose movement distance (0.1mm, 1mm, 10mm, 100mm)</li> <li>Click directional buttons to move</li> </ol> <p>Safety</p> <p>Manual movements in DWC bypass the state machine validation. Be careful not to cause collisions. Misclicks can be catastrophic. </p>"},{"location":"how-to/web-ui/#homing-axes","title":"Homing Axes","text":"<ol> <li>In Machine Control, find the \"Home\" section</li> <li>Click Home All to home all axes, or</li> <li>Click individual axis buttons (Home X, Home Y, etc.)</li> </ol>"},{"location":"how-to/web-ui/#running-macros","title":"Running Macros","text":"<ol> <li>Click the Macros tab (or Job tab in some versions)</li> <li>Browse available macros</li> <li>Click a macro name to execute it</li> </ol> <p>Useful macros: - <code>homeall.g</code>: Home all axes - <code>tool_lock.g</code>: Lock the current tool - <code>tool_unlock.g</code>: Unlock the current tool</p>"},{"location":"how-to/web-ui/#sending-g-code-commands","title":"Sending G-code Commands","text":"<ol> <li>Click the Console tab</li> <li>Type G-code in the input box at the bottom</li> <li>Press Enter to send</li> </ol> <p>Common G-code commands: <pre><code>G28          ; Home all axes\nG90          ; Absolute positioning\nG91          ; Relative positioning\nG1 X100 Y50 F5000  ; Move to X=100, Y=50 at 5000 mm/min\nM114         ; Report current position\nM999         ; Reset controller after emergency stop\n</code></pre></p>"},{"location":"how-to/web-ui/#monitoring-status","title":"Monitoring Status","text":"<p>The Status section shows: - Current X, Y, Z, U, V positions - Active tool (T0, T1, etc.) - Machine state (idle, processing, paused) - Temperature readings (if applicable)</p>"},{"location":"how-to/web-ui/#when-to-use-dwc","title":"When to Use DWC","text":"<p>Use Duet Web Control for:</p> <ul> <li>Manual troubleshooting</li> <li>Testing individual movements</li> <li>Verifying positions</li> <li>Emergency control</li> <li>Low-level debugging</li> </ul> <p>Do not use for:</p> <ul> <li>Automated operations (use Python API instead)</li> <li>Complex multi-step procedures (use JubileeManager)</li> <li>Production workflows (use custom scripts)</li> </ul>"},{"location":"how-to/web-ui/#advanced-features","title":"Advanced Features","text":""},{"location":"how-to/web-ui/#creating-custom-macros-dwc","title":"Creating Custom Macros (DWC)","text":"<p>You can create custom G-code macros for repetitive tasks:</p> <ol> <li>In DWC, go to System tab</li> <li>Navigate to <code>/sys/macros/</code></li> <li>Create new file (e.g., <code>my_custom_move.g</code>)</li> <li>Write G-code:</li> </ol> <pre><code>; My Custom Move Macro\nG28                      ; Home all\nG1 X100 Y100 Z50 F5000  ; Move to position\nM400                     ; Wait for moves to complete\nM291 P\"Movement complete\" S1  ; Show message\n</code></pre> <ol> <li>Save file</li> <li>Macro appears in Macros list</li> </ol>"},{"location":"how-to/web-ui/#monitoring-from-multiple-devices","title":"Monitoring from Multiple Devices","text":"<p>You can access DWC from multiple browsers simultaneously:</p> <ul> <li>One person monitors in lab</li> <li>Another controls remotely</li> <li>All see the same state</li> </ul> <p>Coordination Required</p> <p>If multiple people send commands, they may conflict. Coordinate who is in control.</p>"},{"location":"how-to/web-ui/#integrating-with-external-tools","title":"Integrating with External Tools","text":"<p>The API can be controlled programmatically:</p> <pre><code># External control example\nfrom src.JubileeManager import JubileeManager\n\n# Create manager (could be controlled by another application)\nmanager = JubileeManager()\nif manager.connect():\n    # Perform operations\n    pass\n</code></pre>"},{"location":"how-to/web-ui/#see-also","title":"See Also","text":"<ul> <li>Using the Automation UI - primary interface for dispensing and hardness jobs</li> <li>Configuration Guide for custom operations</li> <li>Run Operations on New Data</li> <li>Interpreting Results</li> <li>Best Practices</li> <li>JubileeManager API for programmatic control</li> </ul>"}]}